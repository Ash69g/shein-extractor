# سجل مشروع SHEIN Extractor

هذا الملف هو المرجع الرسمي لتقدم المشروع. يوثّق المتطلبات، القرارات، العمليات، الأوامر، الاكتشافات، الاختبارات، المشكلات، والنتائج. يجب تحديثه بعد كل خطوة جوهرية.

## 2026-07-23 — رفع دقة صور تقارير PDF المضغوطة

- رُفع الحد الأقصى الافتراضي لأبعاد صورة المنتج من `360px` إلى `512px`.
- الهدف هو تحسين وضوح الصور داخل التقرير مع استمرار تصغيرها وضغطها قبل تضمينها في PDF.
- بقيت جودة JPEG الافتراضية عند `78` للموازنة بين وضوح الصورة وحجم الملف.
- خفّض القياس المحلي صورة الاختبار من `2,009,351` بايت إلى `66,501` بايت بأبعاد `512 × 384`، أي انخفاضًا بنسبة `96.7%`.
- نجحت مجموعة الاختبارات الكاملة: `36 passed, 1 warning in 3.13s`.
- نجح `ruff check .` وكانت النتيجة `All checks passed!`.
- رُفع وسم صورة Docker في `compose.hostinger.yml` من `0.2.0` إلى `0.2.1` حتى يمكن نشر التحسين دون الالتباس مع صورة الخادم السابقة.
- أُنشئت حزمة النشر النظيفة `shein-extractor-hostinger-20260723-122130.tar.gz` بحجم `48,668` بايت، دون `.env` أو بيانات التشغيل أو الاختبارات أو ملفات التخزين المؤقت.
- بصمة الحزمة هي `C8461C0ED660636DD4CE41D9522AC3FF6EA5FFBFB52F7EA84A09BECD2C6DEA3C`.
- تم التحقق من احتواء الحزمة على حد الصور `512px`، واعتماد `Pillow==12.3.0`، ووسم الصورة `shein-extractor-api:0.2.1`.
- يلزم رفع الحزمة الجديدة وإعادة بناء حاوية `shein-api` قبل قياس حجم تقرير حقيقي على VPS.

## 1. هدف المشروع

بناء برنامج Windows بواجهة عربية يستقبل رابط مشاركة من SHEIN، يحلله، ثم يعرض جميع المنتجات وبياناتها بصورة واضحة. قد يحتوي الرابط منتجًا واحدًا أو عدة منتجات.

### المتطلبات الثابتة

- دعم روابط `onelink.shein.com` الحديثة وروابط المشاركة الوسيطة والنهائية.
- استخراج جميع المنتجات دون استثناء.
- عدم تجاهل المنتجات النافدة أو غير المتوفرة.
- اعتبار قوائم `normalProducts` و`outStock` و`unavailable` أجزاء متساوية الأهمية من السلة.
- دعم السوق السعودي والعملة السعودية مبدئيًا.
- عرض النتائج في واجهة رسومية لاحقًا، مع تأجيل قرار صيغ التصدير النهائية.

### الحقول المطلوبة

- `goods_id`
- `goods_sn`
- `sku_code`
- `goods_name`
- `goods_img`
- `goods_attr` للمقاس واللون والخصائص
- `stock`
- `sold_out`
- `availability`
- `display_price`
- `amountWithSymbol`

## 2. البيئة والأدوات

- نظام التشغيل: Windows.
- محرر الأكواد: Visual Studio Code.
- Python المستخدم في المشروع: `3.14.6`.
- البيئة الافتراضية: `.venv`.
- المتصفح الآلي: Chromium عبر Playwright.
- الحزم الأساسية: `playwright`, `httpx`, `pydantic`, `pytest`, `ruff`.
- إضافات VS Code: Python، Pylance، Python Debugger، Ruff.

### أوامر الإعداد الأساسية

```powershell
py -3.14 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install playwright httpx pydantic pytest ruff
python -m playwright install chromium
```

إذا منعت سياسة PowerShell تفعيل البيئة في جلسة حالية:

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned
.\.venv\Scripts\Activate.ps1
```

## 3. بنية الحل الحالية

1. يستقبل `diagnose_link.py` رابط المشاركة ويشغّل Chromium بمحاكاة هاتف.
2. يتتبع الرابط من `onelink` إلى رابط `appjump` ثم صفحة السلة الفعلية.
3. يسجل طلبات الشبكة والاستجابات والتشخيصات في `diagnostics/`.
4. يراقب `extract_cart.py` استجابة واجهة السلة البرمجية.
5. يجمع المنتجات من مجموعات التوفر الثلاث دون حذف أي مجموعة.
6. يحوّل البيانات إلى النماذج الموحدة الموجودة في `models.py`.
7. يحفظ الناتج بصيغة JSON مؤقتًا داخل `outputs/` إلى أن تُحدد احتياجات التصدير النهائية.

## 4. الاكتشافات التقنية

### مسار الرابط

فتح رابط `onelink` بطلب HTTP عادي لا يكفي دائمًا للوصول إلى صفحة السلة. عند تشغيله في Chromium بمحاكاة هاتف، أصبح المسار كالتالي:

```text
onelink.shein.com
  -> api-shein.shein.com/h5/sharejump/appjump
  -> m.shein.com/ar/cart/share/landing
```

صفحة `appjump` تحتوي معلومات وسيطة مثل نوع المشاركة `cart` ومعرّف المجموعة `group_id`.

### واجهة بيانات السلة

تم تحديد الطلب المسؤول عن بيانات المنتجات:

```text
POST https://m.shein.com/ar/bff-api/order/cart/share/landing?_ver=1.1.8&_lang=ar
```

تحتوي الاستجابة على:

- `allProductSize`: العدد الكلي.
- `normalProducts`: المنتجات المتاحة.
- `outStock`: المنتجات النافدة.
- `unavailable`: المنتجات غير المتوفرة.

### قاعدة تحديد الحالة

مصدر المجموعة له الأولوية على قيمة المخزون. لا يجوز اعتبار المنتج متاحًا لمجرد أن `stock` أكبر من صفر؛ فقد يظهر المنتج داخل `unavailable` مع `stock=999` و` soldOutStatus=true`. لذلك:

- عضو `normalProducts` يصنّف `available`.
- عضو `outStock` يصنّف `out_of_stock` ويضبط `sold_out=true`.
- عضو `unavailable` يصنّف `unavailable` مهما كانت قيمة `stock`.

### قاعدة تمييز المتغيرات

لا يجوز حذف العناصر المتكررة اعتمادًا على `goods_id` أو `sku_code` وحدهما. قد يتكرر المنتج نفسه داخل السلة بمتغيرات مختلفة. وفق التسمية المعتمدة في هذا المشروع، يكون `sku_code` هو الرمز الطويل الذي يبدأ بالحرف `s`، بينما تكون بقية صيغته غير ثابتة. أما `goods_sn` فهو رمز المتغير الذي يبدأ عادةً بالحرف `I`. عند الحاجة إلى مفتاح تمييز، يُستخدم `goods_sn` أو تركيب `goods_id + goods_sn`، مع الاحتفاظ بـ`goods_attr` والمخزون المستقل لكل متغير.

#### قواعد SKU المعتمدة

- التحقق الشكلي الوحيد المطلوب لـ`sku_code` هو أن يبدأ بالحرف `s`.
- لا نفترض صيغة ثابتة لما بعد الحرف الأول؛ قد تكون أحرفًا أو أرقامًا أو اسم فئة مثل `sbbeauty`.
- يجوز تكرار `sku_code` داخل السلة نفسها.
- قد يمثل كل تكرار مقاسًا أو لونًا أو متغيرًا مختلفًا، ويظهر الفرق في `goods_sn` و`goods_attr` والمخزون.
- لا نحذف أي عنصر بسبب تكرار SKU، ولا نستخدم `sku_code` وحده كمفتاح فريد.
- المفتاح الأنسب للمتغير هو `goods_sn`، ويمكن دعمه بـ`goods_id + goods_sn` عند الحاجة.

### تصحيح ربط `sku_code` و`goods_sn`

حقول استجابة SHEIN لا تتوافق مع التسمية المطلوبة للمشروع، ولذلك نعكس الاسمين أثناء التطبيع فقط:

```text
SHEIN goods_sn  -> output sku_code
SHEIN sku_code  -> output goods_sn
```

مثال النتيجة الصحيحة:

```json
{
  "sku_code": "sw260424154768930179807",
  "goods_sn": "I6mocmgbosy2it"
}
```

لا نغير استجابة SHEIN الأصلية؛ التعديل يحدث فقط عند إنشاء نموذج الناتج الموحد.

## 5. خريطة الملفات

- `diagnose_link.py`: تحليل الروابط وتسجيل التحويلات والشبكة والصفحة.
- `extract_cart.py`: التقاط استجابة السلة واستخراج كل المنتجات.
- `models.py`: نماذج البيانات الموحدة وحالات التوفر.
- `tests/test_extract_cart.py`: اختبار دمج مجموعات المنتجات الثلاث.
- `diagnostics/`: تقارير ولقطات التشخيص، غير مضافة إلى Git.
- `outputs/`: نتائج الاستخراج المؤقتة، غير مضافة إلى Git.
- `README.md`: تعليمات التشغيل المختصرة.
- `PROJECT_LOG.md`: المرجع الكامل للمشروع.

## 6. التجارب والنتائج

### الرابط الأول

```text
https://onelink.shein.com/43/5vz3b3lgwdi4
```

- تم الوصول إلى صفحة السلة الفعلية بواسطة محاكاة الهاتف.
- معرّف المجموعة: `804701030`.
- تم العثور على 48 منتجًا.
- كانت المنتجات كلها ضمن `normalProducts` في وقت التجربة.
- أثبتت التجربة صحة مسار الرابط والقدرة على التقاط استجابة API.

### الرابط الثاني: اختبار المنتجات غير المتوفرة

```text
https://onelink.shein.com/42/5vnv1lnmis19
```

- معرّف المجموعة: `801526900`.
- العدد الكلي: 17.
- المتاحة `normalProducts`: 15.
- النافدة `outStock`: 0.
- غير المتوفرة `unavailable`: 2.
- تم حفظ جميع المنتجات السبعة عشر، ولم تُحذف المنتجات غير المتوفرة.
- ملف النتيجة وقت التجربة: `outputs/cart-801526900-20260717-055052.json`.

#### عينتا المنتجات غير المتوفرة

1. المنتج `goods_id=123667021`، المخزون `0`، والحالة `unavailable` و`sold_out=true`.
2. المنتج `goods_id=479973962`، المخزون `999`، ومع ذلك الحالة `unavailable` و`sold_out=true`.

تؤكد العينة الثانية ضرورة الاعتماد على مجموعة API وحقل الحالة، لا على المخزون وحده.

### الرابط الثالث: منتجات مختلفة وحالات غير متوفرة

```text
https://onelink.shein.com/42/5vrmblpvcjdx
```

- معرّف المجموعة: `802609185`.
- العدد المعلن والاستخراج الفعلي: 51 من 51.
- المتاحة `normalProducts`: 48.
- النافدة `outStock`: 0.
- غير المتوفرة `unavailable`: 3.
- العناصر ذات `sold_out=true`: 3، وهي العناصر غير المتوفرة نفسها.
- العناصر ذات `stock=0`: 3.
- لم ينقص أي من الحقول الأساسية: `goods_id`, `goods_sn`, `sku_code`, `goods_name`, `goods_img`, `goods_attr`, `amountWithSymbol`.
- التقطت الأسعار للعناصر الـ51 كلها من `priceData.unitPrice.price`.
- تراوح السعر بين `SR3.00` و`SR106.00`، مع 33 قيمة سعر مختلفة.
- جميع العناصر كانت تحمل `is_on_sale=1` وقت التجربة.
- ملف النتيجة: `outputs/cart-802609185-20260717-060731.json`.

#### المنتجات الثلاثة غير المتوفرة

1. `goods_id=515674972`، `sku_code=st260626170927065053574`، `goods_sn=I5mquqv8wi28gy`، المخزون 0، السعر `SR81.00`، والصفة `الأسود والأحمر / EUR41`.
2. `goods_id=439459215`، `sku_code=sb25102117488575952`، `goods_sn=I3mnjy9w1h54h2`، المخزون 0، السعر `SR26.00`.
3. `goods_id=503681478`، `sku_code=sl260614180879700736038`، `goods_sn=I2mqdmgxqxxhun`، المخزون 0، السعر `SR10.66`، والصفة `متعدد الألوان / رمادي`.

#### التكرارات الصحيحة للمتغيرات

- تكرر `goods_id=361175826` مرتين لهاتفي `Galaxy S21 Ultra` و`Galaxy S23 Ultra`، مع `goods_sn` ومخزون مختلفين.
- تكرر `goods_id=28387385` مرتين لطرازي iPad مختلفين، مع `goods_sn` ومخزون مختلفين.
- لذلك يجب إبقاء السطرين وعدم إجراء إزالة تكرار بواسطة `goods_id` أو `sku_code` فقط.

### الرابط الرابع: SKU مكرر وحالات مخزون متباينة

```text
https://onelink.shein.com/42/5vo11fncmips
```

- معرّف المجموعة: `801551150`.
- العدد المعلن والاستخراج الفعلي: 79 من 79.
- المتاحة `normalProducts`: 73.
- النافدة `outStock`: 0.
- غير المتوفرة `unavailable`: 6.
- العناصر ذات `sold_out=true`: 6، وهي العناصر غير المتوفرة نفسها.
- العناصر ذات `stock=0`: 3 فقط؛ أما العناصر غير المتوفرة الثلاثة الأخرى فلديها `stock=999`.
- جميع قيم `sku_code` البالغ عددها 79 بدأت بالحرف `s`.
- لم ينقص أي حقل أساسي من العناصر: `goods_id`, `goods_sn`, `sku_code`, `goods_name`, `goods_img`, `goods_attr`، و`amountWithSymbol`.
- التقطت أسعار العناصر الـ79 كلها من `priceData.unitPrice.price`.
- ملف النتيجة: `outputs/cart-801551150-20260717-063333.json`.

#### المنتجات غير المتوفرة

- ثلاثة منتجات غير متوفرة لها `stock=0`.
- ثلاثة منتجات غير متوفرة لها `stock=999`، وهي `goods_id=508878051`, `goods_id=96707395`، و`goods_id=49514325`.
- تؤكد هذه النتيجة للمرة الثانية أن `stock` ليس مصدرًا كافيًا لتحديد التوفر، وأن مجموعة `unavailable` لها الأولوية.

#### SKU مكرر بخمسة متغيرات

- تكرر `sku_code=sj260329134990204763950` خمس مرات للمنتج `goods_id=448705755`.
- بقيت العناصر الخمسة كلها في الناتج.
- لكل عنصر `goods_sn` مختلف، وصفة مختلفة تنتهي بأحد الخيارات `C`, `J`, `B`, `E`، أو `D`.
- المخزون مستقل لكل متغير: بعض العناصر `999` وبعضها `9` أو `7`.
- السعر موحد `SR10.00`، لكن تساوي السعر وSKU لا يعني أن العناصر مكررة ويجوز حذفها.

### الاختبارات الآلية

الأمر:

```powershell
python -m pytest -q
```

أحدث نتيجة في 2026-07-17:

```text
4 passed in 2.11s
```

تغطي الاختبارات الحالية دمج `normalProducts` و`outStock` و`unavailable`، وإبقاء المنتج النافد، وعكس `sku_code` و`goods_sn`، وحفظ المتغيرات المتكررة، وبديل السعر، ورفض الاستجابة غير الصالحة.

## 7. مشكلات واجهتنا وحلولها

- كان تثبيت Python 3.13 القديم تالفًا ويعطي `No module named 'encodings'`; تم استخدام Python 3.14.6 وإنشاء البيئة منه بنجاح.
- فشل تفعيل `.venv` أول مرة لأن إنشائها لم يكتمل؛ بعد إنشائها بـ Python 3.14 تم التفعيل بنجاح.
- تعذر تحديث VS Code بسبب بقاء عملية منه مفتوحة ومنع حذف مجلد التحديث؛ الحل إغلاق جميع عمليات VS Code ثم إعادة المحاولة.
- ظهر تحذير `Cache entry deserialization failed`; هو تلف غير حرج في ذاكرة pip المؤقتة وتم تجاهل المدخل.
- ظهر `UnicodeDecodeError` أثناء محاولة قراءة استجابة ثنائية كنص في التشخيص؛ تم التعامل معه دون منع اكتمال التقرير.
- قد تظهر العربية مشوهة في PowerShell القديم رغم أن ملفات JSON محفوظة بصيغة UTF-8 صحيحة.

## 8. الحالة الحالية

- إعداد بيئة التطوير: مكتمل.
- تحليل روابط SHEIN الحديثة: مكتمل للحالات المجربة.
- اكتشاف واجهة بيانات السلة: مكتمل.
- استخراج جميع مجموعات التوفر: يعمل ومثبت بتجربة فعلية.
- توحيد الحقول الأساسية: مكتمل للحالات المجربة.
- حفظ المتغيرات المتعددة للمنتج نفسه: مؤكد بالتجربة.
- اختبارات الحالات والحقول والأخطاء: موجودة، وآخر تحقق مسجل نجح فيه `18 passed`.
- الواجهة الرسومية العربية: منفذة باستخدام `PySide6`، وتشمل إدخال الفاتورة والسجل وعرض النتائج.
- تحليل نص الفاتورة: منفذ في `invoice_parser.py` ويستخرج اسم العميل ورقم الطلبية ورابط SHEIN.
- الحفظ الحالي: JSON فقط داخل `outputs/`.
- توليد PDF: غير منفذ بعد.
- واجهة HTTP أو Webhook لاستدعاء المحلل من n8n: غير منفذة بعد.
- التكامل مع Telegram وn8n: جرى تحليل بنيته المقترحة ولم يبدأ التنفيذ.
- هدف النشر المعتمد مبدئيًا: خدمة Docker دائمة على VPS، وليست عملية محلية على جهاز المستخدم.
- بيئة النشر المعتمدة للمرحلة الأولى: نفس Hostinger VPS بخطة `KVM 2` الذي يشغل n8n، مع فصل المحلل في حاوية مستقلة.

## 9. الخطوات التالية

1. فصل مسار آلي موحد يستقبل `invoice_text` ويستدعي `parse_invoice_text` ثم `extract_cart` دون الاعتماد على واجهة `PySide6`.
2. إنشاء مولد PDF عربي من نموذج `CartExtraction` وإضافة اختبارات له.
3. إضافة خدمة HTTP صغيرة، ويفضل `FastAPI`، تعيد نجاحًا واضحًا مع ملف PDF أو خطأ منظمًا قابلًا للتفرع داخل n8n.
4. تعريف حالة «لا توجد نتيجة» صراحة عند كون `products` فارغة، بدل اعتبار الاستخراج الفارغ نجاحًا.
5. تجهيز Workflow في n8n: `Telegram Trigger` ثم التحقق من المجموعة والنص ثم `HTTP Request` ثم فرع نجاح لإرسال PDF وفرع فشل لإرسال رسالة.
6. إضافة مصادقة بمفتاح API، ومنع الطلبات المتزامنة غير المحدودة لأن كل تحليل يفتح Chromium عبر Playwright.
7. تجهيز `Dockerfile` مبني على Debian/Ubuntu مع تثبيت Chromium واعتماديات Playwright وتثبيت إصدارات الحزم.
8. إضافة خدمة `shein-api` إلى شبكة Docker نفسها الخاصة بـn8n، مع `healthcheck` و`restart: unless-stopped` وVolume للنتائج المؤقتة.
9. قياس استهلاك الذاكرة والمعالج بتحليل فعلي على الـVPS قبل تحديد هل تكفي الخطة الحالية أم يلزم VPS مستقل.

## 10. سجل زمني

### 2026-07-17 — إعداد المشروع

- تثبيت Python 3.14.6 والتحقق من `pip`.
- إنشاء `.venv` وتفعيلها.
- تثبيت المكتبات وChromium وإضافات VS Code.

### 2026-07-17 — تشخيص الرابط

- بناء `diagnose_link.py`.
- اكتشاف أن محاكاة الهاتف ضرورية للوصول إلى صفحة مشاركة السلة.
- استخراج رابط `appjump` وصفحة السلة ومعرّف المجموعة.

### 2026-07-17 — بناء المستخرج

- اكتشاف طلب `bff-api/order/cart/share/landing`.
- إضافة `models.py` و`extract_cart.py`.
- دمج `normalProducts` و`outStock` و`unavailable` في نتيجة واحدة.
- إضافة اختبار آلي أولي ونجاحه.

### 2026-07-17 — تجربة حالات عدم التوفر

- تشغيل الرابط `https://onelink.shein.com/42/5vnv1lnmis19`.
- استخراج 17 منتجًا: 15 متاحًا و2 غير متوفرين.
- التحقق من عدم إسقاط المنتجات غير المتوفرة.
- تثبيت قاعدة أن حالة المجموعة تتقدم على قيمة المخزون.

### 2026-07-17 — اعتماد التوثيق المستمر

- إنشاء هذا السجل ليكون مرجع المشروع الرسمي.
- إضافة `AGENTS.md` لإلزام تحديث السجل مع كل عملية أو قرار أو اختبار لاحق.

### 2026-07-17 — تجهيز تجربة الرابط الثالث

- الرابط المطلوب: `https://onelink.shein.com/42/5vrmblpvcjdx`.
- الهدف: اختبار منتجات مختلفة ووجود منتجات نافدة أو غير متوفرة.
- تعذّر تشغيل Python من داخل بيئة Codex بسبب منع Windows إنشاء عملية `.venv\\Scripts\\python.exe` برسالة `Access is denied`؛ هذا قيد في بيئة التنفيذ وليس خطأ في المستخرج.
- وُسّع الاختبار الآلي ليثبت أن منتج `unavailable` يظل غير متوفر حتى لو كانت قيمة `stock=999`.
- أضيف التحقق من `sold_out` و`goods_img` و`goods_attr` ومصدر السعر و`amountWithSymbol` في الحالة نفسها.
- ما زال مطلوبًا تشغيل الرابط محليًا من طرفية VS Code، ثم تحليل ملف JSON الناتج وتوثيق الأعداد والحقول الفعلية.

### 2026-07-17 — نجاح تجربة الرابط الثالث

- نُفذ المستخرج محليًا بنجاح على الرابط الثالث.
- استُخرجت 51 من 51 عنصرًا: 48 متاحة و3 غير متوفرة و0 ضمن `outStock`.
- نجح الاختبار الآلي بنتيجة `1 passed in 0.43s`.
- تم تحليل اكتمال الحقول والأسعار وحالات المخزون لكل العناصر.
- اكتُشف تكرار صحيح لاثنين من `goods_id` بسبب اختلاف المتغيرات و`goods_sn`؛ تم اعتماد قاعدة عدم إزالة التكرار بواسطة `goods_id` أو `sku_code` وحده.

### 2026-07-17 — تصحيح مسميات SKU وSN

- أوضح المستخدم أن القيمة الطويلة مثل `sw260424154768930179807` هي SKU المطلوبة.
- عُكس ربط الحقلين في طبقة التطبيع: قيمة SHEIN `goods_sn` تُخرج باسم `sku_code`، وقيمة SHEIN `sku_code` تُخرج باسم `goods_sn`.
- أضيفت تأكيدات اختبارية صريحة للحقلين في المنتجات المتاحة وغير المتوفرة.
- صُححت قاعدة تمييز المتغيرات والسجلات السابقة في هذا المرجع وفق المسميات الجديدة.
- ملفات JSON القديمة لا تتغير تلقائيًا؛ أي تشغيل جديد للمستخرج سينتج التسميات المصححة.

### 2026-07-17 — توسيع اختبارات الحالات

- أضيف اختبار يثبت الاحتفاظ بمتغيرين لهما `goods_id` و`sku_code` نفسيهما مع اختلاف `goods_sn` و`goods_attr`.
- أضيف اختبار لاستخدام `salePrice` عندما لا تتوفر بنية `priceData.unitPrice.price`.
- أضيف اختبار يرفض استجابة SHEIN غير الصالحة التي لا تحتوي كائن `info` برسالة واضحة.
- أصبحت مجموعة الاختبارات تغطي المجموعات الثلاث، انعكاس `sku_code` و`goods_sn`، المتغيرات المتكررة، بديل السعر، والاستجابة غير الصالحة.

### 2026-07-17 — التحقق الفعلي من تصحيح SKU وSN

- نجحت الاختبارات الأربعة بنتيجة `4 passed in 0.48s`.
- أُعيد تشغيل الرابط `https://onelink.shein.com/42/5vrmblpvcjdx`، وحُفظ الناتج المصحح في `outputs/cart-802609185-20260717-061910.json`.
- بقيت نتيجة السلة ثابتة: 51 من 51 عنصرًا، منها 48 متاحة و3 غير متوفرة و0 في `outStock`.
- تأكد وجود `goods_sn` و`sku_code` في العناصر الـ51 كلها دون قيم مفقودة.
- تأكد أن القيم ذات بادئة `I` أصبحت في `goods_sn` لكل العناصر، وأن الرموز الطويلة أصبحت في `sku_code`.
- المثال المؤكد: `sku_code=sw260424154768930179807` و`goods_sn=I6mocmgbosy2it`.
- ظهر SKU صحيح بصيغة `sbbeauty31210326552` لمنتج SHEGLAM؛ لذلك لا نفرض تعبيرًا نمطيًا ضيقًا مثل حرفين متبوعين بأرقام على قيم SKU.

### 2026-07-17 — الرابط المخطط للتجربة التالية

- الرابط: `https://onelink.shein.com/42/5vo11fncmips`.
- الهدف: توسيع تغطية الاحتمالات الفعلية، خصوصًا مجموعات التوفر وبنى الأسعار والمتغيرات المختلفة.
- بعد تشغيله ستُقارن قيمة `allProductSize` بعدد العناصر المستخرجة، وتُفحص المجموعات الثلاث واكتمال الحقول وتكرارات المتغيرات ومصادر الأسعار.

### 2026-07-17 — اعتماد قاعدة تكرار SKU

- أكد المستخدم أن `sku_code` يبدأ دائمًا بالحرف `s`، وأن بقية الصيغة تختلف باختلاف المنتج.
- أكد المستخدم أن SKU نفسه قد يتكرر في السلة مع اختلاف المقاس.
- حُدث الاختبار ليحفظ عنصرين لهما `goods_id` و`sku_code` نفسيهما، لكن بمقاسي `S` و`M` وقيمتي `goods_sn` مختلفتين.
- أضيف تحقق اختباري من الحرف الأول `s` فقط، دون فرض نمط على بقية SKU.
- ثُبت قرار عدم إزالة التكرار بواسطة `sku_code`، والاعتماد على `goods_sn` لتمييز المتغير.

### 2026-07-17 — بروتوكول تجربة الرابط الرابع

- الرابط: `https://onelink.shein.com/42/5vo11fncmips`.
- يُشغّل الاختبار الآلي أولًا للتأكد من سلامة قواعد التطبيع الحالية.
- يُشغّل المستخرج بعدها لإنشاء ملف JSON جديد.
- تُقارن قيمة `allProductSize` بعدد عناصر `products` للتأكد من عدم فقد أي منتج.
- تُراجع أعداد `normalProducts` و`outStock` و`unavailable` كلٌ على حدة.
- يُتحقق من أن كل `sku_code` يبدأ بالحرف `s` دون فرض صيغة على بقية القيمة.
- تُراجع قيم SKU المكررة، ويُتأكد من بقاء جميع المقاسات والألوان مع `goods_sn` و`goods_attr` المستقلين.
- تُراجع علاقة `stock` و`sold_out` و`availability` مع إعطاء مجموعة المصدر الأولوية.
- يُتحقق من اكتمال `goods_id`, `goods_sn`, `sku_code`, `goods_name`, `goods_img`, `goods_attr`, `display_price`, و`amountWithSymbol`.
- تُحصى مصادر الأسعار وتُراجع أي بنية سعر جديدة لم تظهر في الروابط السابقة.
- إذا ظهرت عناصر فعلية في `outStock` فستُعتمد هذه التجربة لإكمال تغطية حالة النفاد؛ وإلا نحتاج رابطًا إضافيًا يحتوي هذه المجموعة.

### 2026-07-17 — نجاح تجربة الرابط الرابع

- نجحت الاختبارات الآلية بنتيجة `4 passed in 2.11s`.
- استُخرجت 79 من 79 عنصرًا: 73 متاحة و6 غير متوفرة و0 في `outStock`.
- تأكد اكتمال الحقول الأساسية والأسعار لكل العناصر.
- بدأت جميع قيم SKU بالحرف `s` كما هو مطلوب.
- تأكد عمليًا الاحتفاظ بخمسة عناصر تحمل SKU نفسه مع اختلاف `goods_sn` و`goods_attr` والمخزون.
- تأكد وجود منتجات `unavailable` بمخزون 999، مما يثبت أولوية تصنيف المجموعة على قيمة المخزون.
- لم تظهر حالة فعلية ضمن `outStock` بعد، لذلك تبقى هذه الحالة هي الجزء الوحيد غير المغطى برابط حقيقي، مع أنها مغطاة بالاختبار الآلي.

### 2026-07-17 — المرحلة التالية: مدقق النتائج الآلي

- إنشاء أداة مستقلة باسم مبدئي `validate_output.py` تستقبل مسار ملف JSON ناتج من المستخرج.
- مقارنة `allProductSize` بعدد عناصر `products` والإبلاغ عن أي فرق.
- عرض أعداد `available`, `out_of_stock`, و`unavailable`.
- التحقق من اكتمال الحقول الأساسية لكل عنصر.
- التحقق من أن `sku_code` يبدأ بالحرف `s` فقط، دون فرض نمط على بقية القيمة.
- اكتشاف مجموعات SKU المكررة وعرض متغيراتها دون اعتبارها أخطاء.
- فحص العلاقة بين `availability`, `sold_out`, و`stock`، مع احترام أولوية مجموعة المصدر.
- تلخيص مصادر الأسعار والعناصر التي لا تحتوي سعرًا.
- إخراج تقرير واضح في الطرفية، ثم إضافة خيار حفظ التقرير بصيغة JSON أو Markdown لاحقًا.
- بعد ثبات المدقق، نستخدمه مع كل رابط تجريبي، ثم ننتقل إلى بناء واجهة Windows العربية.

### 2026-07-17 — تنفيذ مدقق النتائج الآلي

- أضيف `validate_output.py` لقراءة ملف نتيجة JSON والتحقق منه دون إعادة فتح رابط SHEIN.
- يقارن المدقق العدد المعلن والمستخرج وأعداد مجموعات المصدر الفعلية.
- يفحص اكتمال الحقول الأساسية وبداية SKU بالحرف `s`.
- يعرض SKU المكرر كمجموعة متغيرات مسموحة مع `goods_sn`, `goods_attr`, المخزون، وحالة التوفر.
- يفحص توافق `source_group` مع `availability` ويصدر تحذيرات للتعارضات غير القاطعة بين الحالة والمخزون.
- يلخص مصادر الأسعار ويستخدم رمز خروج `0` للملف السليم، و`1` لأخطاء البيانات، و`2` لتعذر قراءة الملف.
- أضيفت ثلاثة اختبارات للمدقق: SKU المكرر، العدد والحقول وSKU غير الصحيح، وحالة `unavailable` ذات مخزون 999.
- أضيف أمر تشغيل المدقق إلى `README.md`.
- اجتاز `validate_output.py` و`tests/test_validate_output.py` فحص Ruff وفحص التنسيق بالكامل.
- تعذر تشغيل Python وpytest من داخل بيئة Codex بسبب رفض Windows إنشاء عملية `.venv\\Scripts\\python.exe` برسالة `Access is denied`؛ لذلك يلزم تنفيذ الاختبارات والمدقق من طرفية VS Code المحلية قبل اعتماد النتيجة الوظيفية النهائية.

### 2026-07-17 — اعتماد المدقق على نتيجة حقيقية

- شُغلت مجموعة الاختبارات محليًا ونجحت بنتيجة `7 passed in 0.49s`.
- شُغّل المدقق على `outputs/cart-801551150-20260717-063333.json` بنجاح.
- طابق العدد المعلن والمستخرج: 79 من 79.
- أكد التقرير وجود 73 عنصرًا متاحًا و6 غير متوفرة.
- أكد عدم وجود حقول أساسية ناقصة وعدم وجود SKU لا يبدأ بالحرف `s`.
- اكتشف مجموعة SKU واحدة مكررة هي `sj260329134990204763950`، وعرض المتغيرات الخمسة منفصلة مع `goods_sn` والصفة والمخزون والحالة.
- انتهى المدقق برسالة نجاح، مما يعني أن الملف اجتاز جميع عمليات التحقق الإلزامية.
- ظهرت عناوين العربية وقيم الصفات العربية مشوهة في طرفية VS Code بسبب ترميز وحدة تحكم Windows، مع بقاء البيانات نفسها صحيحة.
- أضيف ضبط UTF-8 داخل `validate_output.py` لوحدة تحكم Windows و`stdout` و`stderr` لتحسين عرض العربية في التشغيل التالي.

### 2026-07-17 — سياسة عرض العربية

- أوضح المستخدم أن تشوه العربية في الطرفية غير مهم في هذه المرحلة.
- الشرط الإلزامي هو سلامة العربية وترميز UTF-8 داخل ملفات النتائج والتقارير، وسلامة عرضها داخل الواجهة الرسومية.
- لن نؤخر تطوير المشروع لمعالجة شكل الطرفية ما دامت البيانات المحفوظة صحيحة.

### 2026-07-17 — المرحلة التالية: واجهة Windows العربية

- اعتماد PySide6 لبناء واجهة Windows أصلية قابلة للتطوير.
- إنشاء نافذة عربية من اليمين إلى اليسار تحتوي حقل رابط SHEIN وزر بدء الاستخراج.
- تشغيل Playwright في عامل خلفي حتى لا تتجمد الواجهة أثناء فتح الرابط.
- عرض حالة العملية ورسائل الخطأ وعدد المنتجات أثناء التنفيذ.
- عرض جميع المنتجات المتاحة والنافدة وغير المتوفرة دون استبعاد.
- عرض الصورة والاسم و`sku_code` و`goods_sn` والصفة والمخزون والسعر وحالة التوفر لكل عنصر.
- تمييز المنتجات غير المتوفرة والنافدة بصريًا دون حذفها.
- إضافة أزرار نسخ `sku_code` و`goods_sn`.
- إبقاء التصدير مؤجلًا حتى يحدد المستخدم الصيغ والحقول المطلوبة.
- تبدأ النسخة الأولى بوظائف أساسية مستقرة، ثم يُحسن التصميم والتحميل التدريجي للصور في مرحلة لاحقة.

### 2026-07-17 — تنفيذ النسخة الأولى من الواجهة

- ثُبت PySide6 6.11.1 ومكوناته بنجاح، ونجح استيراد `QApplication`.
- أضيف `app.py` كواجهة Windows عربية من اليمين إلى اليسار بتصميم داكن.
- أضيف حقل رابط وزرا اللصق والاستخراج مع التحقق من نطاق SHEIN.
- نُقل تشغيل Playwright إلى `QThread` حتى لا تتجمد الواجهة أثناء الاستخراج.
- أضيف مؤشر تحميل وحقل حالة ورسائل للرابط المنتهي وأخطاء الاستخراج.
- أضيفت بطاقات إجمالي المنتجات والمتاحة والنافدة وغير المتوفرة.
- أضيف جدول يعرض الحالة والاسم وSKU وSN والخصائص والمخزون والسعر.
- أضيف زر فتح صورة المنتج وأزرار نسخ SKU وSN لكل متغير.
- تحتفظ الواجهة بجميع عناصر `normalProducts`, `outStock`, و`unavailable` دون إزالة SKU المكرر.
- يُحفظ ملف JSON تلقائيًا بواسطة طبقة الاستخراج الحالية، بينما يبقى تصميم التصدير النهائي مؤجلًا.
- أضيف `requirements.txt` لتجميع تبعيات المشروع، وأضيف أمر تشغيل الواجهة إلى `README.md`.

### 2026-07-17 — فحص توافق روابط `appjump` القديمة

- الرابط المطلوب اختباره: `https://api-shein.shein.com/h5/sharejump/appjump?link=RPJO83BaCJu_b&localcountry=SA&shc=2_RPJO83BaCJu&url_from=GM75021138555`.
- يقبل `validate_shein_url` الرابط لأنه يستخدم `https` وينتمي إلى نطاق فرعي من `shein.com`.
- يفتح `extract_cart.py` الرابط المدخل مباشرة داخل Chromium بمحاكاة iPhone، ثم يراقب واجهة `bff-api/order/cart/share/landing` نفسها المستخدمة مع روابط `onelink`.
- لذلك لا تحتاج صيغة `appjump` القديمة إلى تحويل يدوي قبل تمريرها للمستخرج.
- الدعم الفعلي مشروط بأن رابط المشاركة القديم ما زال صالحًا ويحوّل إلى صفحة سلة تحتوي بياناتها؛ روابط المشاركة قد تنتهي أو تتوقف عن إرجاع السلة.
- إذا فشل هذا الرابط المحدد بسبب انتهاء صلاحيته، فهذا لا يثبت عدم دعم صيغة الروابط القديمة، ويجب إعادة الاختبار برابط `appjump` قديم ما زال نشطًا.
- معيار النجاح: الوصول إلى صفحة السلة، التقاط استجابة API، تطابق العدد المعلن والمستخرج، ثم اجتياز الملف الناتج بواسطة `validate_output.py`.

### 2026-07-17 — نتائج روابط `appjump` القديمة والمنتهية

- نجح رابط `appjump` القديم ذو الرمز `RPJO83BaCJu_b` فعليًا، ووصل إلى السلة `group_id=792897405`.
- استخرج الرابط القديم الصالح 55 منتجًا: 48 متاحة و7 غير متوفرة و0 في `outStock`.
- حُفظ الناتج في `outputs/cart-792897405-20260717-070253.json`، مما يثبت دعم صيغة الروابط القديمة عندما تكون المشاركة صالحة.
- فشل الرابط المنتهي ذو الرمز `RlaNXtY5d8b_b` بعد انتهاء المهلة، ولم يُنشئ ملف منتجات.
- أكد تقرير `diagnostics/link-report-20260717-070627.json` أن رابط HTTP ورابط المتصفح النهائي بقيا صفحة `appjump` نفسها، وأن العنوان فارغ ولم تظهر أي استجابة API للسلة.
- اعتُمد بقاء المتصفح على المسار `/h5/sharejump/appjump` دون التقاط API كعلامة على رابط مشاركة منتهي أو غير متاح.
- أضيف `ExpiredShareLinkError` لتمييز الرابط المنتهي عن خطأ الشبكة أو انتهاء المهلة العام.
- عُدلت واجهة سطر الأوامر لعرض رسالة قصيرة للمستخدم وإرجاع رمز خروج `1` بدل طباعة Traceback كامل لأخطاء الاستخراج المتوقعة.
- أضيف اختبار للرابط المنتهي واختبار لخطأ الالتقاط العام بعد الوصول إلى صفحة السلة.

### 2026-07-17 — اعتماد معالجة الرابط المنتهي

- أُعيد تشغيل الرابط المنتهي `RlaNXtY5d8b_b` بمهلة 15 ثانية بعد تعديل معالجة الأخطاء.
- انتهى التشغيل برسالة مستخدم مختصرة بدل Traceback.
- عاد البرنامج إلى موجه PowerShell بصورة طبيعية ولم يُنشئ ملفًا جديدًا داخل `outputs`.
- ظهرت العربية مشوهة في الطرفية فقط، وهو مقبول وفق سياسة المشروع ولا يؤثر في ملفات النتائج أو الواجهة المستقبلية.
- أصبحت حالة الرابط القديم المنتهي جاهزة للعرض داخل الواجهة الرسومية كرسالة خطأ مفهومة.

### 2026-07-17 — المواصفات المعتمدة لتطوير واجهة المستخدم

هذه المواصفات تمثل التصور المبدئي المعتمد للواجهة بعد مراجعة المستخدم، مع إضافات تنظيمية لتحسين سهولة الاستخدام والأداء.

#### التخطيط العام

- واجهة Windows داكنة وعربية بالكامل من اليمين إلى اليسار.
- في أعلى الشاشة شريط أدوات واضح يحتوي أزرار بدء تحليل جديد، مسح العرض الحالي، استعراض السجل، ونسخ البيانات لاحقًا.
- أسفل شريط الأدوات توجد خانة رابط SHEIN، وبجوارها زر بدء التحليل وزر لصق اختياري لتسريع الاستخدام.
- أسفل خانة الرابط مباشرة توجد خانة اسم العميل.
- أسفل خانة اسم العميل تبقى خانة الحالة ومؤشر التحميل كما هما في النسخة الحالية.
- تبقى بطاقات أعداد المنتجات الحالية: الإجمالي، المتاحة، النافدة، وغير المتوفرة.
- تظهر نتائج المنتجات أسفل خانات الإدخال والحالة والعدادات بتنسيق مرتب وواضح.

#### خانة اسم العميل وتسمية الملف

- يُكتب اسم العميل قبل بدء تحليل الرابط.
- يُقرأ الاسم مرة واحدة عند بدء التحليل حتى لا يتغير اسم الملف أثناء التنفيذ.
- اسم ملف JSON يكون بالصيغة:

```text
اسم-العميل-YYYYMMDD-HHMMSS.json
```

- مثال: `محمد-20260717-073349.json`.
- إذا تُرك اسم العميل فارغًا، يستخدم الاسم الافتراضي `shein-cart`، مثل `shein-cart-20260717-073349.json`.
- تُزال أو تُستبدل المحارف غير المسموحة في أسماء ملفات Windows تلقائيًا مع إبقاء الاسم العربي قابلًا للقراءة.
- إذا تكرر الاسم والتوقيت بصورة نادرة، يضاف رقم تسلسلي دون الكتابة فوق ملف سابق.
- يسجل داخل JSON أيضًا اسم العميل ووقت التحليل كبيانات وصفية، مع الاحتفاظ بجميع بيانات السلة والمنتجات.

#### ترتيب حقول عرض المنتجات

تعرض الواجهة الحقول التالية بهذا الترتيب فقط:

1. **الحالة:** متاح، نافد، غير متوفر، أو غير معروف، مع لون واضح لكل حالة.
2. **الصورة:** صورة مصغرة للمنتج تظهر داخل البرنامج دون فتح المتصفح.
3. **اسم المنتج:** أول 100 حرف فقط لمنع تكدس العرض.
4. **SKU:** القيمة المعتمدة التي تبدأ بالحرف `s`.
5. **الخصائص أو المقاس:** قيمة `goods_attr` كاملة بقدر المساحة المتاحة.
6. **السعر:** قيمة `amountWithSymbol` مع رمز العملة.

#### تفاصيل العرض المقترحة

- يحتفظ البرنامج بالاسم الكامل للمنتج، ويعرضه كتلميح عند تمرير مؤشر الفأرة فوق الاسم المختصر.
- تعرض الصورة المصغرة داخل صف المنتج بمقاس ثابت للحفاظ على انتظام الصفوف.
- تُحمّل الصور في الخلفية وبصورة تدريجية حتى لا تتجمد الواجهة عند وجود عشرات المنتجات.
- تستخدم صورة بديلة عند فشل تحميل صورة المنتج، دون تعطيل بقية النتائج.
- تُخزن الصور المصغرة مؤقتًا أثناء جلسة البرنامج لتقليل إعادة التحميل عند الرجوع إلى تحليل سابق.
- لا يُحذف أي صف بسبب تكرار SKU؛ تظهر جميع المقاسات والألوان والمتغيرات مستقلة.
- تميّز المنتجات النافدة وغير المتوفرة بصريًا، لكنها تبقى ضمن النتائج وفي موقعها الطبيعي.

#### محتوى ملف JSON

- تقليل الحقول في الواجهة لا يغيّر ملف JSON.
- يحتفظ JSON بجميع البيانات المستخرجة، بما فيها الحقول غير الظاهرة في الجدول مثل `goods_id`, `goods_sn`, `stock`, `sold_out`, `availability`, `source_group`, `display_price`, و`is_on_sale`.
- يضاف اسم العميل ووقت التحليل ومسار أو اسم ملف المصدر ضمن البيانات الوصفية دون حذف أي حقل حالي.

#### زر مسح البيانات وبدء تحليل جديد

- يتوفر زر واضح في الأعلى باسم `مسح / تحليل جديد`.
- يمسح الرابط واسم العميل والحالة والعدادات والجدول من الواجهة.
- لا يحذف ملفات JSON أو التحليلات السابقة من القرص.
- إذا كان التحليل قيد التنفيذ، يُعطل الزر أو يطلب الانتظار حتى تنتهي العملية لتجنب إتلاف جلسة Playwright.
- إذا كانت هناك نتائج لم ينسخها المستخدم، يمكن عرض تأكيد بسيط قبل مسح العرض.

#### زر نسخ البيانات المستخرجة

- يضاف زر في الأعلى باسم `نسخ البيانات`.
- يبقى الزر معطلًا قبل وجود نتائج.
- تنسيق النص المنسوخ سيحدد لاحقًا بناءً على احتياج المستخدم.
- تُفصل وظيفة النسخ عن JSON؛ تغيير تنسيق النسخ مستقبلًا لا يغير بنية ملف النتيجة.

#### سجل التحليلات السابقة

- يضاف مكان واضح باسم `سجل التحليلات` يعمل كذاكرة للبرنامج.
- يعتمد السجل مبدئيًا على ملفات JSON المحفوظة داخل `outputs` بدل إضافة قاعدة بيانات مبكرًا.
- يعرض السجل اسم العميل ووقت التحليل وعدد المنتجات وحالاتها، ويرتب العناصر من الأحدث إلى الأقدم.
- عند اختيار تحليل قديم، يحمّل البرنامج ملف JSON المحلي ويعرض الحقول الستة نفسها في جدول النتائج دون فتح SHEIN أو تشغيل Playwright من جديد.
- لا يؤدي استعراض تحليل سابق إلى تعديل الملف الأصلي.
- يضاف لاحقًا خيار حذف تحليل محدد من السجل مع رسالة تأكيد، وليس ضمن النسخة الأولى.
- إذا زاد عدد الملفات مستقبلًا بصورة كبيرة، يمكن إضافة بحث باسم العميل وتصفية حسب التاريخ والحالة.

#### التنقل المعتمد داخل سجل التحليلات

- يفتح السجل كصفحة مستقلة داخل نافذة البرنامج نفسها، وليس كنافذة Windows منفصلة.
- تستخدم الواجهة صفحات داخلية يمكن الانتقال بينها مع الاحتفاظ بحالة كل صفحة.
- صفحة السجل قابلة للتمرير وتعرض جميع ملفات JSON السابقة من الأحدث إلى الأقدم.
- يستطيع المستخدم فتح أي ملف سابق من القائمة.
- عند فتح ملف سابق، ينتقل البرنامج إلى صفحة تفاصيل تعرض المنتجات بنفس مكون وشكل عرض نتائج التحليل الحالي.
- تتضمن صفحة التفاصيل أزرار نسخ البيانات واستخراجها، مع تأجيل تحديد صيغة النسخ والتصدير إلى قرار لاحق.
- تتضمن صفحة التفاصيل زر `العودة إلى السجل` يعيد المستخدم إلى موضعه السابق في قائمة السجل.
- يتوفر في أعلى صفحة السجل وصفحة التفاصيل زر ثابت `واجهة التحليل` للعودة مباشرة إلى شاشة تحليل رابط جديد.
- لا يؤدي التنقل بين الصفحات إلى مسح الرابط أو النتائج الحالية تلقائيًا.
- زر `مسح / تحليل جديد` وحده هو المسؤول عن تنظيف شاشة التحليل الحالية.

### 2026-07-17 — اقتراحات تحسين الواجهة قبل التنفيذ

الاقتراحات التالية غير معتمدة نهائيًا بعد، وتنتظر اختيار المستخدم:

1. **استخدام `QStackedWidget` للتنقل:** ثلاث صفحات داخل النافذة نفسها: `التحليل`، `السجل`، و`تفاصيل التحليل`. هذا هو الخيار الموصى به لأنه أسرع وأبسط ويحافظ على حالة كل صفحة.
2. **شريط تنقل علوي ثابت:** زران واضحان `التحليل` و`السجل` يظهران في جميع الصفحات، مع تمييز الصفحة الحالية بلون مختلف.
3. **بطاقات للسجل بدل جدول تقليدي:** تعرض كل بطاقة اسم العميل، تاريخ ووقت التحليل، العدد الإجمالي، أعداد الحالات، وصورة مصغرة لأول منتج إن توفرت.
4. **بحث فوري في السجل:** البحث باسم العميل أو اسم الملف دون الحاجة لفتح كل تحليل.
5. **فلاتر خفيفة:** تصفية حسب اليوم أو وجود منتجات غير متوفرة أو نافدة. يمكن تأجيلها إذا أردنا نسخة أولى أبسط.
6. **مكون نتائج واحد مشترك:** تستخدم شاشة التحليل وصفحة تفاصيل السجل المكون نفسه لعرض المنتجات، لمنع اختلاف الشكل أو السلوك بين الجديد والقديم.
7. **حفظ موضع التمرير:** عند فتح ملف ثم العودة إلى السجل، يعود المستخدم إلى البطاقة والموضع السابقين بدل بداية القائمة.
8. **شريط معلومات أعلى التفاصيل:** يعرض اسم العميل، وقت التحليل، رابط المصدر، معرّف المجموعة، واسم ملف JSON قبل جدول المنتجات.
9. **مؤشر سلامة الملف:** إذا كان ملف قديمًا أو ناقصًا، يظهر تنبيه واضح بدل فشل الصفحة، مع عرض ما يمكن قراءته إن أمكن.
10. **تحميل تدريجي للسجل والصور:** تُعرض البطاقات النصية أولًا، ثم الصور، حتى يظل السجل سريعًا عند وجود ملفات كثيرة.
11. **زر فتح مجلد الملف:** يفتح موقع ملف JSON في مستكشف Windows عند الحاجة للمراجعة اليدوية.
12. **عدم إضافة الحذف في النسخة الأولى:** تجنب حذف ملفات مهمة بالخطأ، ثم إضافته لاحقًا بزر مستقل وتأكيد واضح إذا طلب المستخدم.
13. **إبقاء النسخ والتصدير منفصلين:** `نسخ البيانات` يضع نصًا في الحافظة، بينما `استخراج/تصدير` ينشئ ملفًا؛ لكل منهما تنسيق مستقل يحدد لاحقًا.

### 2026-07-17 — التصميم النهائي المعتمد للنافذة والسجل

يلغي هذا القرار اقتراح الصفحات المنفصلة و`QStackedWidget`، ويعتمد نافذة واحدة تجمع التحليل والسجل والعرض في الوقت نفسه.

#### بنية النافذة

- تستخدم النافذة `QSplitter` أفقيًا لتقسيم المساحة إلى منطقتين.
- المنطقة الرئيسية على اليمين تحتوي إدخال الرابط واسم العميل والحالة والعدادات ومعلومات الملف والنتائج.
- يوجد على اليسار **Resizable History Sidebar** يعرض سجل التحليلات السابقة.
- يستطيع المستخدم سحب الحد الفاصل لتكبير أو تصغير عرض السجل.
- يوضع حد أدنى مناسب لعرض السجل حتى تبقى أسماء العملاء والملفات قابلة للقراءة.
- يقترح إضافة زر صغير لطي السجل وإظهاره لتوفير مساحة أكبر للنتائج عند الحاجة.
- تبقى الواجهة كلها في نافذة واحدة، ولا تفتح نافذة أو صفحة مستقلة للسجل أو التفاصيل.

#### ترتيب وعرض السجل

- يقرأ السجل ملفات JSON الموجودة داخل `outputs`.
- يرتب الملفات حسب وقت التحليل من الأحدث إلى الأقدم؛ أحدث تحليل يظهر في أعلى القائمة.
- يحدث السجل تلقائيًا بعد نجاح أي تحليل جديد وحفظ ملفه.
- يعرض كل عنصر اسم العميل أو الاسم الافتراضي، وقت التحليل، اسم الملف، وإجمالي المنتجات مع ملخص الحالات.
- يُميز العنصر المحدد حاليًا بلون واضح.
- عند النقر على أي عنصر، تُحمّل بياناته وتظهر فورًا في منطقة النتائج نفسها المستخدمة للتحليل الجديد.
- لا يُفتح SHEIN ولا Playwright عند استعراض ملف سابق.
- لا نضيف حذف السجلات أو الملفات، بناءً على قرار المستخدم بالحفاظ على جميع التحليلات.

#### البحث في السجل

- توجد خانة بحث أعلى العمود الجانبي.
- تبحث فورًا باسم العميل أو اسم ملف JSON.
- لا تعدل عملية البحث الملفات ولا ترتيبها الأصلي؛ هي تخفي العناصر غير المطابقة فقط.
- عند مسح نص البحث، تظهر القائمة كاملة بالترتيب من الأحدث إلى الأقدم.

#### معلومات التحليل المعروض

- توجد بطاقة معلومات أعلى النتائج وأسفل خانة الحالة والعدادات.
- تعرض اسم العميل، تاريخ ووقت التحليل، اسم ملف JSON، رابط المصدر، ومعرّف المجموعة.
- تظهر البطاقة سواء كانت البيانات من تحليل جديد أو ملف مختار من السجل.
- بالنسبة للملفات القديمة التي لا تحتوي اسم عميل أو وقتًا وصفيًا داخل JSON، يُستخرج الوقت من اسم الملف أو تاريخ تعديله ويعرض العميل باسم افتراضي مثل `غير محدد`.

#### أزرار الإجراءات المعتمدة

- يوجد زر `تحليل جديد` في أعلى المنطقة الرئيسية، وهو الاسم المعتمد بدل `مسح`.
- يعمل الزر سواء كانت الشاشة تعرض تحليلًا جديدًا أو ملفًا من السجل.
- يمسح الرابط واسم العميل والحالة والعدادات ومعلومات الملف والنتائج الحالية فقط.
- لا يحذف زر `تحليل جديد` أي ملف من `outputs` ولا يمس السجل.
- يوجد زر مستقل `نسخ البيانات` لنسخ البيانات إلى الحافظة وفق تنسيق سيحدده المستخدم لاحقًا.
- يوجد زر مستقل `تصدير البيانات` لإنشاء ملف وفق صيغة سيحددها المستخدم لاحقًا.
- يبقى زرا النسخ والتصدير معطلين حتى توجد بيانات معروضة.
- فصل النسخ عن التصدير قرار نهائي، حتى يمكن تغيير تنسيق كل منهما بصورة مستقلة.

#### السلوك والأداء

- يستخدم التحليل الجديد وعرض السجل مكون النتائج نفسه تمامًا.
- عند اختيار سجل أثناء وجود تحليل قيد التنفيذ، يمنع التبديل مؤقتًا أو يطلب الانتظار حتى انتهاء العملية.
- تظهر البيانات النصية المحلية فور اختيار الملف، ثم تُحمّل الصور تدريجيًا.
- يحفظ البرنامج عرض العمود الجانبي الذي اختاره المستخدم بين الجلسات باستخدام `QSettings` إن أمكن.
- تعتمد ملفات JSON كمصدر أساسي للسجل دون قاعدة بيانات في هذه المرحلة.
- عند زيادة الملفات مستقبلًا يمكن إضافة تحميل تدريجي للعناصر، دون تغيير تصميم الواجهة الأساسي.

#### اعتماد طي العمود الجانبي

- اعتمد المستخدم رسميًا إمكانية طي سجل التحليلات الجانبي للحصول على مساحة أكبر لعرض المنتجات.
- يضاف زر واضح في أعلى العمود الجانبي لطي السجل.
- عند طي السجل، تختفي قائمته وتتمدد منطقة النتائج لتستخدم المساحة المتاحة.
- يبقى زر أو مؤشر صغير ظاهرًا لإعادة فتح السجل بسهولة.
- الطي لا يمسح البحث أو العنصر المحدد ولا يغير ملفات السجل.

#### توضيح اقتراح حفظ عرض السجل

- المقصود بعرض السجل هو عرض العمود الجانبي بالبكسل، وليس محتوى السجل.
- مثال: إذا سحب المستخدم الحد الفاصل وجعل عرض السجل 360 بكسل، ثم أغلق البرنامج، يمكن للبرنامج تذكر الرقم 360.
- عند تشغيل البرنامج مرة أخرى، يفتح العمود بنفس العرض الذي اختاره المستخدم بدل الرجوع إلى العرض الافتراضي.
- يمكن أيضًا تذكر حالة العمود إن كان مفتوحًا أو مطويًا عند إغلاق البرنامج.
- تُحفظ هذه التفضيلات محليًا بواسطة `QSettings`، ولا تعدل ملفات JSON ولا بيانات العملاء.
- اعتمد المستخدم هذا الاقتراح نهائيًا.
- يتذكر البرنامج عرض العمود الجانبي وحالته المفتوحة أو المطوية بين مرات التشغيل.

### 2026-07-17 — قواعد الحفظ التلقائي وإعادة تسمية السجل

- يحفظ البرنامج ملف JSON تلقائيًا فور اكتمال التحليل بنجاح.
- لا يحتاج المستخدم إلى الضغط على زر حفظ مستقل.
- يكتب المستخدم اسم العميل في الخانة المخصصة بعد لصق الرابط وقبل الضغط على زر التحليل.
- إذا كانت خانة العميل فارغة عند بدء التحليل، يستخدم البرنامج الاسم الافتراضي المعتمد `shein-cart`.
- يثبت البرنامج اسم العميل ووقت بدء التحليل عند الضغط على الزر، فلا يتأثر اسم الملف بتعديل الخانات أثناء التنفيذ.
- الصيغة المعتمدة لاسم الملف هي `اسم-العميل-YYYYMMDD-HHMMSS.json`.
- يمكن إعادة تسمية اسم الملف من داخل سجل التحليلات.
- عند إعادة التسمية، يسمح بتعديل جزء اسم العميل فقط.
- لا يمكن تعديل أو حذف جزء التاريخ والوقت من واجهة إعادة التسمية.
- مثال: يمكن تحويل `shein-cart-20260717-073349.json` إلى `محمد-20260717-073349.json`، لكن يبقى `20260717-073349` ثابتًا.
- تعالج الواجهة المحارف غير المسموحة في Windows، وتمنع الكتابة فوق ملف موجود.
- إعادة التسمية لا تعدل محتوى JSON ولا وقت التحليل الأصلي؛ هي تغير اسم الملف فقط.
- يحدث ترتيب السجل بعد إعادة التسمية مع بقاء العنصر في موضعه الزمني نفسه.

### 2026-07-17 — الهيكل الكامل المقترح للبرنامج قبل التنفيذ النهائي

#### 1. النافذة الرئيسية

- نافذة Windows واحدة داكنة وعربية من اليمين إلى اليسار.
- تنقسم أفقيًا بواسطة `QSplitter` إلى منطقة نتائج رئيسية على اليمين وسجل تحليلات على اليسار.
- لا توجد نوافذ مستقلة أو صفحات منفصلة لعرض السجل.
- يمكن تغيير عرض السجل بالسحب، أو طيه بالكامل، أو إظهاره مجددًا.
- يحفظ `QSettings` عرض السجل وحالته بين مرات تشغيل البرنامج.

#### 2. شريط الإجراءات العلوي

- زر `تحليل جديد`: ينظف منطقة التحليل والعرض الحالية ويجهز البرنامج لرابط جديد دون حذف أي ملف.
- زر `نسخ البيانات`: مستقل عن التصدير، ويظل معطلًا حتى توجد بيانات؛ يحدد تنسيقه لاحقًا.
- زر `تصدير البيانات`: مستقل عن النسخ، ويظل معطلًا حتى توجد بيانات؛ تحدد صيغته لاحقًا.
- زر أو أيقونة `إظهار/إخفاء السجل`: يطوي العمود الجانبي أو يعيده.
- أثناء التحليل تُعطل الإجراءات التي قد تغير البيانات أو السجل حتى تنتهي العملية.

#### 3. منطقة إدخال التحليل

- صف أول يحتوي خانة رابط SHEIN وزر `لصق` وزر `تحليل الرابط`.
- تقبل الخانة روابط `onelink` الحديثة وروابط `appjump` القديمة الصالحة.
- صف ثانٍ أسفل الرابط يحتوي خانة `اسم العميل`.
- اسم العميل اختياري، والبديل عند الفراغ هو `shein-cart`.
- يبدأ التحليل عند الضغط على الزر أو مفتاح Enter بعد التحقق من الرابط.

#### 4. حالة العملية

- خانة حالة واضحة تحت اسم العميل.
- مؤشر تحميل متحرك أثناء فتح Chromium والتقاط API.
- رسائل عربية مفهومة للنجاح، الرابط المنتهي، الرابط غير الصالح، فشل الشبكة، وانتهاء المهلة.
- لا تظهر Traceback للمستخدم داخل الواجهة.
- عند النجاح تعرض الحالة عدد المنتجات ومسار الملف المحفوظ تلقائيًا.

#### 5. بطاقات أعداد المنتجات

- بطاقة إجمالي المنتجات.
- بطاقة المنتجات المتاحة.
- بطاقة المنتجات النافدة.
- بطاقة المنتجات غير المتوفرة.
- تستخدم الألوان نفسها في البطاقات وحالة الصفوف لتسهيل القراءة.
- تبقى البطاقات موجودة عند عرض تحليل جديد أو ملف سابق.

#### 6. بطاقة معلومات التحليل المعروض

- تظهر أعلى قائمة المنتجات بعد وجود بيانات.
- تعرض اسم العميل أو الاسم المستنتج من الملف.
- تعرض تاريخ ووقت التحليل بصيغة مقروءة.
- تعرض اسم ملف JSON الحالي.
- تعرض رابط المصدر كاملًا مع إمكانية نسخه لاحقًا.
- تعرض `group_id` والسوق المحلي.
- تميز إن كانت البيانات من تحليل جديد أو من سجل محلي.

#### 7. جدول أو قائمة المنتجات

- يستخدم المكون نفسه لنتيجة التحليل الجديد ونتيجة الملف القديم.
- ترتيب الحقول النهائي: الحالة، الصورة، اسم المنتج، SKU، الخصائص أو المقاس، السعر.
- تظهر صورة مصغرة داخل البرنامج دون فتح المتصفح.
- يقتصر الاسم الظاهر على أول 100 حرف.
- يظهر الاسم الكامل كتلميح عند تمرير المؤشر.
- تظهر `goods_attr` كاملة بقدر المساحة المتاحة.
- تظهر قيمة `amountWithSymbol` للسعر.
- يحتفظ البرنامج بكل SKU مكرر ومتغيراته دون حذف.
- تميز الحالات بالألوان مع إبقاء المنتجات النافدة وغير المتوفرة ظاهرة.
- تظهر النصوص أولًا، ثم تُحمّل الصور تدريجيًا في الخلفية مع صورة بديلة عند الفشل.

#### أبعاد الجدول والصور المعتمدة

- اعتمد المستخدم ضرورة موازنة حجم الجدول بحيث تكون الصور واضحة دون أن تكون كبيرة أو صغيرة بصورة مزعجة.
- الحجم الابتدائي المقترح للصورة المصغرة هو نحو `96×96` بكسل مع الحفاظ على نسبة أبعاد الصورة.
- الارتفاع الابتدائي لصف المنتج نحو `112` بكسل، لترك هامش مريح حول الصورة والنصوص.
- عرض عمود الصورة نحو `112` بكسل، وعرض الحالة والسعر ثابت نسبيًا.
- يحصل اسم المنتج والخصائص على أعمدة مرنة تتمدد مع عرض النافذة.
- يكون عمود SKU بعرض ثابت مناسب لقراءة الرمز دون أخذ مساحة مبالغ فيها.
- يمكن للمستخدم تغيير عرض الأعمدة بالسحب عند الحاجة.
- عند تصغير النافذة تبقى الصورة ثابتة الحجم ويظهر شريط تمرير أفقي بدل ضغط المحتوى حتى يصبح غير مقروء.
- يمكن تعديل القيم الأولية بعد أول تجربة بصرية، لكنها تمثل التوازن المعتمد للنسخة الأولى.

### 2026-07-17 — تنفيذ التصميم النهائي للواجهة والسجل

- أضيفت بيانات `customer_name`, `analyzed_at`, و`output_file` إلى نموذج JSON مع بقائها اختيارية لدعم الملفات القديمة.
- أصبح الحفظ التلقائي يستخدم اسم العميل والتوقيت، مع الاسم الافتراضي `shein-cart` وتنظيف محارف Windows ومنع الكتابة فوق الملفات.
- أضيف اختبار للحفظ باسم عميل عربي ووقت ثابت، واختبار لتنظيف أسماء الملفات.
- أعيد بناء `app.py` بنافذة واحدة و`QSplitter` وسجل على اليسار ومنطقة التحليل والنتائج على اليمين.
- أضيف حفظ عرض السجل وحالته المفتوحة أو المطوية بواسطة `QSettings`.
- أضيف البحث الفوري باسم العميل أو الملف والترتيب من الأحدث إلى الأقدم.
- أضيف تحميل ملفات JSON القديمة والجديدة وعرضها في مكون النتائج نفسه دون تشغيل Playwright.
- أضيفت إعادة تسمية جزء العميل من اسم الملف مع تثبيت التاريخ والوقت وعدم تعديل JSON.
- يستنتج السجل اسم العرض من اسم الملف بعد إعادة التسمية، حتى يظهر الاسم الجديد رغم بقاء JSON دون تعديل.
- أضيف زر `تحليل جديد` يعمل من التحليل الحالي أو السجل دون حذف الملفات.
- أضيف زرا النسخ والتصدير بصورة مستقلة ومعطلة مؤقتًا إلى أن يحدد المستخدم التنسيقات.
- أضيفت بطاقة معلومات العميل والوقت والملف والرابط ومعرّف المجموعة والسوق.
- أعيد ترتيب الجدول إلى الحقول الستة المعتمدة: الحالة، الصورة، الاسم، SKU، الخصائص، والسعر.
- اعتمدت صورة `96×96` وصف `112px` وأعمدة قابلة لتغيير العرض لتحقيق توازن بصري.
- أضيف تحميل الصور داخل البرنامج بواسطة `QNetworkAccessManager` مع تخزين مؤقت وصور بديلة عند الخطأ.
- اختُصر الاسم الظاهر إلى 100 حرف مع تلميح يحتوي الاسم الكامل.
- أضيف اختبار لاستخراج اسم العميل من اسم الملف واختبار لإعادة التسمية مع الحفاظ على التوقيت.
- اجتازت `app.py` وطبقة الحفظ والنماذج والمدقق وجميع ملفات الاختبارات فحص Ruff وفحص التنسيق بعد اكتمال التعديلات.
- يتبقى تشغيل اختبارات pytest محليًا ثم تشغيل الواجهة ومراجعة الأبعاد والصور بصريًا على الجهاز.

### 2026-07-17 — تعديلات المحاذاة والشريط والجدول بعد المعاينة

- طلب المستخدم توسيط جميع النصوص داخل الخانات والبطاقات والجدول، مع إبقاء نصوص سجل التحليلات بمحاذاتها الحالية.
- وُسطت نصوص الرابط واسم العميل والحالة وبطاقات الأعداد وبطاقة معلومات التحليل وخلايا جدول المنتجات.
- بقيت عناصر السجل وخانة البحث فيه كما كانت لسهولة قراءة أسماء الملفات والتفاصيل المتعددة الأسطر.
- نُقل الشريط العلوي إلى المنطقة الرئيسية فقط، فأصبح سجل التحليلات يمتد من أعلى النافذة إلى أسفلها.
- بقي زرا `لصق` و`تحليل الرابط` بجوار خانة الرابط كما طلب المستخدم.
- كُبر زر `تحليل جديد` ووضع في الشريط العلوي بجوار زر إظهار أو إخفاء السجل.
- مُيز زر `تحليل جديد` باللون الأخضر، وزر السجل بالبنفسجي، وزر النسخ بالأزرق، وزر التصدير بالبرتقالي، وزر التحليل بالأزرق السماوي.
- زيد حجم الصورة المصغرة من `96×96` إلى `120×120` بكسل.
- زيد ارتفاع صف المنتج من `112` إلى `140` بكسل، وعرض عمود الصورة إلى نحو `144` بكسل.
- صُغر عرض عمود الخصائص إلى نحو `250` بكسل، وعمود السعر إلى نحو `88` بكسل.
- زيدت مساحة اسم المنتج إلى نحو `390` بكسل، مع بقاء الأعمدة قابلة لتغيير العرض بالسحب.

#### التحقق بعد التعديل

- نجح فحص `Ruff` بالكامل: `All checks passed!`.
- أكد فحص التنسيق أن الملفات السبعة المستهدفة منسقة بالفعل.
- تعذر تشغيل `pytest` من جلسة Codex لأن نظام التشغيل رفض إنشاء عملية Python من `.venv` برسالة `Access is denied`؛ لذلك يلزم تشغيل الاختبارات من طرفية VS Code المحلية.

#### 8. الحفظ التلقائي

- عند بدء التحليل يلتقط البرنامج اسم العميل والتوقيت الحالي.
- بعد نجاح الاستخراج، يحفظ JSON تلقائيًا داخل `outputs`.
- يحتوي JSON على جميع الحقول، وليس فقط الحقول الستة الظاهرة في الواجهة.
- يستخدم اسم العميل والتوقيت في اسم الملف.
- يستخدم `shein-cart` تلقائيًا إذا لم يكتب المستخدم اسمًا.
- بعد الحفظ يحدث السجل فورًا ويظهر الملف الجديد في أعلى القائمة.

#### 9. سجل التحليلات الجانبي

- يوجد في عمود يسار النافذة وقابل لتغيير الحجم والطي.
- يحتوي عنوانًا وعدد الملفات وخانة بحث وزر مسح نص البحث.
- يرتب الملفات حسب وقت التحليل من الأحدث إلى الأقدم.
- يعرض كل عنصر اسم العميل أو اسم الملف، التوقيت، العدد الإجمالي، وملخص الحالات.
- يميز العنصر المعروض حاليًا.
- النقر على عنصر يقرأ JSON محليًا ويعرضه في منطقة النتائج نفسها دون تشغيل الإنترنت أو Playwright.
- يحفظ موضع التمرير والعنصر المحدد أثناء التنقل ما أمكن.
- لا توجد وظيفة حذف للسجلات.

#### 10. البحث في السجل

- يعمل البحث فور الكتابة.
- يبحث باسم العميل أو اسم الملف.
- لا يغير الترتيب الزمني.
- عند مسح البحث تعود جميع الملفات للظهور.
- لا يعدل البحث أي ملف على القرص.

#### 11. إعادة تسمية ملف من السجل

- يتوفر إجراء `إعادة تسمية` لكل عنصر أو من قائمة النقر بزر الفأرة الأيمن.
- تعرض نافذة صغيرة اسم العميل الحالي فقط كحقل قابل للتعديل.
- تعرض التاريخ والوقت بجانبه كقيمة ثابتة غير قابلة للتحرير.
- بعد التأكيد يعاد تكوين اسم الملف مع نفس التاريخ والوقت.
- لا يعدل JSON ولا ترتيب العنصر الزمني.
- يمنع الاسم الفارغ أو غير الصالح أو المتعارض مع ملف موجود.

#### 12. النسخ والتصدير

- زر النسخ وزر التصدير منفصلان نهائيًا.
- يعملان مع تحليل جديد أو ملف مختار من السجل.
- تنسيق النسخ سيحدد لاحقًا، ويمكن أن يكون نصًا مرتبًا للحافظة.
- تنسيق التصدير سيحدد لاحقًا، ويمكن أن يدعم ملفًا أو أكثر.
- تغيير تنسيقهما مستقبلًا لا يغير JSON الأصلي.

#### 13. زر تحليل جديد

- يعمل من أي حالة: بعد تحليل، أو أثناء عرض ملف سابق، أو بعد ظهور خطأ.
- يمسح الرابط واسم العميل والحالة والعدادات وبطاقة المعلومات والنتائج.
- لا يحذف السجل أو ملفات JSON.
- لا يعمل أثناء عملية استخراج جارية حتى لا تتلف الجلسة.
- بعد التنفيذ يعيد التركيز إلى خانة الرابط.

#### 14. التوافق والملفات القديمة

- يقرأ البرنامج ملفات JSON القديمة الموجودة حاليًا داخل `outputs`.
- إذا لم يحتو الملف القديم اسم عميل، يعرض اسمًا مستنتجًا أو `غير محدد`.
- إذا لم يحتو وقتًا وصفيًا، يستخرجه من اسم الملف أو تاريخ تعديل الملف.
- يتحقق Pydantic من الملف قبل عرضه.
- إذا كان الملف تالفًا، يظهر في السجل بحالة خطأ واضحة ولا يعطل البرنامج كله.

#### 15. الأداء

- يعمل Playwright داخل `QThread`.
- لا تُحمّل الصور على مسار الواجهة الرئيسي.
- يعتمد السجل على ملفات JSON مباشرة دون قاعدة بيانات حاليًا.
- يمكن إضافة تحميل تدريجي للسجل إذا زاد عدد الملفات كثيرًا.
- تعاد استخدام الصور المحملة في الجلسة لتقليل الشبكة وتحسين التنقل.

#### 16. ترتيب التنفيذ بعد الاعتماد

1. تحديث نموذج JSON والحفظ لدعم اسم العميل والتوقيت واسم الملف.
2. إضافة أدوات آمنة لتنظيف الاسم وإعادة تسمية جزء العميل فقط.
3. إعادة بناء `app.py` باستخدام `QSplitter` والسجل الجانبي.
4. إضافة البحث والتحميل من ملفات `outputs`.
5. توحيد مكون عرض النتائج للبيانات الجديدة والقديمة.
6. إضافة الصور المصغرة غير المتزامنة واختصار الاسم.
7. إضافة زر تحليل جديد وحفظ حالة وعرض السجل بواسطة `QSettings`.
8. إضافة أزرار النسخ والتصدير بحالة معطلة إلى أن يحدد المستخدم التنسيق.
9. إجراء اختبارات يدوية على روابط حديثة وقديمة ومنتهية وعلى ملفات سجل قديمة.

#### رأي التصميم

- هذا التصميم مناسب أكثر لسير العمل المتكرر لأنه يبقي السجل والنتيجة أمام المستخدم في الوقت نفسه.
- العمود القابل لتغيير الحجم يمنع السجل من أخذ مساحة ثابتة غير ضرورية.
- إعادة استخدام منطقة النتائج تقلل تعقيد الكود وتمنع اختلاف عرض البيانات الجديدة عن القديمة.
- زر `تحليل جديد` أوضح وظيفيًا من زر `مسح` لأنه يصف هدف المستخدم ولا يوحي بحذف الملفات.
- فصل النسخ والتصدير من البداية يمنع تداخل المتطلبات عندما يحدد المستخدم تنسيق كل عملية لاحقًا.

#### تحسينات الأداء والاعتمادية

- يبقى تحليل الرابط في `QThread` حتى تظل الواجهة مستجيبة.
- تُحمّل الصور بشكل غير متزامن ومستقل عن عملية استخراج البيانات.
- تظهر البيانات النصية فور اكتمال الاستخراج، ثم تظهر الصور تدريجيًا دون تأخير عرض النتائج.
- عند فتح تحليل سابق، تُقرأ البيانات محليًا بسرعة دون اتصال بالشبكة.
- يُتحقق من سلامة الملف بواسطة نماذج Pydantic قبل عرضه من السجل.
- لا تُخزن بيانات السجل مرتين؛ ملفات JSON هي المصدر الأساسي، ويمكن إضافة فهرس خفيف لاحقًا فقط إذا أصبح الأداء بحاجة إليه.

#### نطاق النسخة التالية

1. تعديل تسمية ملفات النتائج لدعم اسم العميل والتوقيت.
2. إضافة بيانات العميل ووقت التحليل إلى نموذج JSON.
3. إعادة ترتيب جدول الواجهة وفق الحقول الستة المعتمدة.
4. اختصار اسم المنتج إلى 100 حرف مع تلميح الاسم الكامل.
5. إضافة صور مصغرة محملة في الخلفية.
6. إضافة زر `مسح / تحليل جديد`.
7. إضافة زر `نسخ البيانات` مع إبقائه معطلًا إلى أن يُحدد تنسيق النسخ.
8. إضافة نافذة أو لوحة `سجل التحليلات` لقراءة ملفات `outputs` وعرضها دون إعادة الاستخراج.

---

## 2026-07-21 — تطبيق تعديلات واجهة Figma على البرنامج

### مصدر التصميم

- جرى جلب التصميم المحدّث من ملف Figma ذي المعرّف `RKTyfMTN59jiSnsydwtcvf`.
- استُخدمت الشاشة الرئيسية عند العقدة `4:2` مرجعًا مباشرًا للأبعاد، الألوان، المسافات، وترتيب العناصر.
- حُفظت أيقونة إظهار وإخفاء السجل الأصلية من Figma داخل `assets/history-toggle.svg` بدل استخدام رمز نصي تقريبي.

### التعديلات المنفذة

1. ضبط الهوامش الخارجية على `16px` والمسافات الأساسية على `10–14px` بما يطابق التصميم.
2. ضبط عرض السجل الابتدائي على نحو `310px` مع استمرار دعمه لتغيير الحجم والطي وحفظ العرض المختار.
3. تبسيط الشريط العلوي ليحتوي على زر أيقونة السجل، زر `تحليل جديد` الأخضر، وعنوان البرنامج.
4. نقل الرابط واسم العميل وزري اللصق والتحليل إلى صف واحد بارتفاع مضغوط وواضح.
5. إنشاء منطقة ملخص موحدة تتكون من:
   - عمود مستقل لزرّي `تصدير البيانات` و`نسخ البيانات`.
   - بطاقة وسطية للحالة ومعلومات الملف والرابط والمجموعة.
   - مجموعة عدادات مدمجة: الإجمالي في الأعلى، ثم المتاح والنافد وغير المتوفر في صف واحد.
6. تحديث الجدول إلى أبعاد Figma التقريبية مع أعمدة متوازنة:
   - الحالة `110px`.
   - الصورة `140px` مع صورة فعلية `120×120px`.
   - اسم المنتج `362px`.
   - `SKU` بعرض `200px`.
   - الخصائص والمقاس `210px`.
   - السعر `110px`.
7. ضبط ارتفاع رأس الجدول على `44px` وارتفاع الصف على `133px` للحفاظ على وضوح الصور وتقليل الفراغ الزائد.
8. اعتماد خط `Noto Sans Arabic` عند توفره مع الرجوع إلى `Segoe UI` تلقائيًا.
9. تحسين عناصر السجل ببطاقات أطول، حواف `8px`، ومسافات داخلية أوضح، مع إبقاء ترتيب الملفات من الأحدث إلى الأقدم.
10. الحفاظ على جميع وظائف البرنامج السابقة دون تغيير: التحليل في الخلفية، السجل، البحث، إعادة التسمية، تحميل الصور، وحفظ JSON التلقائي.

### معالجة اتجاه الواجهة

- بقي الاتجاه العام للبرنامج `RTL` لدعم العربية.
- ثُبت اتجاه حاوية الإدخال وحاوية الملخص على `LTR` فقط لضمان مطابقة ترتيب Figma بصريًا.
- بقي اتجاه النصوص والحقول والمحتوى الداخلي عربيًا `RTL`، وبقيت خلايا الجدول في المنتصف.

### التحقق البصري والتقني

- نجح إنشاء `MainWindow` باستخدام منصة Qt غير المرئية `offscreen`.
- جرى التحقق من تحميل أيقونة Figma وعدم رجوع `QIcon.isNull()` بقيمة صحيحة.
- أُنشئت معاينة فارغة في `diagnostics/figma-ui-preview.png` لمراجعة التخطيط الأساسي.
- أُنشئت معاينة ببيانات JSON فعلية في `diagnostics/figma-ui-data-preview.png` لمراجعة الصفوف والصور والنصوص الطويلة.
- أكدت المعاينة أن الإجراءات تظهر يسارًا، معلومات التحليل في الوسط، والعدادات يمينًا كما في Figma.
- نجح فحص `ruff check app.py` دون ملاحظات.
- ظهر خطأ ترميز عند طباعة نص زر عربي داخل طرفية Windows فقط؛ لم يؤثر في إنشاء النافذة أو عرض النص داخل Qt أو ملفات JSON.

### نتائج التحقق النهائي

- نجح `ruff format --check app.py tests` وكانت الملفات الأربعة منسقة مسبقًا.
- نجح `ruff check app.py tests` دون أخطاء أو تحذيرات.
- تعذر تشغيل ثلاثة اختبارات في المحاولة الأولى بسبب منع الوصول إلى مجلد pytest العام داخل `%TEMP%`، بينما نجحت الاختبارات العشرة التي لا تستخدم `tmp_path`.
- أُعيد التشغيل باستخدام `--basetemp=.pytest-tmp-run -p no:cacheprovider`، ونجحت المجموعة كاملة: `13 passed in 0.87s`.
- نجح اختبار واجهة `offscreen` في إنشاء النافذة، تحميل أحدث ملف من السجل، التحقق من أيقونة Figma، والتحقق من الجدول ذي الأعمدة الستة.
- أضيف `.pytest-tmp-run/` إلى `.gitignore` لأن Windows أبقى مجلد الاختبار المؤقت بعد انتهاء التشغيل.

---

## 2026-07-22 — الإدخال الموحد للفاتورة ورابط السلة

### القرار المعتمد

- اعتماد منطقة إدخال واحدة متعددة الأسطر تقبل فاتورة العميل كاملة أو رابط SHEIN فقط.
- استخراج ثلاثة حقول فقط من الفاتورة وتجاهل بقية محتواها: `customer_name` و`order_number` ورابط السلة.
- إبقاء الحقول المستخرجة قابلة للتعديل اليدوي قبل بدء التحليل.
- استخدام زر تحليل واحد مشترك، مع زر لصق خاص بالفاتورة وزر لصق خاص بحقل الرابط.
- رفض النص الذي يحتوي أكثر من رابط SHEIN لتجنب ربط عميل أو رقم طلبية بالسلة الخطأ.

### التنفيذ

1. إنشاء `invoice_parser.py` بمحلل مستقل يدعم اختلاف المسافات والأسطر والرموز الزخرفية في الفاتورة.
2. دعم روابط `onelink.shein.com` وروابط `api-shein.shein.com` القديمة.
3. إضافة `order_number` اختياريًا إلى نموذج `CartExtraction`، مع استمرار قراءة ملفات JSON القديمة دون تعديلها.
4. تمرير رقم الطلبية عبر `ExtractionWorker` إلى `write_output` وحفظه داخل JSON.
5. اعتماد تسمية الملفات بالشكل `رقم-الطلبية-اسم-العميل-YYYYMMDD-HHMMSS.json` عند توفر الرقم والاسم.
6. الإبقاء على `shein-cart-YYYYMMDD-HHMMSS.json` عند لصق رابط فقط دون اسم أو رقم.
7. إضافة رقم الطلبية إلى معلومات التحليل وبطاقات السجل ونتائج البحث.
8. الحفاظ على رقم الطلبية عند إعادة تسمية اسم العميل من السجل.
9. إضافة منطقة الفاتورة فوق صف الحقول، وترتيب الحقول بصريًا: رقم الطلبية، اسم العميل، رابط السلة.
10. تعطيل جميع حقول وأزرار الإدخال الجديدة أثناء عمل الاستخراج في الخلفية.

### الاختبارات الأولية

- نجح تحليل الفاتورتين النموذجيتين واستخراج `H-2237` و`T-501` والاسم والرابط الموافق لكل فاتورة.
- نجح التعامل مع رابط منفرد مع ترك الاسم ورقم الطلبية فارغين.
- نجح اكتشاف لصق فاتورتين معًا باعتباره حالة روابط متعددة.
- نجحت اختبارات المحلل والحفظ وإعادة التسمية: `15 passed in 0.33s`.
- نجح اختبار واجهة `offscreen` في تعبئة `حياة شطوان` و`T-501` ورابط `onelink` داخل الحقول الصحيحة.
- حُفظت معاينة الواجهة في `diagnostics/invoice-input-preview.png`.

### التحقق النهائي

- زيد ارتفاع بطاقة السجل تلقائيًا عند وجود `order_number` حتى لا يُقص سطر رقم الطلبية.
- نجح `ruff format --check` على ملفات التطبيق والاختبارات، وكانت جميع الملفات منسقة.
- نجح `ruff check` دون أخطاء أو تحذيرات.
- نجحت مجموعة اختبارات المشروع كاملة بعد التعديل: `18 passed in 0.38s`.

---

## 2026-07-22 — تحليل تشغيل البرنامج من n8n وTelegram

### ما تم فحصه

- تتبع مسار الفاتورة من `parse_invoice_text` إلى `extract_cart` ثم `write_output`.
- التحقق من أن `extract_cart.py` يملك طبقة استخراج مستقلة يمكن إعادة استخدامها دون تشغيل واجهة `PySide6`.
- التحقق من أن الإخراج الحالي JSON فقط، وأن زري النسخ والتصدير في الواجهة غير مفعّلين، ولا توجد مكتبة أو دالة لتوليد PDF.
- مراجعة وثائق n8n الرسمية لعقد `Telegram Trigger` و`Telegram` و`Execute Command`.

### الخيارات التي جرى تقييمها

1. **خدمة HTTP مستقلة — الخيار الموصى به:** إضافة API تستقبل نص الفاتورة وتعيد PDF أو خطأ منظمًا. يعمل هذا الخيار مع n8n المستضاف ذاتيًا وn8n Cloud، ويفصل Playwright ومكتبات Python عن حاوية n8n.
2. **عقدة `Execute Command`:** مناسبة لتجربة محلية سريعة إذا كان n8n مستضافًا ذاتيًا على الجهاز نفسه، لكنها غير متاحة في n8n Cloud، ومع Docker ينفذ الأمر داخل حاوية n8n لا على الجهاز المضيف. كما أنها معطلة افتراضيًا ابتداءً من n8n 2.0 لأسباب أمنية.
3. **SSH إلى جهاز تشغيل البرنامج:** خيار وسيط إذا كان المستخرج على جهاز منفصل ولا يمكن نشر API، لكنه يزيد إدارة الملفات والمسارات والمفاتيح.
4. **عقدة n8n مخصصة أو إدخال من قاعدة بيانات/طابور:** غير ضروري للنسخة الأولى؛ يصبح مفيدًا فقط عند ارتفاع عدد الطلبات والحاجة إلى عمال متعددين وحالة مهام غير متزامنة.

### التدفق المقترح

```text
Telegram Trigger
  -> التحقق من chat.id ومن وجود message.text
  -> HTTP Request: POST /analyze-invoice
  -> IF success && product_count > 0
       -> Telegram: Send Document إلى chat.id نفسه
     ELSE
       -> Telegram: Send Message برسالة الفشل إلى chat.id نفسه
```

### عقد الخدمة المقترح

- الطلب: `invoice_text` مع معرّفات Telegram لأغراض التتبع فقط عند الحاجة.
- النجاح: PDF كاستجابة ثنائية مع معلومات مختصرة في الرؤوس، أو JSON يحتوي رابط تنزيل مؤقتًا.
- الفشل المتوقع: رمز مناسب ورسالة عربية منظمة مثل `invalid_invoice` أو `multiple_links` أو `expired_link` أو `no_products` أو `extraction_failed`.
- شرط النجاح التجاري: وجود منتج واحد على الأقل؛ فالنسخة الحالية لا تحول قائمة `products` الفارغة إلى فشل تلقائيًا.

### النتيجة

- لا يمكن إكمال السيناريو المطلوب من n8n بالكود الحالي وحده لأن مولد PDF وواجهة الاستدعاء الآلي غير موجودين.
- طبقة الاستخراج الحالية قابلة لإعادة الاستخدام، لذلك لا حاجة لمحاكاة لصق النص أو الضغط على أزرار الواجهة.
- لم يُغيّر الكود ولم تُشغّل الاختبارات في هذه الخطوة لأنها كانت تحليلًا معماريًا وتوثيقًا فقط.

---

## 2026-07-22 — خيارات استضافة المحلل بجانب n8n على Hostinger

### معلومات البيئة التي تم التحقق منها

- توضح وثائق Hostinger أن قالب `Ubuntu 24.04 with n8n` يشغّل n8n داخل Docker، وأن ملف Compose موجود افتراضيًا في `/root/docker-compose.yml` ويمكن تعديله عبر SSH.
- تسمح استضافة VPS لدى Hostinger بتثبيت برامج وحاويات مخصصة بصلاحية root، بخلاف الاستضافة المشتركة أو Cloud Hosting المقيدة.
- بحسب صفحة خطط Hostinger وقت التحليل: `KVM 1` يوفّر `1 vCPU / 4 GB RAM`، و`KVM 2` يوفّر `2 vCPU / 8 GB RAM`. الأسعار متغيرة ولم تعتمد كقرار للمشروع.
- توصي وثائق Playwright عند تشغيل Chromium داخل Docker باستخدام `--init` و`--ipc=host` لتقليل العمليات العالقة وتجنب نفاد الذاكرة المشتركة، وبمطابقة إصدار Playwright مع إصدار المتصفح في الصورة.

### الخيار الموصى به للبدء

- تشغيل خدمة مستقلة باسم `shein-api` في حاوية Docker ثانية على **نفس Hostinger VPS** ونفس شبكة Docker الخاصة بـn8n.
- يستدعي n8n الخدمة داخليًا عبر عنوان مثل `http://shein-api:8000/analyze-invoice`؛ وبذلك لا يحتاج منفذ API إلى الظهور على الإنترنت ولا يحتاج نطاقًا عامًا مستقلًا.
- يبقى n8n هو الطرف العام الذي يستقبل Telegram، بينما تنفذ حاوية Python تحليل الفاتورة وPlaywright وتوليد PDF ثم تعيد الملف إلى n8n.
- عدم تشغيل `app.py` أو واجهة `PySide6` على الخادم؛ يعاد استخدام `invoice_parser.py` و`extract_cart.py` والنماذج داخل خدمة API بلا واجهة سطح مكتب.
- بدء التشغيل بتزامن تحليل واحد فقط، لأن كل طلب يفتح Chromium، ثم رفع التزامن بعد قياس فعلي للذاكرة والمعالج.

### تقدير الموارد

- `KVM 1` قد يكفي للتجربة أو الاستخدام الخفيف جدًا، لكنه يترك هامشًا محدودًا عند تشغيل n8n وChromium وتوليد PDF معًا، خصوصًا مع `1 vCPU`.
- `KVM 2` هو الاختيار العملي الموصى به لنفس الخادم بسبب `2 vCPU / 8 GB RAM`، مع مراقبة الموارد في أول أسبوع تشغيل.
- إذا كانت workflows الأخرى ثقيلة أو وصلت الفواتير بصورة متزامنة، ينقل `shein-api` إلى VPS ثانٍ بدل زيادة مخاطر تأثير Chromium في n8n.
- هذا التقدير معماري وليس قياسًا فعليًا؛ القرار النهائي يتطلب benchmark على الخطة الحالية.

### البدائل

1. **VPS Hostinger ثانٍ للمحلل:** أفضل عزل واعتمادية، لكنه يضيف تكلفة وإدارة خادم آخر. يتصل به n8n عبر HTTPS ومفتاح API.
2. **منصة حاويات مُدارة تدعم Docker:** تقلل إدارة الخادم، لكنها قد تعاني من التشغيل البارد والمهلات والتكلفة المتغيرة مع Chromium، لذلك ليست الخيار الأول.
3. **Serverless Functions التقليدية:** غير موصى بها لهذا المشروع بسبب حجم Chromium، زمن التحليل، والملفات الثنائية والمهلات.
4. **تشغيل Playwright كخدمة متصفح بعيدة:** مفيد عند التوسع الكبير، لكنه يزيد المكونات والتكلفة قبل وجود حاجة فعلية.

### متطلبات التشغيل الآمن

- عدم نشر منفذ `8000` للعامة عند استخدام نفس شبكة Docker؛ وإذا اضطر للنشر، يجب وضع HTTPS ومفتاح API وتقييد الوصول.
- عدم تمرير نص Telegram إلى shell أو `Execute Command`؛ يرسل كـJSON إلى API لمنع حقن الأوامر.
- إضافة حد لحجم نص الفاتورة، مهلة للتحليل، رسائل خطأ منظمة، وتنظيف دوري لملفات PDF وJSON المؤقتة.
- إضافة `healthcheck` وإعادة تشغيل تلقائي، وحفظ الأسرار في `.env` خارج المستودع.
- حفظ نسخة احتياطية من ملف Compose وبيانات n8n قبل أي تعديل على VPS الحالي.

### النتيجة

- أفضل توازن حاليًا هو **نفس Hostinger VPS + حاويتان منفصلتان (`n8n` و`shein-api`) + اتصال داخلي**.
- يوصى بـ`KVM 2` إذا كانت الخطة الحالية أقل أو إذا كان n8n مستخدمًا في workflows أخرى، مع إبقاء خيار فصل المحلل في VPS ثانٍ عند نمو الحمل.
- لم يُنفذ نشر أو تعديل كود في هذه الخطوة، ولم تُشغّل اختبارات؛ تم تحديث التوثيق بالتحليل والقرار المبدئي فقط.

---

## 2026-07-22 — مراجعة خطة Hostinger الفعلية من لوحة hPanel

### البيانات الظاهرة في الصور

- الخطة الحالية: `KVM 2`.
- نظام التشغيل والقالب: `Ubuntu 24.04 with n8n`، وظهور `Docker Manager` يؤكد ملاءمة البيئة لإضافة حاوية مستقلة.
- الموارد: `2 CPU cores` و`8 GB RAM` و`100 GB` مساحة و`8 TB` bandwidth.
- الاستخدام وقت التقاط الصور: CPU نحو `1%`، والذاكرة نحو `11%`، والقرص `5 GB / 100 GB`، واستهلاك bandwidth نحو `0.007 TB / 8 TB`.
- توجد صلاحية root وSSH، والخادم يعمل منذ نحو 14 يومًا وقت الصورة.
- موقع الخادم: باريس، فرنسا.
- النسخ الاحتياطي الحالي أسبوعي، وتظهر لقطتان ضمن `Snapshot & backups`.
- تعرض لوحة Hostinger عدد `0` تحت `Firewall rules`؛ لا يثبت ذلك وحده حالة جدار Ubuntu الداخلي، لكنه يستلزم فحص قواعد Hostinger و`ufw` قبل النشر.

### التقييم

- الموارد الحالية مناسبة للمرحلة الأولى، ولا توجد حاجة إلى ترقية الخطة أو شراء VPS ثانٍ الآن.
- هامش الذاكرة والقرص كبير وفق لقطة الاستخدام، لكن يلزم قياس الاستهلاك أثناء تحليل فعلي لأن Chromium هو الحمل الأكبر وليس n8n في وضع الخمول.
- يبدأ المحلل بعملية واحدة متزامنة، مع مراقبة CPU وRAM؛ لا يرفع التزامن قبل جمع نتائج فعلية.
- موقع باريس مقبول مبدئيًا، لكن يجب اختبار رابط SHEIN فعلي من IP الخادم لأن سلوك الموقع قد يختلف مع عناوين مراكز البيانات أو الموقع الجغرافي.

### توصيات قبل النشر

1. أخذ Snapshot يدوي قبل تعديل `docker-compose.yml` أو خدمات n8n.
2. إضافة SSH key واستخدامه بدل الاعتماد المستمر على كلمة مرور root، ثم مراجعة تعطيل دخول root بكلمة المرور بعد التأكد من المفتاح.
3. ضبط Hostinger Firewall أو `ufw` للسماح فقط بالمنافذ العامة اللازمة مثل SSH وHTTP/HTTPS؛ لا يفتح منفذ `shein-api` للعامة.
4. تشغيل اختبار دخان لـPlaywright على VPS باستخدام رابط معروف قبل بناء التكامل الكامل، للتأكد من عدم حظر SHEIN لعنوان الخادم.
5. بعد نجاح اختبار الدخان، نشر `shein-api` على شبكة Docker الداخلية، ثم قياس الذاكرة والمعالج أثناء تحليل فاتورة كاملة وتوليد PDF.
6. الإبقاء على النسخ الأسبوعي مبدئيًا مع تصدير دوري لبيانات n8n؛ الترقية للنسخ اليومي قرار تشغيلي وليست شرطًا لبدء المشروع.

### القرار

- اعتماد نفس `KVM 2` للنسخة الأولى دون ترقية.
- لا يشترى نطاق منفصل للمحلل ولا يفتح API على الإنترنت؛ يتصل n8n به عبر شبكة Docker الداخلية.
- نقطة التحقق الحاسمة التالية هي نجاح Playwright وSHEIN على Ubuntu داخل VPS، ثم قياس الموارد تحت الحمل.
- لم يُنفذ أي تغيير على الخادم أو كود التطبيق في هذه الخطوة، ولم تُشغّل اختبارات؛ التحليل مبني على صور hPanel المرفقة.
---

## 2026-07-22 — جلب تصميم Figma المحدث وتحليل بحث المنتجات

### التصميم الذي تم التحقق منه

- تم جلب أحدث تصميم من صفحة `Current App UI v2` والإطار `56:84` في ملف Figma.
- أضيف حقل `Search Field` في الشريط العلوي بجوار زر `تحليل جديد`، مع النص الإرشادي: `بحث بالاسم، SKU، أو المقاس...`.
- بقي بحث السجل الجانبي مستقلًا ومخصصًا لاسم العميل أو اسم الملف.
- لم يُنفذ أي تعديل وظيفي على `app.py` في هذه الخطوة.

### السلوك المقترح لبحث المنتجات

- تصفية صفوف الجدول محليًا وفوريًا دون تعديل بيانات `current_extraction` أو ملف JSON.
- البحث دون حساسية لحالة الأحرف داخل `goods_name` و`sku_code` و`goods_attr`، مع دعم البحث في الحالة والسعر كتحسين إضافي.
- إظهار عدد النتائج المطابقة بجانب حقل البحث، وإظهار رسالة واضحة عند عدم وجود نتائج.
- إعادة عرض جميع الصفوف عند مسح البحث أو فتح تحليل آخر، مع بقاء بطاقات الملخص ممثلة لإجمالي التحليل الأصلي.
- لا تُشغّل الاختبارات لأن هذه الخطوة اقتصرت على جلب التصميم والتحليل قبل موافقة المستخدم على التنفيذ.

### التنفيذ بعد اعتماد المقترح

- طُبّق حقل بحث المنتجات في الشريط العلوي بجوار زر `تحليل جديد` وفق تصميم Figma، مع عداد يوضح عدد الصفوف الظاهرة من إجمالي المنتجات.
- يبحث الحقل فوريًا داخل `goods_name` و`sku_code` و`goods_attr` والحالة والسعر.
- تعتمد المطابقة على نص موحد غير حساس لحالة الأحرف أو المسافات الزائدة، مع تجاهل التشكيل والتطويل العربي.
- تُخفى الصفوف غير المطابقة باستخدام `setRowHidden` بدل إعادة إنشاء الجدول، وبذلك تبقى الصور والخلايا المحملة دون إعادة تنزيل.
- تظهر رسالة `لا توجد منتجات مطابقة للبحث.` عند انعدام النتائج، وتعود جميع الصفوف عند مسح البحث.
- يُمسح بحث المنتجات تلقائيًا عند بدء تحليل جديد أو فتح تحليل من السجل، وتبقى بطاقات الإحصائيات ممثلة لإجمالي التحليل الأصلي.

### الاختبارات والتحقق

- الأمر: `.\.venv\Scripts\python.exe -m ruff check app.py tests\test_app_helpers.py`
- النتيجة: `All checks passed!`
- تعذر التشغيل الأول لـ`pytest` بسبب منع الوصول إلى مجلد Windows المؤقت `pytest-of-abd73`، وليس بسبب فشل وظيفي.
- أعيد الاختبار بمجلد مؤقت داخل المشروع وباستخدام `-p no:cacheprovider --basetemp`.
- النتيجة النهائية: `21 passed in 0.34s`.
- نجح فحص واجهة غير مرئي: البحث عن `sw200` أخفى الصف غير المطابق وأظهر `1 من 2`، والبحث غير المطابق عرض حالة النتائج الفارغة.
- أضيف اختبار صريح لحالات البحث الثلاث: مطابقة صف واحد، مطابقة عدة صفوف، وعدم وجود أي صف مطابق.

### تصحيح مطابقة واجهة Figma بعد التجربة اليدوية

- أظهرت مقارنة لقطة البرنامج مع تصميم Figma أن منطق البحث كان مطبقًا، لكن منطقة إدخال الفاتورة بقيت بالتخطيط السابق.
- أعيد بناء لوحة الإدخال بارتفاع `108px`: زر `تحليل الرابط` بعرض `110px` وارتفاع `88px` ممتد بجانب صفّي المحتوى.
- أصبح زر `لصق الفاتورة` وحقل الفاتورة في الصف الأول بارتفاع `44px`، وزر `لصق الرابط` وحقول الرابط واسم العميل ورقم الطلبية في الصف الثاني.
- أزيل عداد النتائج المرئي من الشريط العلوي لأنه غير موجود في تصميم Figma النهائي؛ بقيت قيمته الداخلية وحالة عدم وجود نتائج مستخدمتين في منطق البحث.
- تمت مراجعة لقطة محلية بحجم `1500x900` وأكدت تطابق البنية العامة، موضع البحث، وامتداد زر التحليل مع التصميم.
- فحص `ruff`: `All checks passed!`.
- الاختبارات الكاملة: `22 passed in 0.34s`.

---

## 2026-07-22 — تنظيف المشروع والإبقاء على عناصر التشغيل

### العناصر المحذوفة

- مجلدات الاختبارات والكاشات المؤقتة: `tests/` و`.ruff_cache/` و`.test-tmp/` و`.pytest-tmp-run/` و`__pycache__/`.
- ملفات التشخيص القديمة داخل `diagnostics/`؛ يمكن لأداة `diagnose_link.py` إنشاء المجلد من جديد عند تشغيلها.
- المجلد المكرر `SHEIN Customer SKU Extractor/` الذي احتوى مستودع Git منفصلًا وملف README فقط.
- أداة التحقق المستقلة `validate_output.py` لأنها ليست جزءًا من تشغيل الواجهة أو استخراج السلة.

### العناصر المحفوظة

- `.venv/` لضمان استمرار التشغيل مباشرة دون إعادة تثبيت المكتبات.
- `outputs/` لأنه يمثل سجل التحليلات الذي تعرضه الواجهة.
- ملفات التطبيق الأساسية و`assets/` وملفات التوثيق.

### المكتبات والتوثيق

- أزيل `pytest` و`ruff` من `requirements.txt` بعد حذف ملفات الاختبار.
- أزيلت حزمتا `pytest` و`ruff` فعليًا من `.venv`، فانخفض حجم البيئة من نحو `817 MB` إلى نحو `782 MB`.
- حُدث `README.md` لإزالة أوامر الاختبار والتحقق المحذوفة وإضافة قائمة واضحة بالملفات الأساسية.

### التحقق بعد التنظيف

- نجح `pip check` بالنتيجة: `No broken requirements found.`
- نجح فحص تشغيل الواجهة بوضع غير مرئي: `Application startup smoke test passed`.
- بقي مجلد `.pytest_cache/` فارغًا بحجم صفري بسبب ACL غير طبيعي يمنع الحساب الحالي من حذفه؛ لا يحتوي ملفات ولا يؤثر في التشغيل، ويمكن حذفه لاحقًا من PowerShell بصلاحية Administrator.

---

## 2026-07-22 — إنشاء الخطة الشاملة للنشر وربط n8n وTelegram

### القرار

- إنشاء وثيقة تنفيذ مستقلة باسم `DEPLOYMENT_PLAN.md` لتكون المرجع العملي من بداية تطوير خدمة الخادم حتى اعتماد الإنتاج.
- اعتماد نفس Hostinger VPS بخطة `KVM 2` للمرحلة الأولى، مع حاوية `shein-api` منفصلة واتصال داخلي مع n8n.
- اعتماد API على مرحلتين: طلب تحليل يعيد JSON منظمًا، ثم طلب مستقل لتنزيل PDF؛ يسمح ذلك لـn8n بالتفرع قبل التعامل مع الملف الثنائي.
- اعتماد `Idempotency-Key` مبني على `chat.id` و`message_id` لمنع إعادة تحليل الرسالة عند Retry.
- إبقاء استقبال الصور وOCR خارج نطاق النسخة الأولى، وقصرها على الفاتورة النصية.

### محتوى الخطة

- 27 قسمًا تغطي النطاق والمعمارية وجرد VPS والنسخ الاحتياطي وإعادة تنظيم Python وتوحيد الأخطاء وPDF وFastAPI والاختبارات وDocker.
- خطوات اختبار Playwright من عنوان Hostinger قبل نشر التكامل الكامل، لأن نجاحه على Windows لا يثبت نجاحه من IP مركز البيانات.
- تفاصيل نشر الخدمة في شبكة Docker الداخلية دون فتح المنفذ `8000` للعامة.
- إعداد Telegram Bot وPrivacy Mode وتقييد `chat.id` وبناء Workflow n8n عقدة بعقدة.
- مصفوفة اختبارات نجاح وفشل وحماية وتشغيل وموارد.
- متطلبات الأمان والخصوصية وتقليل الاحتفاظ ببيانات executions والملفات الثنائية.
- سياسات المراقبة والنسخ الاحتياطي والاستعادة والتراجع والتشغيل التجريبي.
- قائمة قبول نهائية وتسليمات وترتيب دفعات التنفيذ.

### الملفات المحدثة

- أضيف `DEPLOYMENT_PLAN.md`.
- أضيف رابط الخطة إلى `README.md`.
- حُدّث `PROJECT_LOG.md` بهذا القرار.

### التحقق

- جرى فحص بنية العناوين والكلمات المحورية داخل الوثيقة باستخدام `rg`.
- عدد أسطر الخطة وقت الإنشاء: `1279` سطرًا.
- لم يُعدل كود التطبيق ولم تُشغّل اختبارات Python لأن هذه الخطوة توثيق وتخطيط فقط.

### الخطوة التالية

- مراجعة المستخدم لنطاق النسخة الأولى واعتماد محتوى PDF ورسائل Telegram.
- بعد الاعتماد يبدأ التنفيذ محليًا بإنشاء خدمة التحليل الموحدة، ثم PDF، ثم FastAPI والاختبارات، قبل أي تعديل على VPS.

---

## 2026-07-22 — تفعيل تصدير جميع المنتجات إلى PDF

### المتطلبات المعتمدة

- تصدير جميع منتجات التحليل دائمًا، دون التأثر ببحث المنتجات أو الصفوف المخفية في الواجهة.
- اقتصار اسم المنتج داخل PDF على أول 100 حرف مع إضافة علامة الحذف عند تجاوز الحد.
- عدم السماح بالتصدير قبل اكتمال ظهور جميع صور المنتجات؛ أي صورة مفقودة أو فاشلة تبقي زر التصدير معطلًا.
- تضمين اسم العميل ورقم الطلبية ورابط السلة ومعرّف المجموعة والسوق ووقت التحليل واسم ملف JSON.
- تضمين بطاقات الإحصائيات وجدول المنتجات بالترتيب: الحالة، الصورة، اسم المنتج، `SKU`، الخصائص/المقاس، السعر.

### التنفيذ

- أضيفت الوحدة `pdf_export.py` لتوليد تقرير A4 أفقي باستخدام `QPdfWriter` دون إضافة مكتبات خارجية جديدة.
- يعرض التقرير ترويسة جدول في كل صفحة، وألوان الحالات، ورقم الصفحة، وإجمالي الصفحات، وتاريخ إنشاء التقرير.
- أضيف اختيار مكان الحفظ باستخدام نافذة النظام، مع اقتراح اسم مطابق لاسم ملف JSON داخل `exports/`.
- أضيف شريط تقدم أثناء إنشاء صفحات التقرير ورسالة نجاح توضح مسار الملف الناتج.
- أضيف تتبع مركزي لصور المنتجات يمنع الطلبات المكررة للصورة نفسها ويحدّث جميع الصفوف المشتركة عند اكتمالها.
- أصبح زر `تصدير البيانات` يتفعّل فقط عندما تكون صور كل منتجات التحليل موجودة في الذاكرة، ويعرض سبب الانتظار أو الفشل في التلميح.
- لا يعدّل التصدير ملف JSON ولا بيانات السجل.

### التحقق الفعلي

- نجح `py_compile` للملفات `app.py` و`pdf_export.py` و`models.py`.
- أُنشئ تقرير تجريبي من 6 منتجات في صفحتين، ثم رُندر إلى PNG وفُحص بصريًا؛ ظهرت الصور والعربية والترويسة والتذييل دون قص.
- أُنشئ تقرير ثانٍ من 17 منتجًا عربيًا في 4 صفحات، وفُحصت الصفحة الأولى والأخيرة بصريًا؛ ظهرت جميع الصور والحالات والصفوف بصورة سليمة.
- ملفات التحقق المؤقتة موجودة تحت `tmp/pdfs/` وهي مستبعدة من Git.

### الخطوة التالية

- تجربة زر التصدير من الواجهة على تحليل حديث والتأكد من مسار الحفظ المختار.
- بعد اعتماد شكل PDF، يبدأ تحديد تنسيق وظيفة `نسخ البيانات` بصورة مستقلة.

---

## 2026-07-22 — مقترحات تحسين تصدير PDF قبل التنفيذ

### الملاحظات الجديدة

- استبدال رسالة نجاح الحفظ المانعة للتفاعل بإشعار مؤقت `Toast` مدته ثانيتان.
- دعم تصدير أي تحليل مفتوح من السجل، وليس التحليل الجديد فقط.
- منع إنشاء نسخ PDF مكررة للتحليل نفسه؛ يعتمد التقرير على اسم ملف JSON المقابل، وإذا كان موجودًا يُفتح بدل إنشائه مجددًا.
- إعادة تصميم معلومات التقرير وبطاقات أعداد المنتجات بتنسيق أوضح وأكثر احترافية.
- عكس ترتيب جدول PDF ليبدأ من اليمين بالحالة ثم الصورة ثم الاسم ثم `SKU` ثم الخصائص ثم السعر.
- تطبيق التفاف النص على اسم المنتج والخصائص، مع إبقاء حد اسم المنتج المعتمد عند أول 100 حرف وإظهار هذه الأحرف كاملة عبر عدة أسطر.
- ضمان عدم تقسيم صف المنتج بين صفحتين، وتكرار رأس الجدول في كل صفحة.

### التوصية التقنية قبل الاعتماد

- عدم استخدام صفحة PDF واحدة بطول غير محدود؛ لأنها تصبح بطيئة وصعبة القراءة والطباعة وقد تسبب مشاكل في بعض عارضات PDF عند وجود عشرات الصور.
- اعتماد `A3 Portrait` بوصفه الحل الأفضل للاتجاه العمودي: عرضه يعادل تقريبًا عرض `A4 Landscape` مع مساحة رأسية أكبر، ويحافظ على وضوح الأعمدة والصور.
- إعادة بناء رسم التقرير بقياسات ديناميكية للصفوف بدل الاعتماد على جدول HTML الحالي؛ يُحسب ارتفاع كل صف من الصورة والنص الملفوف، ويُنقل الصف كاملًا إلى الصفحة التالية إذا لم يتبق له مكان.
- عرض معلومات التحليل في بطاقة مرتبة، ثم أربع بطاقات إحصائية، ثم الجدول؛ وتستخدم الصفحات التالية ترويسة مختصرة مع رأس الجدول المتكرر.
- عند إعادة تسمية سجل JSON لاحقًا، يُنصح بإعادة تسمية ملف PDF المرتبط به تلقائيًا للحفاظ على التطابق ومنع الملفات اليتيمة.

### الحالة

- هذه المقترحات موثقة فقط ولم يُنفذ أي تعديل برمجي بعد، بانتظار اعتماد المستخدم لحجم الصفحة وسلوك فتح التقرير الموجود.

### قرار المستخدم بعد المراجعة

- اعتماد PDF متعدد الصفحات بحجم `A4 Portrait` بدل `A3 Portrait` متى أمكن، وعدم استخدام صفحة واحدة طويلة أو مفتوحة الحجم.
- اعتماد بقية المقترحات: إشعار مؤقت، دعم السجل، منع التكرار، فتح التقرير الموجود، مزامنة الاسم، التصميم الاحترافي، الاتجاه من اليمين إلى اليسار، التفاف النص، تكرار رأس الجدول، ومنع انقسام الصفوف.
- التنفيذ لم يبدأ بعد في هذه النقطة؛ بقيت توصيات الضبط النهائي قبل تعديل الكود.

### مقترحات إضافية قيد الاعتماد

- إبقاء قيمة `SKU` كاملة في سطر واحد داخل PDF دون التفاف أو اختصار.
- إنشاء PDF تلقائيًا بعد انتهاء التحليل واكتمال تحميل جميع الصور، مع استمرار منع الملفات المكررة.
- إعادة تصميم معلومات التحليل أعلى التقرير على شكل بطاقات `Cards` واضحة واحترافية.
- التوصية المبدئية: حفظ PDF التلقائي في `exports/` باسم مطابق لملف JSON دون فتحه تلقائيًا، وإظهار `Toast` بالنتيجة.
- إذا فشلت صورة، يبقى JSON محفوظًا ولا يفشل التحليل؛ يُعرض تنبيه مؤقت بأن PDF ينتظر اكتمال الصور، ويظل زر التصدير متاحًا لإعادة المحاولة لاحقًا.
- لم يبدأ التنفيذ بعد، وهذه التفاصيل تنتظر موافقة المستخدم النهائية.

### تفصيل مقترح تحميل الصور وبطاقات PDF

- بعد انتهاء استخراج JSON ينتقل شريط الحالة نفسه إلى مراحل: تحميل الصور، إعادة المحاولات، إنشاء صفحات PDF، ثم الاكتمال.
- تُعاد محاولة كل صورة فاشلة حتى 10 مرات، مع إعادة محاولة الصور الفاشلة فقط وبفاصل زمني متدرج، ودون تكرار تنزيل الصور الناجحة.
- لا يبدأ إنشاء PDF أثناء وجود صور ما زالت قيد التحميل أو إعادة المحاولة.
- إذا انتهت المحاولات العشر دون نجاح صورة معينة، يُنشأ التقرير مع خلية بديلة واضحة تحمل النص `الصورة غير متوفرة` بدل إسقاط المنتج أو إيقاف التقرير كاملًا.
- يُعرض عدد الصور الناجحة والفاشلة في حالة التقدم، ويُسجل عدد الصور البديلة في رسالة اكتمال التصدير.
- الصف الأول من بطاقات المعلومات المقترحة: اسم العميل، رقم الطلبية، معرّف المجموعة، السوق.
- الصف الثاني: وقت التحليل، ملف البيانات، رابط السلة، وملخص أعداد المنتجات.
- يُعرض رابط السلة بنص مختصر أو ملتف ضمن حد صغير مع الاحتفاظ بالرابط الكامل كوجهة للرابط القابل للنقر.
- يلتف اسم ملف البيانات داخل بطاقته حتى لا يوسع البطاقة أو يضغط بقية المحتوى.
- لم يبدأ التنفيذ بعد؛ هذه صياغة تفصيلية للمراجعة النهائية.

---

## 2026-07-22 — تنفيذ PDF العمودي والتصدير التلقائي

### تخطيط التقرير

- أعيد بناء `pdf_export.py` باستخدام الرسم المباشر عبر `QPainter` بدل جدول HTML السابق لضبط اتجاه RTL والقياسات بدقة.
- أصبح التقرير `A4 Portrait` بهوامش صغيرة، مع ترويسة جدول متكررة في كل صفحة ومنع تقسيم صف المنتج بين صفحتين.
- رُتبت الأعمدة فعليًا من اليمين: الحالة، الصورة، اسم المنتج، `SKU`، الخصائص/المقاس، السعر.
- يظهر `SKU` كاملًا في سطر واحد دون التفاف؛ يصغر حجم خطه تدريجيًا عند الحاجة بدل قص القيمة.
- يقتصر اسم المنتج على أول 100 حرف مع التفاف النص، بينما تظهر الخصائص كاملة مع التفاف وارتفاع صف ديناميكي.
- أضيفت بطاقات معلومات متجاورة في صفين: العميل والطلب والمجموعة والسوق، ثم الوقت والملف والرابط وأعداد المنتجات.
- يلتف اسم ملف JSON داخل بطاقته، ويظهر رابط السلة مختصرًا في سطرين تقريبًا للحفاظ على المساحة.
- أضيفت موازنة بسيطة للصفحات تمنع بقاء صفحة أخيرة تحتوي صفًا واحدًا أو صفين متى أمكن.

### الصور والتقدم

- أضيف حد `MAX_IMAGE_ATTEMPTS = 10` لكل رابط صورة، مع إعادة محاولة الصور الفاشلة فقط وفاصل زمني متدرج يصل إلى 3 ثوانٍ.
- يعرض شريط الحالة نفسه تقدم تحميل الصور ثم تقدم إنشاء صفحات PDF.
- لا يبدأ PDF أثناء وجود صور قيد التحميل أو إعادة المحاولة.
- بعد استنفاد المحاولات، لا يُحذف المنتج ولا يفشل التقرير؛ تظهر خلية بديلة واضحة باسم `الصورة غير متوفرة`.

### التصدير والسجل

- يبدأ التصدير تلقائيًا بعد نجاح التحليل وانتهاء جميع الصور أو استنفاد محاولاتها.
- يُحفظ التقرير في `exports/` باسم مطابق لاسم ملف JSON دون نافذة اختيار مسار.
- يمنع البرنامج إنشاء PDF مكرر؛ إذا كان الملف موجودًا يفتحه زر التصدير مباشرة.
- تعمل الوظيفة مع التحليلات القديمة المختارة من السجل، ويُعاد تسمية PDF المرتبط عند إعادة تسمية ملف السجل.
- استبدلت رسالة نجاح الحفظ بإشعار `Toast` مؤقت مدته الافتراضية ثانيتان.

### التحقق الفعلي

- نجح إنشاء تقرير من 17 منتجًا في صفحتين مع ترويسة متكررة وصفوف سليمة.
- نجح إنشاء تقرير من 35 منتجًا وصورة كاملة في 4 صفحات قبل موازنة الصفحة الأخيرة، ثم أضيفت الموازنة لتوزيع الصفوف بصورة أفضل.
- نجح اختبار صورة غير قابلة للتحميل بمحاولتين مخفضتين لأغراض الاختبار؛ انتهت المحاولات وأُنشئ PDF مع خلية `الصورة غير متوفرة` وعدّاد صحيح للصورة البديلة.
- نجح اختبار واجهة غير مرئي للتصدير التلقائي؛ تم إنشاء الملف داخل `exports/` بعد اكتمال الصور دون تدخل المستخدم.
- تمت معاينة الصفحة الأولى والأخيرة وصفحة الصورة البديلة بعد تحويل PDF إلى PNG، وظهرت العربية والصور والبطاقات والجدول دون قص أو انقسام صفوف.

### الخطوة التالية

- تجربة تحليل جديد من الواجهة للتأكد من تجربة المستخدم الكاملة للإشعار وشريط التقدم.
- بعد اعتماد التصدير النهائي، الانتقال إلى تحديد تنسيق زر `نسخ البيانات`.

---

## 2026-07-22 — مقترحات إعادة ترتيب وألوان بطاقات PDF

### الترتيب المطلوب

- الصف الأول: اسم العميل، رقم الطلبية، معرّف المجموعة، السوق، وقت التحليل.
- الصف الثاني: ملف البيانات، رابط السلة، أعداد المنتجات.
- التوصية للصف الثاني: منح رابط السلة أكبر عرض، ثم ملف البيانات، ثم بطاقة الأعداد.

### الألوان المقترحة

- توحيد جميع عناوين البطاقات بحجم واحد وخط عريض ولون رمادي مزرق واضح.
- استخدام ألوان دلالية لقيم البطاقات دون تغيير خلفيتها الفاتحة: العميل نيلي، الطلب برتقالي، المجموعة أزرق، السوق تركوازي، والوقت بنفسجي هادئ.
- عرض بطاقة أعداد المنتجات بأربع قيم ملونة داخل البطاقة: الإجمالي أزرق، المتاح أخضر، النافد كهرماني، وغير المتوفر أحمر.
- إبقاء اسم المنتج بلون داكن، وجعل `SKU` أزرق داكن، والسعر أخضر داكن، والخصائص بلون رمادي مزرق لتوضيح وظيفة كل عمود دون مبالغة لونية.

### الرابط والملف

- إلغاء اختصار رابط السلة داخل PDF وعرضه كاملًا.
- جعل نص الرابط قابلًا للكسر عند `/` و`?` و`&` و`=` والشرطة والشرطة السفلية حتى يلتف دون فقد أي حرف.
- تصغير خط قيمة الرابط وحدها تلقائيًا بحسب طوله، مع إبقاء عنوان البطاقة بنفس حجم بقية العناوين.
- زيادة ارتفاع الصف الثاني ديناميكيًا عند الحاجة، والسماح لاسم ملف البيانات بالالتفاف الكامل.

### الحالة

- لم يُنفذ أي تعديل في هذه الخطوة؛ المقترحات موثقة بانتظار الاعتماد.

---

## 2026-07-22 — تحسين بطاقات PDF والرابط التفاعلي

### التعديلات المنفذة

- أعيد ترتيب الصف الأول من بطاقات التقرير ليحتوي من اليمين على: اسم العميل، رقم الطلبية، معرّف المجموعة، السوق، ووقت التحليل.
- أصبح الصف الثاني يحتوي من اليمين على: ملف البيانات، رابط السلة، وأعداد المنتجات، مع تخصيص أكبر مساحة لرابط السلة.
- وُحّدت عناوين البطاقات بحجم ثابت وخط عريض ولون رمادي مزرق، مع ألوان دلالية منفصلة لقيم العميل والطلب والمجموعة والسوق والوقت.
- أعيد تصميم بطاقة أعداد المنتجات على صفين: `الإجمالي - المتاح` ثم `النافد - غير المتوفر`، ويظهر رقم كل حالة بجوار اسمها بلونها الدلالي.
- أصبح رابط السلة ظاهرًا كاملًا دون اختصار، مع التفاف ذكي وتصغير تلقائي لخط قيمة الرابط فقط عند الحاجة.
- أضيفت إلى الصفحة الأولى منطقة رابط PDF فعلية تغطي بطاقة رابط السلة؛ يمكن النقر عليها لفتح رابط السلة الأصلي الكامل.
- أصبح اسم المنتج في الجدول بالأسود الداكن، و`SKU` بالأزرق الداكن، والسعر بالأخضر الداكن، والخصائص بالرمادي المزرق، مع إبقاء الحالة بلونها الدلالي.
- أضيفت مكتبة `pypdf` إلى `requirements.txt` لإضافة رابط URL التفاعلي بعد اكتمال رسم التقرير بواسطة `QPainter`.

### التحقق الفعلي

- نجح فحص `py_compile` لملفات `pdf_export.py` و`app.py` و`models.py`.
- أُنشئ تقرير تجريبي من سجل حقيقي مع 8 منتجات و8 صور ناجحة دون صور بديلة.
- أُعيد التحقق على السجل الكامل المكوّن من 35 منتجًا و35 صورة؛ أُنشئ التقرير في 4 صفحات مع ترويسة متكررة وصفحة أخيرة متوازنة تضم 3 صفوف كاملة.
- تحقق فحص `pypdf` من وجود رابط تفاعلي واحد في الصفحة الأولى، وأن وجهته تطابق رابط السلة الأصلي كاملًا.
- حُولت الصفحة الأولى إلى PNG باستخدام Poppler، وأكدت المراجعة البصرية سلامة ترتيب البطاقات ووضوح الألوان واكتمال الرابط وعدم قص قيم `SKU` أو صفوف الجدول.

---

## 2026-07-22 — إعادة بناء المشروع بأسلوب Clean Architecture

### الهدف

- إزالة تكدس المسؤوليات من ملفات الجذر والواجهة مع الحفاظ الكامل على السلوك الحالي.
- جعل قواعد المجال وحالات الاستخدام مستقلة عن PySide6 وPlaywright ونظام الملفات.
- تسهيل إضافة وظائف جديدة واختبارها واستبدال التقنيات الخارجية دون تعديل منطق العمل.

### البنية الجديدة

- نُقلت كيانات المنتجات والسلة والأسعار والحالات إلى `src/shein_extractor/domain/`.
- أضيفت طبقة `application/` وتضم حالات الاستخدام `AnalyzeCart` و`LoadAnalysis` و`ExportReport`، ومنافذ الاستخراج والتخزين والتقارير.
- فُصل استخراج SHEIN بواسطة Playwright، وتطبيع الاستجابة، والتخزين بصيغة JSON، وإنشاء PDF، وتشخيص الروابط داخل `infrastructure/`.
- نُقلت واجهة PySide6 إلى `presentation/qt/`، وفُصلت عناصر السجل والبطاقات والحوارات والعامل الخلفي وتحميل الصور ودورة تصدير PDF إلى وحدات مستقلة.
- أصبح `bootstrap.py` نقطة التركيب الوحيدة التي تنشئ التطبيقات الفعلية للمنافذ وتحقن حالات الاستخدام في الواجهة.
- أضيفت طبقة `cli/` للأوامر، مع إبقاء ملفات الجذر القديمة كواجهات توافق رفيعة حتى تستمر أوامر التشغيل السابقة دون تغيير.
- أضيف `pyproject.toml` لتثبيت المشروع كحزمة بنمط `src layout` وتوفير أوامر تشغيل مباشرة.
- أضيف `ARCHITECTURE.md` لشرح الطبقات واتجاه الاعتماديات وطريقة إضافة الوظائف مستقبلًا.

### تحسين تنظيم الواجهة

- أزيلت تعريفات قديمة مكررة من `MainWindow` كانت تشمل العامل والسجل والبطاقات والبحث.
- نُقل تحميل الصور وإعادة المحاولة إلى `image_loading.py`.
- نُقلت دورة تصدير PDF وحالة تقدمها إلى `pdf_reporting.py`.
- لم تعد الواجهة تستدعي مولد PDF مباشرة؛ تستدعي `ExportReport` المحقون من نقطة التركيب.
- نُقل التحقق من روابط SHEIN إلى طبقة التطبيق لأنه قاعدة إدخال مستقلة عن المتصفح.

### التوافق والتشغيل

- يستمر الأمر `python app.py` في تشغيل البرنامج كما كان.
- يستمر `python extract_cart.py <url>` و`python diagnose_link.py <url>` دون تغيير.
- يمكن بعد التثبيت استخدام `shein-extractor` و`shein-extract-cart` و`shein-diagnose-link`.
- ثُبتت الحزمة محليًا داخل `.venv` باستخدام `pip install -e . --no-deps`.

### التحقق الفعلي

- نجح `compileall` لجميع ملفات الحزمة ونقاط التشغيل المتوافقة.
- نجح فتح النافذة الرئيسية باستخدام منصة Qt غير المرئية وتحميل 38 سجلًا محفوظًا.
- نجح تحليل نموذج فاتورة واستخراج اسم العميل ورقم الطلبية ورابط السلة.
- نجح تحميل سجل JSON حقيقي والبحث في منتج بواسطة SKU أو الخصائص.
- نجحت شاشة المساعدة لكل من `extract_cart.py` و`diagnose_link.py`.
- تحقق فحص اتجاه الاعتماديات من عدم اعتماد `domain` أو `application` على `infrastructure` أو `presentation`.

---

## 2026-07-22 — إصلاح استثناء تصدير PDF عند إغلاق البرنامج

### سبب الخطأ

- كان `QtPdfReportExporter` يحاول قراءة `result.path` من `PdfExportResult`، بينما النتيجة الأصلية تحتوي فقط على `page_count` و`unavailable_image_count`.
- ظهر الخطأ أثناء تنفيذ تصدير PDF تلقائي مؤجل بالتزامن مع إغلاق النافذة.

### الإصلاح

- أصبح محول PDF يعيد `output_path` المعروف بوصفه مسار التقرير بدل قراءة حقل غير موجود.
- أضيفت حالة `is_closing` إلى النافذة الرئيسية.
- عند قبول الإغلاق تُلغى علامة التصدير التلقائي، وتتوقف `_auto_export_pdf` و`_create_pdf` عن بدء أي تقرير جديد.

### التحقق الفعلي

- نجح إنشاء PDF حقيقي من سجل محفوظ عبر `ExportReport` و`QtPdfReportExporter`؛ نتج تقرير من 5 صفحات بحجم 74682 بايت دون استثناء.
- نجح اختبار إغلاق النافذة مع وجود تصدير تلقائي مؤجل، ولم يُستدعَ المصدّر بعد بدء الإغلاق.
- نجح `compileall` بعد الإصلاح.

---

## 2026-07-22 — التدقيق الشامل للنسخة المحلية قبل دراسة الاستضافة

### نطاق هذه المرحلة

- اقتصر العمل على تثبيت وظائف المشروع المحلي الحالية والتحقق منها.
- لم تُضف FastAPI أو Docker أو إعدادات Hostinger أو تكامل n8n وTelegram في هذه المرحلة بناءً على قرار المستخدم.

### المشكلات المكتشفة والمصححة

- صُحح annotation قديم داخل `MainWindow` كان يشير إلى `ExtractionWorker` غير معرف بدل `CleanExtractionWorker`.
- أصبح `PlaywrightCartGateway` يغلق `BrowserContext` وChromium داخل `finally` عند النجاح أو الفشل.
- أضيف تحويل واضح لأخطاء مهلة Playwright وأخطاء تشغيل المتصفح إلى `CartExtractionError` مفهوم.
- توسعت معالجة استجابة SHEIN لتشمل أخطاء `UnicodeDecodeError` و`ValueError` عند محاولة قراءة JSON غير صالح.
- ثُبتت إصدارات المكتبات التي تم التحقق بها داخل `pyproject.toml` و`requirements.txt` لضمان قابلية إعادة بناء البيئة نفسها.
- أضيف إعداد ثابت لمجلد pytest داخل المشروع لتجنب مشكلة صلاحيات مجلد Windows المؤقت.
- أضيف تجاهل مخلفات pytest و`egg-info` المحلية إلى `.gitignore`.

### الاختبارات المضافة

- تحليل نماذج الفواتير العربية واستخراج اسم العميل ورقم الطلبية والرابط.
- التعامل مع الرابط وحده واكتشاف وجود أكثر من رابط في الإدخال.
- رفض الروابط التي لا تنتمي إلى SHEIN.
- استخراج مجموعات `normalProducts` و`outStock` و`unavailable` دون إسقاط أي منتج.
- التحقق من عقد تبديل `goods_sn` و`sku_code` وأن SKU المطلوب يبدأ بحرف `s`.
- تنسيق حالة المنتج النافد وتثبيت `sold_out=True` و`stock=0`.
- تنسيق حالة استخدام `AnalyzeCart` مع بوابة وهمية ومستودع JSON حقيقي مؤقت.
- إغلاق Chromium وBrowserContext في حالتي النجاح والمهلة.
- إنشاء PDF فعلي والتحقق من ترويسة `%PDF` وعدد الصفحات والصور البديلة.
- فتح وإغلاق النافذة ومنع التصدير التلقائي بعد بدء الإغلاق.

### نتائج التحقق الفعلية

- نجح `ruff check .` دون أي أخطاء.
- نجح `pytest -q`: عدد الاختبارات `10 passed`.
- نجح `py_compile` لنقاط التشغيل القديمة و`compileall` لجميع ملفات `src`.
- نجح `pip check` دون اعتماديات مكسورة.
- نجح استيراد 20 وحدة رئيسية من جميع الطبقات.
- نجح فحص اتجاه الاعتماديات؛ لا تعتمد `domain` أو `application` على الطبقات الخارجية.
- نجح فتح الواجهة غير المرئية وتحميل سجل فعلي وتنزيل `17/17` صورة دون فشل ثم الإغلاق الطبيعي.
- نجح استخراج حي من رابط SHEIN محفوظ حديثًا خلال 15.6 ثانية: استخرج `17` منتجًا، وتطابقت أعداد المجموعات مع عدد المنتجات، وبدأت جميع قيم SKU المتوفرة بحرف `s`.
- نجح تنزيل `17/17` صورة حقيقية وإنشاء PDF كامل من صفحتين بحجم `170206` بايت، دون صور بديلة، مع وجود رابط PDF تفاعلي.
- نجحت نقاط التشغيل `shein-extract-cart` و`shein-diagnose-link` بعد إعادة تثبيت الحزمة بنمط editable.

### الحالة الحالية

- النسخة المحلية جاهزة للتجربة والاستخدام بوظائفها الحالية.
- المرحلة التالية منفصلة: دراسة Hostinger ثم تصميم خدمة HTTP Headless متكاملة مع n8n وTelegram، دون رفع تطبيق Qt كما هو.
## 2026-07-22 — بدء دراسة النشر على Hostinger والتكامل مع n8n

- تم اعتماد جاهزية النسخة المحلية مبدئيًا بعد نجاح `10` اختبارات ونجاح `ruff check .` وتشغيل الواجهة يدويًا.
- لم يبدأ تنفيذ طبقة الخادم بعد؛ المرحلة الحالية مخصصة لتثبيت المتطلبات والقرارات التشغيلية قبل تعديل الكود.
- التصور الموصى به: تشغيل `n8n` وخدمة Python مستقلة باسم `shein-api` داخل Docker على Hostinger VPS، وربطهما بشبكة Docker داخلية دون كشف منفذ خدمة التحليل للعامة.
- تتولى `n8n` استقبال رسالة Telegram والتحقق من المجموعة ومنع التكرار وإرسال ملف PDF أو رسالة الخطأ، بينما تتولى `shein-api` تحليل الفاتورة واستخراج سلة SHEIN وإنشاء PDF فقط.
- تبدأ النسخة الأولى بطلب واحد متزامن، مع مهلة كلية واضحة وتنظيف تلقائي للملفات المؤقتة ومراقبة استهلاك Chromium للذاكرة.
- القرارات المطلوب تثبيتها قبل التنفيذ: خطة VPS الفعلية، وضع n8n الحالي، مصدر الرسائل ومجموعة الرد، سياسة حفظ النتائج، حجم الاستخدام المتوقع، وطريقة التعامل مع الطلبات المتزامنة.

### القرارات المثبتة بعد مراجعة Hostinger

- الخادم الحالي Hostinger `KVM 2`: نواتان CPU، وذاكرة 8 GB، ومساحة 100 GB، ونظام Ubuntu 24.04 مع قالب n8n.
- قالب n8n في Hostinger يعمل داخل Docker، مع ضرورة التحقق الفعلي من الحاويات والشبكات قبل أي تعديل.
- وصول SSH متاح بحساب `root`، كما يتوفر Browser Terminal من hPanel.
- استقبال رسائل Telegram وإرسال ملفات PDF سيكونان في المجموعة نفسها، ولا يشترط الرد المباشر على الرسالة الأصلية.
- كل رسالة Telegram جديدة تمثل تحليلًا جديدًا مستقلًا، حتى لو كان محتواها مطابقًا لرسالة سابقة.
- تعالج الطلبات بطابور `FIFO` حسب وقت الوصول، وتبدأ النسخة الأولى بعامل تحليل واحد لتجنب ضغط عدة عمليات Chromium على الخادم.
- عند إعادة محاولة n8n للرسالة نفسها تقنيًا يستخدم `chat_id + message_id` لإرجاع المهمة نفسها ومنع التكرار غير المقصود، بينما الرسالة الجديدة تحصل على مهمة جديدة.
- يحتفظ بملفات JSON وPDF دائمًا إلى أن يحذفها المستخدم يدويًا، مع حذف الصور المؤقتة ومراقبة مساحة القرص وإظهار تحذيرات قبل الامتلاء.
- لا تُحذف بيانات العملاء أو روابط السلال تلقائيًا في النسخة الأولى.
- لم يُنشأ Telegram Bot بعد، وسيتم إنشاؤه وإضافة `chat_id` في مرحلة إعداد Workflow بعد اكتمال خدمة الخادم.
- المعمارية المستهدفة: `n8n -> shein-api -> Redis FIFO -> shein-worker -> JSON/PDF`، مع شبكة Docker داخلية وعدم كشف `shein-api` للعامة.
- المرحلة الجارية الآن هي جرد الخادم والتحقق من Docker وn8n بأوامر قراءة فقط؛ لا يبدأ تعديل الخادم قبل اكتمالها وأخذ Snapshot حديث.

### نتيجة المرحلة الأولى: جرد الخادم

- اكتملت المرحلة الأولى بنجاح في 2026-07-22 دون إجراء أي تعديل على الخادم.
- النظام: Ubuntu 24.04.3 LTS، والنواة `6.8.0-134-generic`، والوصول الحالي بحساب `root`.
- الموارد: نواتان CPU، وذاكرة 7.8 GiB منها 6.8 GiB متاحة، ومساحة 96 GB منها 91 GB متاحة.
- لا توجد Swap حاليًا؛ تتم مراجعة الحاجة إلى إضافة Swap قبل تشغيل Chromium في الإنتاج.
- Docker `29.0.2` وDocker Compose `2.40.3` مثبتان، وخدمة Docker في حالتي `active` و`enabled`.
- الحاويتان `root-n8n-1` و`root-traefik-1` تعملان منذ أسبوعين.
- n8n غير مكشوف مباشرة للعامة؛ المنفذ `5678` مربوط بـ `127.0.0.1`، وTraefik وحده ينشر المنفذين 80 و443.
- ملف Compose الفعلي هو `/root/docker-compose.yml`، والمشروع اسمه `root`، والشبكة المشتركة هي `root_default`.
- الخادم يحتاج إلى إعادة تشغيل بسبب تحديثات النظام، لكن لا تتم إعادة التشغيل قبل إنشاء Snapshot وحفظ نسخة n8n والتحقق من بيانات الاستعادة.
- المرحلة التالية: تحديد مواقع بيانات n8n ومفتاح التشفير دون إظهارهما، ثم إنشاء Snapshot ونسخة احتياطية قابلة للاستعادة قبل أي تحديث أو إعادة تشغيل.

### Snapshot ما قبل النشر

- أُنشئ Snapshot يدوي من hPanel بتاريخ `2026-07-22 09:58` بنجاح.
- واجهة Hostinger الحالية لا تطلب اسمًا للـSnapshot، ولذلك أُنشئ بالمعرّف والتاريخ تلقائيًا.
- تاريخ الانتهاء الظاهر في hPanel هو `2026-07-23`، لذا تعد هذه حماية قصيرة المدى ويجب إكمال النسخة القابلة للتنزيل قبل انتهائها.
- لم يتم الضغط على `Restore` ولم تتم إعادة تشغيل الخادم.

### نتيجة فحص بيانات n8n قبل النسخ الاحتياطي

- تم التحقق فعليًا من أن إصدار n8n الحالي هو `2.10.3`.
- بيانات n8n الدائمة محفوظة داخل Docker volume باسم `n8n_data`، ومصدره على الخادم `/var/lib/docker/volumes/n8n_data/_data` ووجهته داخل الحاوية `/home/node/.n8n`.
- يوجد bind mount إضافي من `/local-files` إلى `/files` داخل حاوية n8n.
- ملفات التشغيل الموجودة هي `/root/docker-compose.yml` و`/root/.env`، ولم تُعرض محتويات أي منهما حفاظًا على الأسرار.
- ملف إعداد n8n موجود في `/home/node/.n8n/config`، ولم تُعرض محتوياته أو مفتاح التشفير.
- قاعدة البيانات الحالية هي SQLite الافتراضية، وملفها `/home/node/.n8n/database.sqlite` مع ملفات WAL/SHM المرتبطة بها.
- لم يظهر المتغير `N8N_ENCRYPTION_KEY` ضمن أسماء متغيرات بيئة الحاوية؛ لذلك يعتمد التثبيت الحالي غالبًا على المفتاح المحفوظ في ملف إعداد n8n، ويجب حفظ مجلد `n8n_data` كاملًا دون تعديل هذا الملف.
- لم تُكشف أي قيم سرية أثناء الفحص؛ المخرجات اقتصرت على الإصدارات والأسماء والمسارات.
- الخطوة التالية المعتمدة: إنشاء نسخة احتياطية متناسقة من بيانات n8n وملفات Compose و`.env` دون إيقاف Traefik، ثم التحقق من الأرشيف قبل تنزيله محليًا.

### إنشاء نسخة n8n الاحتياطية القابلة للتنزيل

- أُوقفت حاوية `root-n8n-1` مؤقتًا لضمان اتساق قاعدة SQLite أثناء النسخ، بينما لم تُوقف حاوية Traefik.
- نُسخت بيانات Docker volume `n8n_data` كاملة، بما يشمل قاعدة البيانات وملف إعداد التشفير، دون عرض محتوياتها.
- أُضيف الملفان `/root/docker-compose.yml` و`/root/.env` ومعلومات صور الحاويات إلى حزمة النسخ الاحتياطي.
- أُنشئ الأرشيف `/root/backups/n8n-backup-20260722-070454.tar.gz` بحجم ظاهر `45M` وصلاحيات `600`.
- قيمة SHA-256 المسجلة للأرشيف هي `21d1e5a77fcb267a4e264fd39bd8fbea1e2d2637bc8ee73f1e32241fa9e76413`.
- عادت حاوية `root-n8n-1` إلى حالة التشغيل بنجاح بعد النسخ.
- ظهور محارف `^[[200~` و`~` في بداية ونهاية بعض الأوامر ناتج عن وضع اللصق في Browser Terminal؛ لم يمنع تنفيذ خطوات النسخ الفعلية، لكنه منع تفعيل `set -e` في بداية الكتلة، لذلك يلزم التحقق الصريح من سلامة الأرشيف قبل تنزيله.
- الخطوة التالية: اختبار سلامة gzip والأرشيف الداخلي والتأكد من وجود `config` و`database.sqlite` ثم فحص استجابة n8n.

### التحقق من سلامة نسخة n8n الاحتياطية

- نجح اختبار gzip للأرشيف الخارجي وظهرت النتيجة `Outer archive: OK`.
- تأكد وجود الملفات المطلوبة داخل الحزمة: `docker-compose.yml` و`.env` و`n8n-data.tar.gz` و`n8n-image.txt` و`traefik-image.txt`.
- نجح اختبار الأرشيف الداخلي لبيانات n8n وظهرت النتيجة `Inner n8n archive: OK`.
- تأكد وجود الملفين الحرجين `./database.sqlite` و`./config` داخل نسخة بيانات n8n.
- تأكد استمرار تشغيل الحاوية `root-n8n-1`، وأعاد المسار المحلي `/healthz` الاستجابة `{"status":"ok"}`.
- أصبحت النسخة `/root/backups/n8n-backup-20260722-070454.tar.gz` مؤهلة للتنزيل والحفظ خارج الخادم.
- الخطوة التالية: تنزيل الأرشيف إلى جهاز Windows عبر SSH/SCP، ثم مقارنة بصمة SHA-256 المحلية بالبصمة المسجلة على الخادم.

### محاولة تنزيل نسخة n8n عبر SCP

- جُرّب تنزيل الأرشيف من جهاز Windows باستخدام `scp` إلى مجلد Downloads.
- انتهت المحاولة برسالة `Connection timed out` عند الاتصال بالخادم `72.62.22.22` على المنفذ `22` قبل الوصول إلى مرحلة المصادقة.
- لا تتعلق النتيجة بصلاحيات PowerShell ولا تستلزم تشغيله كمسؤول؛ الأداة `scp.exe` بدأت بالفعل بصورة صحيحة.
- يدل التوقف قبل طلب كلمة المرور على أن منفذ SSH غير قابل للوصول خارجيًا حاليًا، أو أن خدمة SSH تستمع إلى منفذ آخر، أو أن جدارًا ناريًا يمنع الاتصال.
- لم تتأثر النسخة الموجودة على الخادم ولم تتوقف خدمة n8n.
- الخطوة الحالية: فحص حالة خدمة SSH والمنفذ الفعلي وجدار UFW من Browser Terminal بأوامر قراءة فقط قبل إجراء أي تعديل.

### نتيجة تشخيص SSH داخل الخادم

- خدمة `ssh` في حالة `active` وتعمل حاليًا.
- إعداد sshd الفعلي يستخدم المنفذ `22` ويستمع على جميع عناوين IPv4 وIPv6 عبر `0.0.0.0:22` و`[::]:22`.
- أكد أمر `ss` وجود مستمع فعلي لـ`sshd` على المنفذ `22`.
- جدار UFW داخل Ubuntu في حالة `inactive`، ولذلك لا يمنع الاتصال الخارجي.
- ظهور `systemctl is-enabled ssh` بقيمة `disabled` لا يعني توقف الخدمة الحالية؛ الخدمة نشطة والمنفذ مستمع، ويظهر أن systemd مشارك في socket activation. ستتم مراجعة الاستمرارية بعد حل الوصول الخارجي وقبل إعادة تشغيل الخادم.
- بناءً على ذلك، مهلة SCP ليست ناتجة عن إعداد sshd أو UFW داخل الخادم؛ الاحتمال الحالي هو جدار Hostinger على مستوى الشبكة أو حجب المنفذ `22` من شبكة المستخدم.
- الخطوة التالية: فحص إعدادات Hostinger `Security -> Firewall` من hPanel دون إضافة أو حذف قاعدة قبل معرفة حالته الحالية.

### نتيجة فحص Hostinger VPS Firewall

- أظهرت صفحة `Security -> Firewall` في hPanel أنه لا توجد مجموعة Firewall منشأة أو مرتبطة بالخادم حاليًا؛ تظهر الواجهة خيار `+ Firewall` للإنشاء لأول مرة.
- بناءً على ذلك، لا يوجد Hostinger cloud firewall حالي يمنع المنفذ `22`، ولم يتم الضغط على زر الإنشاء أو تغيير أي قاعدة.
- بعد استبعاد sshd وUFW وHostinger Firewall، أصبح الاحتمال الأقوى هو حجب المنفذ `22` من شبكة المستخدم أو مزود الإنترنت، مع بقاء احتمال قواعد نظام أخرى أقل شيوعًا للمراجعة عند الحاجة.
- الخطوة التالية: اختبار الوصول إلى المنفذ `22` من شبكة مختلفة، ويفضل نقطة اتصال الهاتف، قبل تعديل إعداد SSH أو إضافة منفذ بديل.

### نتيجة اختبار SSH من شبكتين مختلفتين

- فشل `Test-NetConnection 72.62.22.22 -Port 22` عبر شبكة المستخدم الأساسية مع `TcpTestSucceeded: False`، رغم نجاح Ping.
- نجح الاختبار نفسه بعد ربط جهاز Windows بنقطة اتصال الهاتف، وظهرت النتيجة `TcpTestSucceeded: True`.
- ثبت بذلك أن الخادم وSSH والمنفذ `22` يعملون بصورة صحيحة، وأن شبكة المستخدم الأساسية أو مزودها يحجب اتصال TCP الصادر إلى المنفذ `22`.
- لا حاجة إلى تشغيل PowerShell كمسؤول، ولا إلى تعديل sshd أو UFW أو إنشاء Hostinger Firewall لمعالجة تنزيل النسخة الحالية.
- القرار الحالي: استخدام إنترنت الهاتف لتنزيل النسخة عبر SCP والتحقق من SHA-256 محليًا. ويمكن لاحقًا دراسة منفذ SSH بديل أو VPN/SSH key للوصول الإداري المستمر دون الاعتماد على شبكة الهاتف.

### وصول SCP إلى مرحلة التحقق من هوية الخادم

- بعد استخدام إنترنت الهاتف، وصل أمر SCP بنجاح إلى خادم SSH وعرض مطالبة التحقق من مفتاح المضيف لأول اتصال.
- البصمة التي عرضها عميل Windows للمفتاح من نوع ED25519 هي `SHA256:2tshFgyA8m+Jx5OW+Wb4iEkRwPonmXreTeMajoJQ/S4`.
- أُغلقت المحاولة الأولى دون قبول المفتاح، وبقيت المحاولة التالية عند مطالبة `yes/no/[fingerprint]` بسبب مشكلة إدخال/تركيز في الطرفية.
- لم تُدخل كلمة مرور ولم يبدأ نقل الملف بعد.
- القرار الأمني: لا يتم تجاوز فحص المضيف مباشرة؛ تُقارن البصمة المعروضة مع المفتاح الفعلي من Browser Terminal أولًا، ثم يستخدم `StrictHostKeyChecking=accept-new` لتسجيل المفتاح الجديد تلقائيًا إذا تطابقت البصمتان.

### التحقق من بصمة مفتاح SSH

- نُفذ `ssh-keygen -lf /etc/ssh/ssh_host_ed25519_key.pub` من Browser Terminal على الخادم.
- أعاد الخادم البصمة `SHA256:2tshFgyA8m+Jx5OW+Wb4iEkRwPonmXreTeMajoJQ/S4` للمفتاح ED25519 بحجم 256 بت.
- تطابقت بصمة الخادم حرفيًا مع البصمة التي عرضها عميل SCP على Windows، ولذلك أصبح قبول المفتاح الجديد آمنًا لهذه المحاولة.
- الخطوة التالية: استخدام `StrictHostKeyChecking=accept-new` لتسجيل المفتاح في `known_hosts` تلقائيًا ثم إدخال كلمة مرور root وبدء التنزيل.

### محاولة مصادقة SCP

- نجح تسجيل مفتاح الخادم في ملف `known_hosts` على Windows بعد التحقق المسبق من البصمة.
- وصل SCP إلى مطالبة كلمة مرور المستخدم `root`، ما يؤكد نجاح الشبكة وSSH والتحقق من هوية الخادم.
- أُدخلت الكلمة الحرفية `root` باعتبارها كلمة المرور، فظهرت `Permission denied`؛ `root` هو اسم الحساب وليس كلمة مروره.
- لم يبدأ تنزيل الملف ولم تتأثر النسخة الاحتياطية أو خدمات الخادم.
- تم التنبيه إلى إلغاء المطالبة وعدم تكرار التخمين، ثم تعيين كلمة مرور root قوية من hPanel وحفظها في مدير كلمات مرور دون مشاركتها في المحادثة.

### تحديث بيانات دخول root

- غُيّرت كلمة مرور حساب `root` بنجاح من لوحة Hostinger hPanel.
- لم تُسجل كلمة المرور أو تُعرض في المحادثة أو ملفات المشروع.
- الخطوة التالية: إعادة أمر SCP عبر إنترنت الهاتف باستخدام كلمة المرور الجديدة وتنزيل النسخة إلى مجلد Downloads.

### تنزيل نسخة n8n والتحقق المحلي النهائي

- بعد عدة محاولات مصادقة غير ناجحة، نُفذ SCP بكلمة مرور root الجديدة ونجح تنزيل الأرشيف حتى `100%` عبر إنترنت الهاتف.
- حُفظ الملف محليًا في `C:\Users\abd73\Downloads\n8n-backup-20260722-070454.tar.gz`.
- الحجم المحلي الفعلي للملف هو `46629264` بايت، ووقت الكتابة المحلي الظاهر `2026-07-22 10:35:48`.
- حُسبت بصمة SHA-256 محليًا وكانت `21D1E5A77FCB267A4E264FD39BD8FBEA1E2D2637BC8EE73F1E32241FA9E76413`.
- تطابقت البصمة المحلية حرفيًا مع بصمة الأرشيف على الخادم، ولذلك ثبت أن التنزيل كامل وغير تالف.
- أصبحت حماية n8n قبل النشر مكتملة بثلاث طبقات: Snapshot مؤقت في Hostinger، نسخة أسبوعية لدى Hostinger، وأرشيف مستقل متحقق منه على جهاز المستخدم.
- يمكن العودة إلى شبكة الإنترنت الأساسية بعد انتهاء SCP؛ لكن الوصول الخارجي عبر SSH على المنفذ `22` سيظل يحتاج إلى إنترنت الهاتف أو حل بديل لاحقًا.
- بعد اكتمال التنزيل لا توجد عملية مرتبطة بنافذة PowerShell المحلية، ويمكن إغلاقها بأمان.
- قبل تثبيت تحديثات Ubuntu أو إعادة تشغيل VPS، تقرر التحقق من حالة `ssh.socket` وآلية التشغيل التلقائي لـSSH؛ لن تتم إعادة التشغيل قبل ضمان إمكانية استعادة الوصول والتحقق لاحقًا من n8n وTraefik.

### التحقق من تشغيل SSH بعد إعادة الإقلاع

- `ssh.socket` في حالة `active` و`enabled` ويستمع على `0.0.0.0:22` و`[::]:22`.
- وحدة `ssh.socket` محملة من `/usr/lib/systemd/system/ssh.socket` وإعدادها الافتراضي `preset: enabled`.
- `ssh.service` في حالة `active` رغم أن الوحدة نفسها `disabled`، لأن تشغيلها يتم عند الطلب بواسطة `ssh.socket`.
- النتيجة: SSH مهيأ للعودة تلقائيًا بعد إعادة تشغيل VPS ولا توجد حاجة لتفعيل `ssh.service` بصورة منفصلة.
- ما زالت Ubuntu تعرض `System restart required`، لكن إعادة التشغيل مؤجلة حتى التحقق من سياسات إعادة تشغيل حاويتي n8n وTraefik تلقائيًا.

### التحقق من التشغيل التلقائي لـDocker والحاويات

- خدمة Docker في حالتي `active` و`enabled`.
- سياسة إعادة تشغيل الحاوية `root-n8n-1` هي `always`.
- سياسة إعادة تشغيل الحاوية `root-traefik-1` هي `always`.
- الحاويتان تعملان حاليًا؛ Traefik منذ أسبوعين وn8n منذ إعادة تشغيله بعد النسخ الاحتياطي.
- أصبحت مكونات الوصول والخدمات الأساسية مهيأة للعودة تلقائيًا بعد إعادة إقلاع VPS: `ssh.socket` وDocker وn8n وTraefik.
- قبل تثبيت التحديثات وإعادة التشغيل، تقرر فحص قائمة الحزم القابلة للترقية وسجل فشل `unattended-upgrades` والوحدات الفاشلة لتجنب تحديث أعمى.

### نتيجة فحص تحديثات Ubuntu قبل الترقية

- لا توجد وحدات systemd فاشلة؛ أعاد `systemctl --failed` عددًا قدره `0`.
- إعادة التشغيل مطلوبة لتفعيل `linux-image-6.8.0-136-generic` وتحديث `linux-base`، بينما النواة الحالية في الجرد السابق كانت `6.8.0-134-generic`.
- قائمة الترقيات المتاحة تشمل مكونات Ubuntu الأساسية والشبكة وAppArmor وcloud-init، وتشمل كذلك Docker وcontainerd وDocker Compose.
- سجل `unattended-upgrades` لا يعرض فشل تثبيت فعلي في آخر تشغيل؛ بل سجل `All upgrades installed` بتاريخ 2026-07-22، مع إبقاء `cloud-init` مؤجلًا/held back.
- الاستنتاج: رسالة وجود تحديث لم يُثبت تلقائيًا ترتبط على الأرجح بالحزمة المؤجلة `cloud-init` وليست عطلًا في خدمة قائمة.
- نظرًا إلى وجود تحديثات Docker ضمن القائمة، لن تنفذ ترقية مباشرة. الخطوة التالية هي تحديث فهارس APT ثم تشغيل محاكاة `apt-get -s upgrade` ومراجعة الحزم التي ستثبت أو تزال قبل الموافقة على التنفيذ الفعلي.

### نتيجة محاكاة ترقية الخادم

- نجح `apt-get update` في تحديث الفهارس من مرايا Ubuntu الرسمية ومستودع Docker دون أخطاء ظاهرة.
- أظهرت محاكاة `apt-get -s upgrade` عدد `55` عملية تثبيت/ترقية، وعدد `0` عمليات إزالة.
- لا توجد حزم سيحذفها APT وفق المحاكاة.
- تشمل التغييرات المهمة Docker CE من `29.0.2` إلى `29.6.2`، وcontainerd من `2.1.5` إلى `2.2.6`، وDocker Compose plugin من `2.40.3` إلى `5.3.1`، إضافة إلى netplan و`linux-base`.
- لم تظهر `cloud-init` ضمن الحزم المهمة التي ستنفذها ترقية `upgrade` الحالية، وهو متوافق مع كونه held back.
- القرار: تنفيذ `apt-get upgrade` بصورة غير تفاعلية مع الاحتفاظ بملفات الإعداد الحالية عبر `--force-confold`، وحفظ السجل في `/root`، ثم فحص Docker وn8n والخدمات قبل تنفيذ أي إعادة تشغيل للخادم.

### نتيجة الترقية الفعلية للخادم قبل النشر

- اكتمل أمر `apt-get upgrade` بنجاح وأعاد رمز الخروج `0`، وحُفظ سجله في `/root/predeploy-upgrade-20260722-075138.log`.
- لم يعرض `dpkg --audit` أي حزم ناقصة الإعداد أو أخطاء في قاعدة الحزم.
- تمت ترقية Docker إلى `29.6.2` وDocker Compose إلى `5.3.1`.
- عادت حاويتا `root-traefik-1` و`root-n8n-1` للعمل بعد الترقية، وأظهر فحص n8n الصحي النتيجة `{"status":"ok"}`.
- جرى إنشاء ملف الإقلاع للنواة الجديدة `6.8.0-136-generic`، بينما ما تزال النواة المشغلة `6.8.0-134-generic`؛ لذلك يلزم إعادة تشغيل VPS لتفعيل النواة الجديدة.
- ظهرت وحدة واحدة فاشلة هي `fwupd.service` أثناء إعادة تشغيل الخدمات. هذه الخدمة خاصة بتحديث Firmware وليست جزءًا من Docker أو Traefik أو n8n، لكن سيعاد فحصها بعد إعادة تشغيل الخادم قبل تجاهلها أو تعطيلها.
- لم تُنفذ إعادة التشغيل بعد. الخطوة التالية هي إعادة تشغيل VPS بشكل مضبوط، ثم التحقق من النواة وSSH وDocker والحاويات وصحة n8n قبل بدء نشر خدمة SHEIN.

### إعادة تشغيل VPS وتفعيل النواة الجديدة

- أُعيد تشغيل VPS بعد اكتمال الترقية، وظهرت شاشة الدخول إلى `Ubuntu 24.04.4 LTS` بالنواة الجديدة `6.8.0-136-generic`، ما يثبت نجاح تفعيل تحديث النواة.
- بعد عودة جلسة Terminal كُتب أمر `reboot` مرة أخرى، ولذلك ظهرت الرسالة `Your session ended` ونُفذت إعادة تشغيل ثانية. هذا لا يدل على عطل ولا يسبب ضررًا للخادم، لكنه يتطلب انتظار عودة VPS وفتح Terminal مرة أخرى.
- الخطوة التالية: فتح جلسة Terminal جديدة دون تكرار `reboot`، ثم تنفيذ فحوص ما بعد الإقلاع لـSSH وDocker وTraefik وn8n و`fwupd.service`.

### نتيجة فحوص ما بعد إعادة الإقلاع

- يعمل VPS بالنواة الجديدة `6.8.0-136-generic`، وكان زمن التشغيل عند الفحص دقيقتين.
- `ssh.socket` في حالتي `active` و`enabled`، ما يؤكد عودة الوصول إلى Terminal بصورة صحيحة.
- أعاد `systemctl --failed` العدد `0`، ولا توجد أي خدمة فاشلة بعد إعادة الإقلاع.
- خدمة Docker في حالتي `active` و`enabled`، والإصدارات الحالية هي Docker `29.6.2` وDocker Compose `5.3.1`.
- عادت حاويتا `root-traefik-1` و`root-n8n-1` تلقائيًا، وكانتا في حالة `Up 2 minutes` وقت الفحص.
- أعاد فحص n8n الصحي `{"status":"ok"}`.
- أصبحت `fwupd.service` في حالة `inactive (dead)` كوحدة `static` وليست في حالة فشل؛ وبذلك زال فشلها السابق بعد إعادة الإقلاع ولا يلزم إصلاحها.
- أصبحت ترقية النظام وإعادة الإقلاع والتحقق من الخدمات الأساسية مكتملة بنجاح. الخطوة التالية هي فحص أسماء التحديثات الثلاثة المتبقية وسبب رسالة التحديث التلقائي قبل متابعة إعداد النشر.

### مراجعة التحديثات المتبقية بعد الإقلاع

- أظهر `apt list --upgradable` حزمتين فقط: `cloud-init` من `24.1.3-0ubuntu3` إلى `26.1-0ubuntu1~24.04.1`، و`fwupd` من `1.9.31-0ubuntu1~24.04.1` إلى `2.0.20-1ubuntu2~24.04.2`.
- يوضح سجل `unattended-upgrades` أن آخر دفعة تحديثات تلقائية اكتملت برسالة `All upgrades installed`، ولا يظهر فشل في `dpkg`.
- رسالة عدم تثبيت تحديث تلقائي ترتبط بحزمة `cloud-init` التي يسجل النظام صراحة أنها `marked to be held back`، وليست بسبب عطل في مدير الحزم.
- لا تُثبت `cloud-init` يدويًا ولا يُلغى حجزها حاليًا لأنها مرتبطة بتهيئة VPS لدى مزود الاستضافة، والخادم يعمل بصورة سليمة.
- لا يلزم تحديث `fwupd` قبل النشر لأنها خدمة Firmware غير مستخدمة فعليًا داخل VPS، وهي حاليًا `inactive (dead)` وليست فاشلة.
- القرار: إبقاء الحزمتين دون تغيير، واعتبار مرحلة تحديث VPS والتحقق منه مكتملة. المرحلة التالية هي تهيئة مشروع SHEIN للعمل كخدمة خلفية headless متكاملة مع n8n.

## 2026-07-23 — تدقيق جاهزية المشروع للتحويل إلى خدمة خادمية

### نطاق الفحص

- تمت مراجعة بنية المشروع وطبقات `domain` و`application` و`infrastructure` و`presentation` و`cli`.
- تم تتبع المسار الكامل من استقبال رابط أو فاتورة، مرورًا باستخراج سلة SHEIN وحفظ JSON، وصولًا إلى إنشاء تقرير PDF.
- تم فحص الاعتماديات وملفات التشغيل الحالية ومدى توافقها مع Linux وDocker والعمل دون واجهة رسومية.

### نتائج التحقق الفعلية

- نجح الأمر `.\.venv\Scripts\python.exe -m pytest -q` وكانت النتيجة: `10 passed in 5.79s`.
- نجح الأمر `.\.venv\Scripts\python.exe -m ruff check .` وكانت النتيجة: `All checks passed!`.
- كشف الأمر `.\.venv\Scripts\python.exe -m ruff format --check .` أن `39` ملفًا يحتاج إلى تنسيق و`13` ملفًا منسق بالفعل. هذه ملاحظة تنسيق فقط وليست خطأ تشغيل.
- حالة Git كانت نظيفة ولم توجد تغييرات غير محفوظة وقت الفحص.
- تم التأكد من صحة اتجاه حقلي `sku_code` و`goods_sn` في طبقة التحويل الحالية.
- تم التأكد من وجود إغلاق آمن لمتصفح Chromium و`context` بعد الاستخراج، بما في ذلك حالات الخطأ.

### التقييم الحالي

- تطبيق سطح المكتب يعمل بصورة سليمة وفق الاختبارات والفحص البرمجي الحالي.
- طبقات المجال وحالات الاستخدام واستخراج المنتجات وحفظ JSON قابلة لإعادة الاستخدام داخل الخدمة الخادمية.
- المشروع غير جاهز بعد للنشر كخدمة HTTP على VPS؛ لأن واجهة API والطابور وحزمة Docker وتصدير PDF الخادمي لم تُنفذ بعد.

### الفجوات المطلوب إكمالها

- إضافة واجهة `FastAPI` لاستقبال الفاتورة أو الرابط وإرجاع حالة الطلب وملف PDF.
- إضافة طابور `FIFO` لمعالجة كل رسالة بصورة مستقلة وبحسب أولوية الوصول، مع تشغيل تحليل واحد مبدئيًا لتقليل تعارض جلسات Chromium.
- إضافة حماية بمفتاح API بين `n8n` والخدمة.
- استبدال اعتماد PDF الخادمي المباشر على `PySide6` بمحوّل headless يعتمد HTML/CSS وChromium عبر Playwright، مع إبقاء محول Qt لتطبيق سطح المكتب. جرى تصحيح القرار السابق الخاص بـ`WeasyPrint` بعد تجربة فعلية على Windows أظهرت حاجته إلى مكتبات نظام GTK/Pango إضافية.
- إضافة `Dockerfile` و`.dockerignore` وملف Compose ومتغيرات بيئة نموذجية واعتماديات خاصة بالخادم.
- إضافة اختبارات لمسار الخدمة الكامل وحالات انتهاء الرابط وفشل الصور وتكرار الطلبات والطابور.

### ترتيب التنفيذ المعتمد للمرحلة التالية

1. إنشاء محول PDF خادمي headless خلف منفذ `ReportExporter` الحالي دون التأثير على تطبيق سطح المكتب.
2. إنشاء خدمة تنسيق موحدة تنفذ: تحليل المدخل، استخراج السلة، حفظ JSON، جلب الصور بالمحاولات، ثم إنشاء PDF.
3. إنشاء API وحماية المصادقة ونقاط فحص الصحة.
4. إنشاء طابور FIFO وحالات واضحة للمهام.
5. إضافة اختبارات الخادم والتكامل.
6. إنشاء ملفات Docker وتشغيل اختبار محلي كامل.
7. نشر الخدمة داخل شبكة Docker `root_default` وربطها مع `n8n` ثم Telegram.

## 2026-07-23 — تنفيذ محول PDF الخادمي

### التغييرات المنفذة

- أضيف `PlaywrightPdfReportExporter` داخل `src/shein_extractor/infrastructure/pdf/headless.py` كتطبيق خادمي لمنفذ `ReportExporter`.
- يعتمد المحول على HTML/CSS واتجاه RTL، ثم يستخدم Chromium عبر Playwright لإنشاء ملف PDF بحجم A4 عمودي.
- يتضمن التقرير بطاقات معلومات التحليل وجدول المنتجات كاملًا، مع تكرار رأس الجدول ومنع تقسيم صف المنتج قدر الإمكان بين الصفحات.
- يقتصر اسم المنتج داخل PDF على أول `100` حرف، ويبقى `SKU` كاملًا دون التفاف.
- تُضمّن صور المنتجات داخل HTML بصيغة Data URI، وتظهر خانة بديلة للصور غير المتوفرة مع تسجيل عددها في نتيجة التصدير.
- نُقلت الأدوات المشتركة `PdfExportResult` و`truncate_product_name` إلى `src/shein_extractor/infrastructure/pdf/common.py` لإعادة استخدامها بين محولي Qt وChromium.
- أصبح `src/shein_extractor/infrastructure/pdf/__init__.py` يستخدم تحميلًا كسولًا للمحولات؛ لذلك يمكن للخدمة الخادمية استيراد محول Chromium دون استيراد `PySide6`.
- بقي `QtPdfReportExporter` دون تغيير في سلوكه لتطبيق سطح المكتب.

### قرار الاعتمادية

- جرت تجربة `WeasyPrint` أولًا، لكنها لم تعمل في بيئة Windows المحلية بسبب غياب مكتبات النظام الأصلية `GTK/Pango`.
- اعتُمد Chromium المتوافر أصلًا ضمن Playwright بدل إضافة سلسلة اعتماديات نظام جديدة؛ وهذا يوحّد متصفح استخراج SHEIN ومتصفح إنشاء PDF داخل صورة Docker لاحقًا.

### نتائج التحقق الفعلية

- نجح الاختبار المحدد بالأمر `.\.venv\Scripts\python.exe -m pytest -q tests\test_headless_pdf.py` وكانت النتيجة `3 passed in 3.28s`.
- شملت الاختبارات إنشاء PDF حقيقي، والتحقق من توقيع الملف وعدد صفحاته والرابط القابل للنقر، وحالة الصورة غير المتوفرة، وعدم استيراد `PySide6` مع المحول الخادمي.
- نجحت مجموعة الاختبارات الكاملة بالأمر `.\.venv\Scripts\python.exe -m pytest -q` وكانت النتيجة `13 passed in 2.30s`.
- نجح الأمر `.\.venv\Scripts\python.exe -m ruff check .` وكانت النتيجة `All checks passed!`.
- نجح فحص تنسيق الملفات الجديدة والمعدلة الخاصة بالمحول الخادمي، وكانت الملفات الأربعة المستهدفة منسقة بالفعل.

### الحالة والخطوة التالية

- أصبح إنشاء PDF الخادمي headless جاهزًا كطبقة بنية تحتية مستقلة عن واجهة سطح المكتب.
- الخطوة التالية هي إنشاء خدمة تنسيق موحدة تنفذ المسار الكامل: تحليل الفاتورة أو الرابط، استخراج السلة، حفظ JSON، جلب الصور بالمحاولات، ثم إنشاء PDF بواسطة `PlaywrightPdfReportExporter`.

## 2026-07-23 — تنفيذ خدمة المعالجة الخادمية الموحدة

### التغييرات المنفذة

- أضيفت حالة الاستخدام `ProcessCart` داخل `src/shein_extractor/application/processing.py` لتنفيذ المسار الكامل من الفاتورة أو الرابط حتى JSON وPDF.
- أضيفت نماذج `ProcessCartRequest` و`ProcessCartResult` ومراحل التقدم `ProcessingStage` و`ProcessingProgress` تمهيدًا لربط المسار مع API والطابور.
- أضيف منفذ `ProductImageFetcher` ونتيجته `ImageFetchResult` إلى طبقة `application`.
- أضيف `HttpxProductImageFetcher` داخل `src/shein_extractor/infrastructure/images/httpx_fetcher.py` لتنزيل الصور بصورة headless مع إعادة المحاولة.
- أضيف الخطأ `InvalidProcessingInputError` لتمييز المدخلات التي لا تحتوي رابط SHEIN واحدًا صالحًا.
- أضيفت اختبارات تغطي الفاتورة الكاملة، والرابط المباشر، وأولوية البيانات الصريحة، والمدخل دون رابط، وتعدد الروابط، وإزالة تكرار روابط الصور، ومراحل التقدم، ومحاولات تنزيل الصور.

### القرارات المعتمدة

- كل مهمة معالجة تقبل رابط SHEIN واحدًا فقط؛ وجود أكثر من رابط يعاد كخطأ إدخال واضح بدل اختيار رابط عشوائي.
- اسم العميل ورقم الطلبية المرسلان صراحة إلى الخدمة يتقدمان على القيم المستخرجة من نص الفاتورة.
- تحفظ نتيجة JSON فور نجاح استخراج السلة وقبل تنزيل الصور وإنشاء PDF.
- تنزل كل صورة فريدة مرة واحدة، حتى لو تكرر رابطها في عدة صفوف.
- الحد الافتراضي هو `10` محاولات لكل صورة. إذا تعذر تنزيلها بعد ذلك، تستمر المهمة ويظهر «غير متوفر» في PDF بدل إسقاط المنتج أو إلغاء التقرير.
- تبث حالة الاستخدام مراحل تقدم مستقلة عن الواجهة، لتستخدم لاحقًا في API وحالة مهام طابور `FIFO`.

### نتائج التحقق الفعلية

- نجحت اختبارات المسار الجديد بالأمر `.\.venv\Scripts\python.exe -m pytest -q tests\test_server_processing.py` وكانت النتيجة `5 passed in 0.42s`.
- نجحت مجموعة الاختبارات الكاملة بالأمر `.\.venv\Scripts\python.exe -m pytest -q` وكانت النتيجة `18 passed in 3.23s`.
- نجح الأمر `.\.venv\Scripts\python.exe -m ruff check .` وكانت النتيجة `All checks passed!`.
- نجح فحص التنسيق للملفات الستة الجديدة والمعدلة في هذه المرحلة وكانت النتيجة `6 files already formatted`.
- كشف فحص التنسيق الكامل وجود `37` ملفًا قديمًا يحتاج إلى إعادة تنسيق و`15` ملفًا منسقًا؛ لم يجر تنسيق شامل لتجنب تعديل ملفات غير مرتبطة بهذه المرحلة، ولا تمثل هذه النتيجة خطأ تشغيل.
- أعيد التحقق النهائي بعد تحديث التوثيق: نجحت الاختبارات وعددها `18` في `2.57s`، ونجح `ruff check` وفحص تنسيق الملفات الستة و`git diff --check`.

### الحالة والخطوة التالية

- أصبح مسار المعالجة الخادمي الأساسي جاهزًا وقابلًا للاستدعاء دون واجهة Qt.
- لم تُجر أي تغييرات على VPS في هذه المرحلة؛ جميع التغييرات الحالية محلية داخل المشروع.
- الخطوة التالية هي إضافة `FastAPI` مع مفتاح API ونقاط فحص الصحة، ثم ربطها بطابور `FIFO` يضمن معالجة الرسائل حسب ترتيب الوصول.

## 2026-07-23 — إضافة واجهة FastAPI المحمية

### التغييرات المنفذة

- أضيفت طبقة `src/shein_extractor/presentation/api` دون ربطها بواجهة Qt.
- أضيف مصنع التطبيق `create_app` لحقن حالة الاستخدام وتسهيل الاختبار دون تشغيل Chromium فعليًا.
- أضيفت النقاط `GET /health/live` و`GET /health/ready` و`POST /v1/process`، إضافة إلى توثيق OpenAPI في `/docs`.
- تحمي ترويسة `X-API-Key` نقطة المعالجة باستخدام مقارنة ثابتة الزمن عبر `secrets.compare_digest`.
- تعيد نقطة المعالجة ملف PDF مباشرة مع ترويسات لرقم الطلب الداخلي وعدد المنتجات وعدد صفحات PDF وعدد الصور غير المتوفرة.
- تحول أخطاء المدخل إلى `422`، والرابط المنتهي إلى `410`، وفشل استخراج SHEIN إلى `502`، وعدم إعداد المفتاح إلى `503`.
- تنفذ المعالجة المتزامنة بواسطة `run_in_threadpool` حتى لا تحجب حلقة الأحداث في FastAPI.
- أضيفت `ApiSettings` لقراءة المفتاح والمسارات ومحاولات الصور والمهلة وعنوان الاستماع والمنفذ من متغيرات البيئة.
- أضيفت نقطة التشغيل `shein-api` واعتماديات `fastapi` و`uvicorn` ضمن المجموعة الاختيارية `server`.
- أضيف `.env.example` بأسماء المتغيرات فقط، وأضيف `.env` إلى `.gitignore` لمنع حفظ الأسرار داخل Git.
- حدث `README.md` بأوامر التشغيل المحلي ونقاط API وشكل الطلب.

### القرارات المعتمدة

- يعاد PDF مباشرة من `POST /v1/process` لتبسيط استلامه كبيانات ثنائية داخل `n8n`.
- تبقى نقاط الصحة عامة حتى يستطيع Docker وTraefik مراقبة الخدمة، بينما تبقى المعالجة محمية بالمفتاح.
- لا يشغل `uvicorn` أكثر من عامل واحد حاليًا؛ لأن طابور `FIFO` لم يضف بعد، ولتقليل التزامن غير المنضبط لجلسات Chromium.
- لم يضف تكامل Telegram أو `n8n` بعد، ولم تجر تغييرات على VPS في هذه المرحلة.

### نتائج التحقق الفعلية

- نجحت اختبارات API المحددة وعددها `10` في `0.54s`.
- نجحت مجموعة الاختبارات الكاملة وعددها `28` في `3.61s`.
- نجح `ruff check .` وكانت النتيجة `All checks passed!`.
- نجح فحص تنسيق الملفات العشرة المستهدفة وكانت النتيجة `10 files already formatted`.
- نجح `python -m compileall -q src`.
- نجح استيراد Composition Root للخادم، وظهرت المسارات: `/health/live` و`/health/ready` و`/v1/process` و`/docs` و`/openapi.json`.
- ظهر تحذير إهمال واحد من `FastAPI TestClient` داخل مكتبة خارجية بخصوص انتقال Starlette مستقبلًا من `httpx` إلى `httpx2`؛ لا يؤثر في تشغيل API أو الاختبارات الحالية.

### الحالة والخطوة التالية

- أصبحت واجهة HTTP الأساسية جاهزة محليًا وتعيد PDF مباشرة عند نجاح المعالجة.
- الخطوة التالية هي تنفيذ طابور `FIFO` دائم الحالات، بحيث تعامل كل رسالة كمهمة مستقلة وتنفذ الرسائل حسب ترتيب الوصول مع عامل Chromium واحد مبدئيًا.

## 2026-07-23 — تنفيذ طابور FIFO دائم الحالات

### التغييرات المنفذة

- أضيف نموذج المهمة `ProcessingJob` وحالاتها `queued` و`running` و`completed` و`failed` داخل `application/jobs.py`.
- أضيف `ProcessingQueue` بعامل خلفي واحد لمعالجة المهام حسب ترتيب الوصول ومنع تشغيل جلسات Chromium متعددة في الوقت نفسه.
- أضيف `SqliteJobRepository` داخل `infrastructure/queue` لحفظ المهام والتقدم والنتائج والأخطاء بصورة دائمة.
- يستخدم مستودع SQLite وضع `WAL` و`busy_timeout`، ويطالب بالمهمة التالية ذريًا باستخدام `BEGIN IMMEDIATE`.
- تعاد المهام التي بقيت بحالة `running` إلى `queued` تلقائيًا عند بدء الخدمة بعد انقطاع أو إعادة تشغيل.
- ربطت دورة حياة `FastAPI` بتشغيل عامل الطابور وإيقافه، وأصبحت الجاهزية تتحقق من العامل والمفتاح ومجلدات التشغيل.
- أضيفت `POST /v1/jobs` لإنشاء مهمة، و`GET /v1/jobs/{job_id}` لمتابعتها، و`GET /v1/jobs/{job_id}/pdf` لتنزيل التقرير المكتمل.
- أبقيت `POST /v1/process` للتوافق؛ يمر عبر الطابور نفسه وينتظر النتيجة ضمن مهلة `SHEIN_SYNC_WAIT_SECONDS`.
- أضيف المتغير `SHEIN_QUEUE_DB` لتحديد ملف قاعدة الطابور، وأضيف مجلد `data/` إلى `.gitignore`.
- حدثت وثائق `README.md` و`ARCHITECTURE.md` لتوضيح عقد المهام والمسار المقترح داخل `n8n`.

### القرارات المعتمدة

- تنفذ المهام حاليًا بعامل واحد لضمان ترتيب `FIFO` الحقيقي وتقليل الضغط على Chromium وذاكرة الخادم.
- كل رسالة واردة تنشئ مهمة مستقلة حتى لو كان محتواها مكررًا؛ لا يرفض النظام الطلبات المكررة.
- تبقى المهمة قيد التنفيذ عند انتهاء مهلة `POST /v1/process`، ويمكن متابعتها لاحقًا باستخدام معرّفها.
- يحتفظ SQLite بمسارات JSON وPDF والعدادات والخطأ المنظم، لتستمر متابعة المهام بعد إعادة تشغيل API.
- واجهات الحالة والتنزيل هي المسار الموصى به لـ `n8n` بدل إبقاء اتصال HTTP واحد مفتوحًا طوال مدة التحليل.

### نتائج التحقق الفعلية

- نجحت اختبارات الطابور وAPI المحددة وعددها `16` في `1.25s`.
- نجحت مجموعة الاختبارات الكاملة بالأمر `.\.venv\Scripts\python.exe -m pytest -q` وكانت النتيجة `34 passed, 1 warning in 3.32s`.
- نجح `ruff check .` وكانت النتيجة `All checks passed!`.
- نجح فحص تنسيق الملفات المستهدفة وكانت النتيجة `11 files already formatted`.
- نجح `python -m compileall -q src`.
- نجح `python -m pip check` وكانت النتيجة `No broken requirements found`.
- نجح `git diff --check` مع تحذيرات نهايات أسطر فقط دون أخطاء مسافات.
- التحذير الوحيد صادر من `FastAPI TestClient` داخل مكتبة خارجية بخصوص انتقال Starlette مستقبلًا من `httpx` إلى `httpx2`، ولا يؤثر في التشغيل الحالي.

### الحالة والخطوة التالية

- أصبح مسار API مع طابور `FIFO` الدائم جاهزًا محليًا وقابلًا لاستقبال عدة طلبات مستقلة بالترتيب.
- لم تجر تغييرات على VPS ضمن هذه المرحلة.
- الخطوة التالية هي إعداد حاوية الخدمة للنشر، وربطها بشبكة Docker الحالية، ثم إنشاء سير عمل `n8n` للاستقبال والمتابعة وتنزيل PDF وإرساله إلى مجموعة Telegram.

### تحقق التسليم النهائي

- أعيد تشغيل مجموعة الاختبارات بعد تحديث التوثيق، ونجحت `34` حالة في `3.19s` مع التحذير الخارجي نفسه فقط.
- أعيد تشغيل `ruff check .` و`compileall` و`pip check` و`git diff --check`، ونجحت جميعها دون أخطاء.
- نجح استيراد Composition Root وظهرت المسارات الفعلية: `/health/live` و`/health/ready` و`/v1/jobs` و`/v1/jobs/{job_id}` و`/v1/jobs/{job_id}/pdf` و`/v1/process`.

## 2026-07-23 — تجهيز حاوية الخدمة للنشر على Hostinger

### التغييرات المنفذة

- أضيف `Dockerfile` مبني على `python:3.13-slim-bookworm` لتشغيل API دون واجهة سطح المكتب.
- يثبت البناء اعتماديات مجموعة `server` فقط، ثم يثبت Chromium واعتماداته بالأمر `python -m playwright install --with-deps chromium`.
- أضيفت خطوط DejaVu وNoto الضرورية لعرض العربية والرموز داخل تقارير PDF.
- تعمل الحاوية بمستخدم غير جذري باسم `shein`، مع `HEALTHCHECK` على `/health/live`.
- يعمل Uvicorn بعامل واحد `--workers 1` للمحافظة على ترتيب طابور `FIFO` ومنع تشغيل عدة جلسات Chromium متزامنة.
- أضيف `compose.hostinger.yml` لتشغيل خدمة `shein-api` وربطها داخليًا بشبكة Docker الحالية `root_default`.
- لا ينشر ملف Compose أي منفذ للإنترنت؛ الوصول إلى API سيكون من `n8n` عبر `http://shein-api:8000`.
- أضيف ربط دائم من `./runtime` على الخادم إلى `/app/runtime` داخل الحاوية لحفظ SQLite وJSON وPDF خارج دورة حياة الحاوية.
- حددت موارد الخدمة مبدئيًا إلى `1.5` CPU و`4 GB` ذاكرة و`1 GB` لذاكرة Chromium المشتركة، مع تدوير سجلات Docker.
- فُصلت اعتماديات PySide6 إلى المجموعة الاختيارية `desktop` حتى لا تثبت مكتبات الواجهة الرسومية داخل حاوية الخادم.
- حُدثت أوامر تثبيت سطح المكتب في `README.md` لاستخدام `.[desktop]` و`.[desktop,dev]`.
- أعيد `.env.example` إلى المسارات المحلية، وأضيف `.env.hostinger.example` للمفتاح السري المطلوب على الخادم.
- أضيف `.dockerignore` لمنع إرسال البيئات الافتراضية والاختبارات والأسرار والتقارير المحلية إلى سياق بناء Docker.
- أضيف `HOSTINGER_DEPLOYMENT.md` ويحتوي على أوامر التجهيز والبناء والتشغيل وفحوص الصحة والتخزين والربط مع `n8n`.

### القرارات المعتمدة

- يبنى Docker ويختبر فعليًا على VPS في Hostinger؛ لا يلزم تثبيت Docker Desktop على Windows.
- تحفظ كل البيانات دائمًا داخل مجلد `runtime/` على الخادم، ولا يستخدم الأمر `docker compose down -v`.
- يبقى API داخليًا وغير مكشوف مباشرة للإنترنت، ويتولى `n8n` استقبال Telegram وإدارة الطلبات.
- يحتفظ النظام بعامل معالجة واحد في المرحلة الأولى لضمان الأولوية حسب وقت الوصول وتقليل استهلاك ذاكرة الخادم.

### نتائج التحقق الفعلية

- تطابقت متغيرات `compose.hostinger.yml` مع `ApiSettings` الفعلية، بما فيها `SHEIN_QUEUE_DB` ومسارات JSON وPDF.
- تحقق فحص ساكن من وجود الشبكة الخارجية `root_default`، وربط `runtime/`، وسياسة إعادة التشغيل، وعدم وجود قسم `ports`.
- تحقق فحص ساكن من تثبيت Chromium، والعمل بمستخدم غير جذري، وفحص `/health/live`، وتشغيل Uvicorn بعامل واحد.
- ظهرت مسارات API الفعلية: `/health/live` و`/health/ready` و`/v1/jobs` و`/v1/jobs/{job_id}` و`/v1/jobs/{job_id}/pdf` و`/v1/process`.
- نجحت مجموعة الاختبارات كاملة: `34 passed, 1 warning in 3.29s`.
- نجح `ruff check .` وكانت النتيجة `All checks passed!`.
- نجح `python -m compileall -q src`.
- نجح `python -m pip check` وكانت النتيجة `No broken requirements found`.
- نجح `git diff --check` دون أخطاء مسافات؛ ظهرت تحذيرات CRLF خاصة ببيئة Windows فقط.
- لم ينفذ بناء صورة Docker محليًا لأن Docker غير مثبت على جهاز Windows، ولم تجر أي تغييرات على VPS في هذه المرحلة.

### الحالة والخطوة التالية

- أصبحت حزمة النشر المحلية جاهزة للرفع إلى Hostinger.
- الخطوة التالية هي رفع المشروع إلى `/opt/shein-extractor` على VPS، ثم تنفيذ `docker compose -f compose.hostinger.yml build --pull` واختبار فحوص الصحة والتخزين والاتصال من حاوية `n8n`.

### تجهيز حزمة الرفع

- أنشئت حزمة نشر نظيفة في `C:\Users\abd73\Downloads\shein-extractor-hostinger-20260723-110701.tar.gz`.
- حجم الحزمة `119222` بايت، وبصمة SHA-256 هي `CB2E88CCA24F738FB3D858BC0DCF6C22E94382DFA71671C7B6DB44C49203094B`.
- تحقق الاختبار من سلامة أرشيف `tar.gz` ومن وجود `Dockerfile` و`compose.hostinger.yml` و`pyproject.toml` ومجلد `src/` ودليل النشر.
- استبعدت الحزمة `.env` و`.venv` وملفات Git والاختبارات والذاكرات المؤقتة والتقارير المحلية؛ لم تتضمن أسرارًا أو مخرجات تشغيل سابقة.
- لم ترفع الحزمة إلى VPS بعد؛ تتطلب خطوة النقل إدخال كلمة مرور `root` من المستخدم عبر `scp`.

### نتائج الرفع والتجهيز على VPS

- رفع المستخدم الحزمة إلى `/root/shein-extractor-hostinger-20260723-110701.tar.gz` عبر `scp` وظهر اكتمال النقل بنسبة `100%`.
- تحقق الخادم من SHA-256 وكانت النتيجة `cb2e88cca24f738fb3d858bc0dcf6c22e94382dfa71671c7b6db44c49203094b`، وهي مطابقة للبصمة المحلية.
- استخرجت الحزمة بنجاح داخل `/opt/shein-extractor` وظهرت ملفات النشر ومجلد `src/`.
- صححت صلاحيات المجلدات إلى `755` والملفات إلى `644`، وحُمي ملف `.env` بصلاحية `600`.
- أنشئت مجلدات `runtime/data` و`runtime/exports` و`runtime/outputs` بصلاحية `700` وملكية UID/GID رقم `10001` المطابق لمستخدم `shein` داخل الحاوية.
- أنشئ `SHEIN_API_KEY` عشوائيًا على الخادم ولم تعرض قيمته؛ أظهر الفحص الاسم فقط بالشكل `SHEIN_API_KEY=<hidden>`.
- ظهور المالك `UNKNOWN:UNKNOWN` لمجلدات `runtime` في نظام المضيف متوقع، لأن UID/GID رقم `10001` معرف داخل الحاوية وليس باسم محلي على VPS.
- نجح فحص شبكة Docker على VPS، وظهرت `root_default` باستخدام المشغل `bridge`.
- نجح `docker compose --env-file .env -f compose.hostinger.yml config -q` برمز خروج `0`.
- أظهر Compose خدمة واحدة باسم `shein-api` وصورة باسم `shein-extractor-api:0.2.0`.
- أصبحت إعدادات Compose جاهزة للبناء الفعلي على VPS دون نشر منفذ عام.
- اكتمل أول بناء فعلي للصورة `shein-extractor-api:0.2.0` على VPS بنجاح، وكانت نتيجة `Exit code: 0`.
- سجل البناء محفوظ على الخادم في `/root/shein-build-20260723-082328.log`.
- معرف الصورة المختصر `6727ad14f2e4`، وحجمها على قرص Docker نحو `2.44GB` وحجم المحتوى `709MB`.
- اكتملت مراحل تصدير الطبقات وفك الصورة دون أخطاء، وأصبحت الصورة جاهزة لتشغيل الحاوية.
- شغلت حاوية `shein-api` فعليًا باستخدام Compose، وظهرت بحالة `Up` و`healthy`.
- أظهر الفحص `Status: running | Health: healthy | Restart count: 0`.
- أعاد `/health/live` النتيجة `{"status":"ok"}`، وأعاد `/health/ready` النتيجة `{"status":"ready"}`.
- نجح اتصال حاوية `root-n8n-1` بالخدمة عبر `http://shein-api:8000/health/live` وأعاد HTTP `200`.
- أظهر `docker compose ps` المنفذ `8000/tcp` داخل شبكة Docker فقط دون ربطه بمنفذ على المضيف، كما هو مخطط أمنيًا.
- روجعت واجهة الاختبار الفعلي بعد نجاح التشغيل: إنشاء المهمة يتم عبر `POST /v1/jobs` باستخدام الترويسة `X-API-Key` والجسم `raw_input` مع الحقول الاختيارية `customer_name` و`order_number` و`timeout_seconds`.
- ستتابع حالة المهمة عبر `GET /v1/jobs/{job_id}`، ثم ينزل التقرير المكتمل عبر `GET /v1/jobs/{job_id}/pdf`.
- أصبحت الخطوة التالية اختبار دورة فعلية كاملة من حاوية `n8n` إلى `shein-api` باستخدام رابط مشاركة حديث وصالح، مع عدم طباعة مفتاح API في الطرفية.
- أرسل أول اختبار فعلي على VPS باستخدام فاتورة العميل `رهف بسام` ورقم الطلبية `H-2259` ورابط `https://onelink.shein.com/44/5wjvd0rfx1en`.
- قبل API الطلب بنجاح وأعاد HTTP `202` ضمنيًا بالحالة `queued` ومعرّف المهمة `0ffdb9ea-470e-4361-9211-4dd55c5d074d`.
- يؤكد ذلك نجاح المصادقة بمفتاح API، وتحليل جسم الطلب، وإضافة المهمة إلى طابور المعالجة الدائم. بقي التحقق من اكتمال التحليل وإنشاء PDF.
- اكتملت أول مهمة فعلية على VPS بالحالة `completed` والمرحلة `completed` دون أي خطأ.
- بدأ تنفيذ المهمة عند `2026-07-23T08:37:45.956113Z` واكتمل عند `2026-07-23T08:38:05.366141Z`، أي خلال نحو 19.4 ثانية.
- استخرج النظام `39` منتجًا وأنشأ تقرير PDF من `5` صفحات، وكانت قيمة `unavailable_image_count` مساوية للصفر؛ ظهرت جميع الصور المطلوبة.
- حفظ JSON داخل الحاوية في `/app/runtime/outputs/H-2259-رهف-بسام-20260723-083752.json`، وحفظ PDF في `/app/runtime/exports/H-2259-رهف-بسام-20260723-083752.pdf`.
- أثبت الاختبار نجاح دورة: استقبال الفاتورة، استخراج اسم العميل ورقم الطلب والرابط، تحليل السلة، تنزيل الصور، حفظ JSON، وإنشاء PDF. بقي اختبار تنزيل PDF من حاوية `n8n`.
- نجحت حاوية `n8n` في الوصول إلى مسار تنزيل PDF واجتاز المحتوى فحص ترويسة `%PDF-` قبل محاولة الحفظ.
- فشل حفظ النسخة داخل `/files/H-2259-rahaf-bassam.pdf` بالخطأ `EACCES: permission denied`؛ لم يُنشأ ملف داخل `/local-files`.
- المشكلة محصورة في صلاحيات مجلد الربط `/local-files` وليست في API أو التحليل أو إنشاء PDF أو اتصال Docker. يلزم فحص UID/GID لمستخدم `n8n` وملكية المجلد قبل تصحيح الصلاحيات.
- أثبت فحص الصلاحيات أن حاوية `n8n` تعمل بالمستخدم `node` ذي UID/GID رقم `1000:1000`.
- كان `/local-files` على المضيف و`/files` داخل الحاوية مملوكين لـ`root:root` بالصلاحية `755`، ولذلك أعاد اختبار الكتابة `Write access: NO`.
- القرار الأمني هو نقل ملكية `/local-files` إلى `1000:1000` واستخدام الصلاحية `750` بدل فتح المجلد للجميع عبر `777`.

### تصحيح صلاحيات مجلد مشاركة n8n — 2026-07-23

- أصبحت ملكية `/local-files` هي UID/GID رقم `1000:1000` وصلاحيته `750`.
- أكد الفحص من داخل حاوية `root-n8n-1` النتيجة `Write access: YES`.
- نجح إنشاء ملف الاختبار `/files/.write-test` ثم حذفه، وظهرت ملكيته `1000:1000`.
- أصبحت مشكلة `EACCES` السابقة محلولة، والخطوة التالية هي إعادة تنزيل تقرير PDF المكتمل من `shein-api` وحفظه في `/files` ثم التحقق منه على المضيف في `/local-files`.
- أعيد اختبار تنزيل تقرير المهمة `0ffdb9ea-470e-4361-9211-4dd55c5d074d` من داخل حاوية `n8n`، وأعاد API حالة HTTP `200`.
- نزّلت حاوية `n8n` ملف PDF بحجم `15304899` بايت، وحفظته في `/files/H-2259-rahaf-bassam.pdf`.
- ظهر الملف على المضيف في `/local-files/H-2259-rahaf-bassam.pdf` بحجم يقارب `15MB`، ونجح فحص الترويسة بالقيمة `%PDF-`.
- اكتملت بذلك دورة التكامل التقنية: `n8n` يصل إلى API، يطلب التقرير، ينزل PDF صالحًا، ويحفظه في مجلد الملفات المشترك. الخطوة التالية هي إعداد Telegram Bot ثم بناء Workflow الاستقبال والمعالجة والإرسال.

## 2026-07-23 — معالجة تضخم تقارير PDF على الخادم

### التحليل والسبب

- بلغ حجم أول تقرير فعلي أنشئ على VPS نحو `15304899` بايت، أي قرابة `15MB` لعدد `39` منتجًا، بينما تراوحت أحجام التقارير المحلية المشابهة تقريبًا بين `118KB` و`299KB`.
- تبيّن أن مسار الخادم `HttpxProductImageFetcher` كان يحتفظ بملفات صور المنتجات الأصلية كاملة الدقة والجودة، ثم يحولها `PlaywrightPdfReportExporter` إلى `data URI` داخل HTML قبل طباعة PDF.
- تحديد أبعاد عرض الصورة داخل HTML إلى `22mm × 22mm` لا يقلل بيانات الصورة الأصلية، ولذلك كان Chromium يضمّن صورًا أكبر بكثير من الحجم المرئي المطلوب.

### التغيير المنفذ

- أضيف الملف `src/shein_extractor/infrastructure/images/optimizer.py` لمعالجة صور المنتجات قبل تمريرها إلى مولد PDF.
- أصبحت الصورة تُصحح حسب اتجاه EXIF، وتُصغّر مع الحفاظ على النسبة إلى حد أقصى `360 × 360` بكسل، ثم تُحوّل إلى JPEG بجودة `78` مع `optimize` و`progressive`.
- الصور ذات الشفافية تُدمج فوق خلفية بيضاء قبل التحويل، حتى لا تظهر بخلفية سوداء.
- إذا تعذر على Pillow التعرف على تنسيق صورة ما، تُعاد البيانات الأصلية بدل إسقاط الصورة أو إفشال التقرير.
- أضيف الاعتماد `Pillow==12.3.0` إلى `pyproject.toml` و`requirements.txt`.
- أضيفت اختبارات للتحقق من خفض الأبعاد والحجم، ومن سلامة مسار الرجوع للبيانات غير المعروفة.

### نتائج التحقق

- خفّض القياس المحلي صورة اختبار من `2,009,351` بايت إلى `27,206` بايت، أي انخفاضًا بنسبة `98.6%`، وأصبحت أبعادها `360 × 270`.
- نجحت الاختبارات المستهدفة: `10 passed in 2.86s`.
- نجحت مجموعة الاختبارات الكاملة: `36 passed, 1 warning in 3.16s`.
- نجح `ruff check .` وكانت النتيجة `All checks passed!`.
- التحذير الوحيد هو تحذير Starlette الخارجي السابق، ولا يرتبط بمعالجة الصور أو إنشاء PDF.

### قرارات التكامل والتخزين

- تنزيل التقرير يدويًا إلى `/files` كان اختبار تكامل فقط. في Workflow النهائي لا حاجة إلى حفظ نسخة دائمة إضافية داخل `/local-files`؛ يستقبل `n8n` ملف PDF كبيانات ثنائية ويرسله مباشرة إلى Telegram.
- تبقى النسخة الأساسية محفوظة داخل `runtime/exports` حسب قرار الاحتفاظ الدائم، وبذلك نتجنب مضاعفة استهلاك المساحة بنسخة ثانية لكل تقرير.
- لدى المستخدم Telegram Bot مستخدم مسبقًا في `n8n`، لذلك لا توجد حاجة إلى إنشاء Bot جديد. الخطوة اللاحقة بعد نشر تحسين الصور هي استخدام بيانات اعتماد Telegram الموجودة وبناء Workflow للمجموعة نفسها.
- يجب إعادة بناء صورة Docker ونشر هذا التحديث على VPS قبل قياس حجم تقرير حقيقي جديد؛ لم تُطبق معالجة الضغط على الحاوية الحالية بعد.

## 2026-07-23 — تثبيت ملفات الإصدار `0.2.1` على VPS

- أنشئت نسخة احتياطية قبل التحديث في `/root/backups/shein-extractor-pre-0.2.1-20260723-094219.tar.gz` بحجم يقارب `119KB`.
- كانت بصمة SHA-256 للنسخة الاحتياطية `9c4de120893bf7e84c06b5f7a9a02779789af20cbfe564e1e47e9b0d18c3f08b`.
- استخرجت حزمة الإصدار الجديد من `/root/shein-extractor-hostinger-20260723-122130.tar.gz` إلى مجلد مرحلي منفصل.
- تحقق الخادم من وجود `DEFAULT_MAX_DIMENSION = 512` واعتماد `Pillow==12.3.0` واسم الصورة `shein-extractor-api:0.2.1`.
- استبدلت ملفات المصدر وإعدادات النشر فقط، مع الحفاظ على `/opt/shein-extractor/.env` ومجلد `/opt/shein-extractor/runtime`.
- بقي ملف `.env` بصلاحية `600` وملكية `root:root`، وبقي `runtime` بصلاحية `700` وملكية UID/GID المخصصين للحاوية؛ ظهور `UNKNOWN:UNKNOWN` على المضيف متوقع.
- نجح فحص `docker compose --env-file .env -f compose.hostinger.yml config -q` برمز خروج `0`.
- أصبحت ملفات الإصدار `0.2.1` مثبتة وجاهزة لإعادة بناء صورة Docker وتشغيل الحاوية المحدثة.
- اكتمل بناء صورة Docker الجديدة `shein-extractor-api:0.2.1` على VPS بنجاح وبرمز خروج `0`.
- حفظ سجل البناء في `/root/shein-build-0.2.1-20260723-094717.log`.
- كان معرّف الصورة المختصر `9debbcfaa27b`، وحجمها على قرص Docker نحو `2.47GB` وحجم محتواها نحو `717MB`.
- بقيت الحاوية الحالية `shein-api` تعمل بالصورة `shein-extractor-api:0.2.0` وبالحالة `healthy` طوال عملية البناء؛ لم يحدث انقطاع أثناء إنشاء الصورة الجديدة.
- كانت مساحة القرص قبل البناء `87GB` متاحة من أصل `96GB`، بنسبة استخدام `10%`.
- الخطوة التالية هي إعادة إنشاء حاوية `shein-api` بالصورة `0.2.1` ثم فحص الصحة والاتصال من `n8n`.
- أعيد إنشاء حاوية `shein-api` بنجاح باستخدام الصورة `shein-extractor-api:0.2.1`.
- ظهرت الحاوية بالحالة `running` و`healthy` دون أي إعادة تشغيل تلقائية، وكانت قيمة `RestartCount` مساوية للصفر.
- تحقق داخل الحاوية من تشغيل `Pillow 12.3.0` ومن أن الحد الأقصى لأبعاد صور التقارير هو `512` بكسل.
- أعاد `/health/live` النتيجة `{"status":"ok"}` وأعاد `/health/ready` النتيجة `{"status":"ready"}`.
- نجح اتصال حاوية `root-n8n-1` بالخدمة الجديدة وأعاد HTTP `200` مع `{"status":"ok"}`.
- اكتمل بذلك نشر الإصدار `0.2.1` تقنيًا، وبقي إنشاء تقرير فعلي جديد وقياس حجمه للتحقق من أثر ضغط الصور.

## 2026-07-23 — التحقق الفعلي من تحسين حجم PDF على VPS

- أنشئ طلب حقيقي بالمعرّف `b8d46cc4-8eec-4912-99f0-6ada302b10fe` من فاتورة تحتوي على رابط سلة SHEIN.
- انتقل الطلب بنجاح عبر الحالات `queued` ثم `extracting_cart` ثم `downloading_images` ثم `completed`.
- استخرج التقرير `39` منتجًا موزعة على `5` صفحات.
- اكتمل تنزيل جميع صور المنتجات، وكانت قيمة `unavailable_image_count` مساوية للصفر.
- حفظ التقرير داخل الحاوية في `/app/runtime/exports/H-2259-رهف-بسام-20260723-095512.pdf`.
- بلغ حجم التقرير الجديد `1,154,241` بايت، أي نحو `1.10MB`.
- كان حجم التقرير السابق قبل تحسين الصور نحو `15MB`، ولذلك تحقق انخفاض فعلي بنسبة `92.5%` مع الحفاظ على جميع الصور.
- أثبت الاختبار أن الحد الأقصى `512px` يوفر توازنًا مناسبًا بين وضوح الصور وحجم ملف PDF.
- اكتمل نشر الإصدار `0.2.1` والتحقق منه عمليًا، وأصبحت المرحلة التالية بناء Workflow في `n8n` لاستقبال رسائل Telegram، تشغيل التحليل، ثم إرسال ملف PDF إلى المجموعة نفسها.

## 2026-07-23 — ربط مجموعة Telegram مع `n8n`

- أضيف Telegram Bot المستخدم مسبقًا إلى مجموعة `الزبائن SKU` بصفة مشرف، وظهر أنه يملك صلاحية الوصول إلى الرسائل.
- أنشئ Workflow باسم `Customer_SKU` وأضيفت إليه عقدة `Telegram Trigger` باستخدام بيانات الاعتماد الموجودة.
- ضُبطت العقدة لاستقبال تحديثات `Message`.
- نجح الاختبار الفعلي واستقبلت العقدة رسالة من المجموعة كعنصر واحد.
- أظهرت نتيجة الاختبار أن نوع المحادثة `supergroup` وأن معرّف المجموعة هو `-1003995399308`.
- يجب عدم تفعيل Telegram Trigger آخر بالبوت نفسه في Workflow آخر، لأن Telegram يسمح بعنوان Webhook واحد فقط لكل Bot في الوقت نفسه.
- الخطوة التالية هي إضافة مرشح يقبل الرسائل النصية القادمة من المجموعة المحددة فقط قبل إرسال محتواها إلى SHEIN API.
- عند إضافة أي عقدة إلى Workflow يجب توضيح اسمها المخصص داخل المشروع ونوعها الأصلي في `n8n` للمستخدم قبل إعدادها.
- تم إعداد عقدة `Filter` للتحقق من أن `message.chat.id` يساوي معرّف مجموعة Telegram المستهدفة `-1003995399308`، وأن `message.text` غير فارغ، مع الربط بين الشرطين بواسطة `AND`.
- نجح الاختبار الفعلي للعقدة وظهرت النتيجة `Kept (1 item)` مع الرسالة `Node executed successfully`، ما يؤكد مرور الرسائل النصية الواردة من المجموعة الصحيحة فقط.
- أضيفت بعد المرشح عقدة أصلية من نوع `HTTP Request` لبدء إرسال الرسالة إلى خدمة التحليل.
- ضُبطت العقدة مبدئيًا على `POST` والرابط الداخلي `http://shein-api:8000/v1/jobs`، ولا تزال إعادة تسميتها وإضافة الترويسة الآمنة والجسم مطلوبة قبل اختبارها.
- أُعيدت تسمية عقدة `HTTP Request` بنجاح إلى `إرسال طلب التحليل`، مع الإبقاء على طريقة `POST` والرابط الداخلي `http://shein-api:8000/v1/jobs`.
- تم إعداد مصادقة `Header Auth` باستخدام الرأس `X-API-Key`، وإرسال الحقلين `raw_input` و`timeout_seconds` إلى واجهة الخدمة.
- نجح اختبار عقدة `إرسال طلب التحليل`، وأعادت الخدمة `job_id` مستقلًا وحالة `queued` وروابط الحالة وملف PDF.
- أُضيفت عقدة باسم `انتظار بدء المعالجة` من النوع الأصلي `Wait` بمدة 5 ثوانٍ قبل أول فحص لحالة المهمة.
- اعتُمد التعامل مع الرسائل المتزامنة وفق الآلية التالية:
  - كل رسالة Telegram تنشئ تنفيذًا مستقلًا داخل `n8n`.
  - كل تنفيذ يرسل طلبًا مستقلًا ويحصل على `job_id` فريد.
  - تحفظ خدمة الاستخراج جميع المهام في طابور SQLite دائم بترتيب الوصول `FIFO`.
  - يعالج العامل الحالي مهمة واحدة في كل مرة لتجنب تعارض جلسات Chromium واستهلاك موارد الخادم.
  - لا تُهمل الرسائل اللاحقة؛ تبقى في الطابور حتى يحين دورها، ثم يُنشأ ملف PDF خاص بكل رسالة.
  - يجب أن تعتمد خطوات فحص الحالة وتنزيل PDF على `job_id` الخاص بالتنفيذ نفسه، دون استخدام معرّف ثابت أو آخر مهمة.
  - يجب الاحتفاظ بـ `chat_id` الأصلي داخل كل تنفيذ لإرسال ملفه إلى مجموعة Telegram الصحيحة.
- تمت إضافة عقدة `HTTP Request` باسم `فحص حالة التحليل` بعد الانتظار، وضُبط رابطها ديناميكيًا باستخدام `job_id` الخاص بكل تنفيذ.
- نجح اختبار فحص الحالة وأعاد `status=completed` و`stage=completed` دون أخطاء.
- أظهرت نتيجة الاختبار استخراج `21` منتجًا في `3` صفحات، مع `unavailable_image_count=0` وحقلي الخطأ بقيمة `null`.
- الخطوة التالية: إضافة عقدة `Switch` باسم `توجيه حالة التحليل` لتوجيه حالات `completed` و`failed`، وإعادة حالتي `queued` و`running` إلى دورة الانتظار.
## تحديث 2026-07-24 — منع الانتظار غير المحدود في n8n

- تقرر وضع مهلة مستقلة لكل طلب تحليل بدل الاعتماد على عدد دورات مشترك.
- المهلة المبدئية: `210000 ms`، أي 3 دقائق و30 ثانية من قيمة `created_at` الخاصة بالمهمة.
- الطلبات المتزامنة تبقى مستقلة لأن كل رسالة تنشئ `job_id` ووقت إنشاء منفصلين.
- أول خطوة في مسار n8n: إضافة عقدة `If` باسم `التحقق من انتهاء مهلة التحليل` بين عقدتي `فحص حالة التحليل` و`توجيه حالة التحليل`.
- عند انتهاء المهلة سيتجه الطلب لاحقًا إلى مسار فشل يرد على الرسالة الأصلية، بينما تستمر الطلبات الأخرى دون تأثر.

## تحديث 2026-07-24 — مراجعة تشغيل Workflow والمهلة وإرسال Telegram

- راجعت لقطات تشغيل `Customer_SKU` بعد تطبيق عقدة `التحقق من انتهاء مهلة التحليل`.
- الأرقام الظاهرة مثل `8 items total` ثم `15 items total` على عقد `انتظار بدء المعالجة` و`فحص حالة التحليل` هي عدد دورات الاستعلام في تنفيذ `n8n` واحد، وليست عدد رسائل Telegram جديدة ولا عدد ملفات أُرسلت.
- سبب استمرار التشغيل حتى أوقفه المستخدم هو أن مخرج `true` من عقدة المهلة لم يكن موصولًا بعد بأي عقدة؛ لذلك كان مسار `queued` أو `running` يعيد التنفيذ إلى `Wait` دون مسار إنهاء عملي.
- السلوك المقصود للرسائل المكررة هو إبقاء كل رسالة Telegram طلبًا مستقلًا: يُنشأ `job_id` جديد ويُرسل PDF جديد لكل رسالة، سواء كان الرابط مكررًا أو جديدًا. لا ينبغي إضافة منع تكرار عام ما دام هذا هو المتطلب التشغيلي.
- نجح تنزيل PDF في حالة خطأ الإرسال؛ أظهرت عقدة `تنزيل ملف PDF` بيانات ثنائية سليمة. وقع الخطأ لاحقًا في عقدة Telegram `كرر إرسال ملف PDF` بالرسالة `Bad Request: message to be replied not found`، أي أن `reply_to_message_id` لم يعد يشير إلى رسالة صالحة داخل `chat_id` المستخدم في تلك المحاولة.
- يلزم تنفيذ مسار فشل نهائي للحالة `failed` ولمخرج المهلة `true`، يرسل رسالة رد على رسالة الطلب الأصلية تتضمن السبب من `error_message`. ويلزم أيضًا مسار بديل لإرسال الملف دون رد إذا رفض Telegram معرّف الرسالة المرجعية، حتى لا تضيع الفاتورة الناجحة بسبب فشل الربط بالرد فقط.

## تحديث 2026-07-24 — استئناف مسار النسخة الاحتياطية JSON إلى المجموعة الثانية

- روجعت محادثة `Link SKU SHEIN` الأصلية لتحديد نقطة التوقف الخاصة بالنسخ الاحتياطي.
- المتطلب المعتمد: عند اكتمال التحليل يرسل `PDF` إلى المجموعة الأساسية، ويرسل إلى مجموعة النسخ الاحتياطي نص رسالة الطلب الأصلية ثم ملف `JSON` الناتج ردًا على ذلك النص، بحيث يبقى كل طلب منظمًا مع ملفه.
- معرّف مجموعة النسخ الاحتياطي المحفوظ هو `-5188698928`، وكان البوت قد أضيف إليها ومنح صلاحية إرسال الرسائل والملفات.
- أُنشئت سابقًا عقدة `إرسال النص إلى مجموعة النسخ` من نوع `Telegram / Send Message` على مخرج `completed`، لكن لم يوثق تنفيذ ناجح لها بعد ولم تضف بعد عقدتا `تنزيل ملف JSON` و`إرسال JSON إلى مجموعة النسخ`.
- الخطوة التالية المعتمدة: التحقق من إعداد عقدة النص واختبارها، ثم تنزيل JSON الخاص بالـ`job_id` الحالي وإرساله إلى المجموعة الثانية كرد على الرسالة التي أنشأتها هذه العقدة.

### تحديث التنفيذ

- تحقق التنفيذ الفعلي لعقدة `إرسال النص إلى مجموعة النسخ` بنجاح؛ أرسلت رسالة النسخ إلى المجموعة `-5188698928` وأعادت Telegram `message_id=37`.
- أضيف endpoint محمي بالمفتاح `GET /v1/jobs/{job_id}/json` داخل `src/shein_extractor/presentation/api/app.py` لتنزيل ملف JSON الخاص بمهمة مكتملة كبيانات ثنائية صالحة للإرسال من `n8n`.
- أضيف الحقل `json_url` إلى استجابة `POST /v1/jobs` لتوضيح رابط تنزيل JSON منذ لحظة إنشاء المهمة.
- لا يعيد endpoint ملفًا لمهمة قيد التنفيذ؛ يعيد `409`. وعند فشل المهمة يعيد الخطأ المنظم نفسه، وعند فقد الملف يعيد `404`.
- حدثت `README.md` واختبارات `tests/test_api.py` لتوثيق المسار والتحقق من تنزيل PDF وJSON ومن الحالات غير الموجودة.
- نتيجة التحقق: نجح الأمر `.\\.venv\\Scripts\\python.exe -m pytest tests\\test_api.py -q` بعدد `12 passed` مع تحذير خارجي واحد من Starlette، ونجح `ruff check src\\shein_extractor\\presentation\\api tests\\test_api.py`.
- يلزم نشر الإصدار المحدث إلى VPS وإعادة إنشاء حاوية `shein-api` قبل أن يمكن إضافة عقدة `تنزيل ملف JSON` في Workflow الحي.

## تحديث 2026-07-24 — توحيد توقيت تسمية JSON وPDF على GMT+3

- طلب المستخدم اعتماد منطقة زمنية ثابتة `GMT+3` في التاريخ والوقت داخل أسماء ملفات JSON وPDF، لأن الحاوية كانت تستخدم توقيتًا مختلفًا عن موقع العمل بفارق يقارب ثلاث ساعات.
- أضيفت الدالة `gmt_plus_3_time` والثابت `GMT_PLUS_3` في `src/shein_extractor/application/timezones.py`.
- أصبحت `JsonExtractionRepository.save` تحوّل وقت التحليل الصريح أو الوقت الافتراضي إلى `GMT+3` قبل إنشاء الاسم وقبل حفظ `analyzed_at` داخل JSON.
- يستمد PDF اسمه من اسم JSON عبر `report_path_for`، لذلك يحمل الملفان تلقائيًا التاريخ والوقت نفسيهما؛ لا يلزم تغيير منفصل في مولد PDF.
- الوقت الداخلي لأحداث الطابور مثل `created_at` يبقى UTC لأغراض المتابعة والحساب، ولا يغير ذلك اسم الملفات أو منطق مهلة n8n.
- أضيف اختبار: وقت `2026-07-22 08:30 UTC` ينتج الاسم الذي يحتوي `20260722-113000` ويحفظ وقت التحليل بإزاحة `+03:00`.
- نجحت الاختبارات المستهدفة بالأمر `.\\.venv\\Scripts\\python.exe -m pytest tests\\test_use_cases_and_repository.py tests\\test_server_processing.py tests\\test_api.py -q` بعدد `20 passed` مع تحذير Starlette خارجي واحد، ونجح `ruff check` للملفات المعدلة.
- يجب نشر تحديث الخدمة على VPS وإعادة بناء حاوية `shein-api` ليظهر التوقيت الجديد في الملفات الفعلية، ثم نعود إلى عقدتي تنزيل وإرسال JSON في `n8n`.

### تجهيز النشر

- رُفع إصدار الحاوية إلى `0.2.2` في `compose.hostinger.yml`، وحدث رقم إصدار API و`pyproject.toml` ليطابقه.
- أنشئت حزمة النشر `shein-extractor-hostinger-20260724-gmt3-json.tar.gz` داخل جذر المشروع. تحتوي التحديثات الخاصة بـGMT+3 وendpoint JSON ولا تحتوي `.env` أو `runtime` أو مخرجات العملاء أو بيئات Python المحلية.
- حجم الحزمة `174143` بايت وبصمتها SHA-256 هي `1B8C4A7081A0CE3A34F871BDF7FC5C319B332CED33F2039B1437C85A96362F29`.
- تحقق الأمر `tar -tzf` من قابلية قراءة الأرشيف دون أخطاء.
- يلزم نقل الحزمة إلى VPS ثم استخراج ملفات المصدر في مجلد مرحلي واستبدال ملفات المشروع فقط، مع الحفاظ على `/opt/shein-extractor/.env` و`/opt/shein-extractor/runtime`، ثم تشغيل `docker compose -f compose.hostinger.yml build --pull` و`up -d` وفحوص الصحة.

### تنفيذ النشر على VPS — المرحلة الآمنة

- رفع المستخدم الحزمة إلى `/root/shein-extractor-hostinger-20260724-gmt3-json.tar.gz` عبر `scp` وظهر اكتمال النقل بنسبة `100%`.
- تحقق المستخدم على VPS من SHA-256 للحزمة؛ تطابقت البصمة الظاهرة مع البصمة المحلية المعتمدة `1B8C4A7081A0CE3A34F871BDF7FC5C319B332CED33F2039B1437C85A96362F29`.
- استخرجت الحزمة في المجلد المرحلي `/root/shein-update-20260724-gmt3-json` دون تعديل خدمة الإنتاج، وتحقق وجود الملف `src/shein_extractor/application/timezones.py` فيه.
- أنشأ المستخدم نسخة احتياطية لملفات التطبيق الحالية في `/root/backups/shein-extractor-pre-0.2.2-20260724.tar.gz`.
- الحجم الظاهر للنسخة الاحتياطية هو `117K`، وقد استثني منها `runtime` و`.env`؛ لذلك لا تحتوي بيانات العملاء أو قاعدة الطابور أو المفتاح السري، ولم تُحذف أو تُعدل تلك البيانات.
- الحالة الحالية: الحاوية المنتجة لم تُستبدل بعد، والتحديث ما زال في المجلد المرحلي. الخطوة التالية هي نسخ ملفات التحديث إلى `/opt/shein-extractor` ثم التحقق من Compose قبل إعادة بناء الحاوية.

### تطبيق ملفات الإصدار على مجلد الخدمة

- نسخ المستخدم محتوى `/root/shein-update-20260724-gmt3-json/` إلى `/opt/shein-extractor/` بالأمر `cp -a`.
- تحقق وجود `/opt/shein-extractor/src/shein_extractor/application/timezones.py` بعد النسخ.
- تحقق أن `compose.hostinger.yml` في مجلد الخدمة يشير إلى الصورة `shein-extractor-api:0.2.2`.
- لا تزال الحاوية العاملة لم تُعد بناؤها أو إعادة إنشائها في هذه النقطة؛ لذلك لم يحدث انقطاع لخدمة التحليل حتى الآن.
- الخطوة التالية: فحص صلاحية إعداد Docker Compose، ثم بناء صورة `0.2.2`، ثم إعادة إنشاء الحاوية وفحوص الصحة.

### التحقق من إعداد Docker قبل البناء

- نفذ المستخدم من `/opt/shein-extractor` الأمر `docker compose --env-file .env -f compose.hostinger.yml config -q`.
- عاد المؤشر دون أي مخرجات أو أخطاء، وهذا يؤكد أن Docker Compose قرأ `.env` وأن تركيب الخدمة والشبكة والمجلدات وصيغة Compose صالحة للإصدار `0.2.2`.
- لم يبدأ البناء أو إعادة إنشاء الحاوية في هذه النقطة؛ تبقى خدمة التحليل بالإصدار السابق عاملة حتى تنجح خطوة البناء التالية.

### بناء صورة الإصدار 0.2.2

- نفذ المستخدم الأمر `docker compose --env-file .env -f compose.hostinger.yml build --pull` من مجلد `/opt/shein-extractor`.
- اكتمل البناء بنجاح بعد نحو `175.2` ثانية دون ظهور `ERROR`.
- أنشأ Docker الصورة `shein-extractor-api:0.2.2` وظهرت الرسالة `Image shein-extractor-api:0.2.2 Built`.
- بقيت الحاوية المنتجة السابقة عاملة أثناء عملية البناء؛ لم يبدأ الانقطاع القصير المرتبط بإعادة الإنشاء بعد.
- الخطوة التالية: تشغيل `docker compose ... up -d` لإعادة إنشاء حاوية `shein-api` بالصورة الجديدة، ثم التحقق من حالتها وفحوص الصحة.

### تشغيل الإصدار 0.2.2

- نفذ المستخدم `docker compose --env-file .env -f compose.hostinger.yml up -d` لإعادة إنشاء حاوية الخدمة.
- بدأ Docker الحاوية `shein-api` بنجاح خلال نحو `0.9` ثانية.
- أظهر `docker compose --env-file .env -f compose.hostinger.yml ps` أن الصورة المستخدمة هي `shein-extractor-api:0.2.2` وأن الحاوية بحالة `Up ... (healthy)`.
- اكتمل تبديل الخدمة إلى الإصدار الجديد، ولم تظهر إعادة تشغيل متكررة أو خطأ Docker. لا يزال مطلوبًا فحص `/health/live` و`/health/ready` والتأكد من توفر endpoint JSON والاتصال من حاوية `n8n`.

### فحوص صحة الإصدار 0.2.2 داخل الحاوية

- أعاد `/health/live` النتيجة `{"status":"ok"}` من داخل حاوية `shein-api`.
- أعاد `/health/ready` النتيجة `{"status":"ready"}` من داخل الحاوية نفسها؛ مما يؤكد جاهزية العامل والمفتاح ومجلدات التشغيل.
- فحص `openapi.json` أكد وجود المسار `/v1/jobs/{job_id}/json` وأعاد `True`.
- تحقق بذلك تشغيل تعديل GMT+3 وendpoint JSON في الصورة المنشورة. تبقى فحص الاتصال من حاوية `root-n8n-1` ثم تنفيذ طلب حقيقي لإنتاج ملف جديد والتأكد من اسمه بتوقيت GMT+3.

### فحص الاتصال من n8n وإكمال النشر التقني

- نفذ المستخدم من VPS أمر اتصال حاوية `root-n8n-1` بالخدمة الداخلية `http://shein-api:8000/health/ready`.
- أعاد الفحص `200 {"status":"ready"}`؛ مما يثبت أن `n8n` يصل إلى الإصدار `0.2.2` عبر شبكة Docker الداخلية.
- اكتمل نشر الإصدار `0.2.2` تقنيًا، بما يشمل دعم تسمية ملفات JSON/PDF بتوقيت `GMT+3` وendpoint تنزيل JSON.
- الخطوة التالية هي اختبار تشغيلي من مجموعة Telegram: إرسال طلب جديد، والتحقق من أن اسم PDF الناتج يحمل وقت `GMT+3`. وبعد تأكيد ذلك نعود لإضافة عقدتي تنزيل وإرسال JSON إلى مجموعة النسخ الاحتياطي.

## تحديث 2026-07-24 — تحليل تأخر الطابور عند الرابط المنتهي

- يعمل `ProcessingQueue` بعامل Chromium واحد وبترتيب `FIFO`، وهو قرار مقصود لحماية ذاكرة VPS ومنع تداخل جلسات المتصفح.
- إذا بدأ العامل معالجة رابط منتهي أو صفحة لا تكتمل، تبقى الرسائل اللاحقة في الطابور حتى تنتهي محاولة ذلك الرابط أو تفشل؛ حلقة الاستعلام في `n8n` لا تسبب هذا الحجز وإنما تراقب حالة كل مهمة فقط.
- لا ينبغي زيادة عدد العمال مباشرة، لأن ذلك قد يشغل عدة جلسات Chromium معًا ويزيد احتمال الضغط وفشل جميع الطلبات.
- التحسين المقترح قبل اتخاذ قرار تنفيذي: تقليل مهلة استخراج الرابط لكل مهمة مع كشف مبكر للروابط المنتهية، وإبقاء مهلة n8n النهائية كحماية مستقلة. بذلك تتحول المهمة المنتهية إلى `failed` سريعًا وتبدأ المهمة التالية دون تغيير ترتيب FIFO.

### القرار والتنفيذ المحلي

- اعتمد المستخدم حدًا أقصى قدره `60` ثانية لكل محاولة استخراج، مع إرسال رسالة فشل عند عدم النجاح، بدلاً من تقليل المهلة إلى 25–30 ثانية.
- أضيف كشف مبكر داخل `PlaywrightCartGateway`: إذا بقي الرابط في المسار `/h5/sharejump/appjump` لمدة `12` ثانية بعد محاولة الفتح من دون ظهور بيانات السلة، ترفع الخدمة `ExpiredShareLinkError` بدل إبقاء عامل FIFO منتظرًا حتى نهاية المهلة الكاملة.
- تبقى الروابط التي لا تثبت أنها منتهية ضمن مهلة `60` ثانية، ثم تفشل برسالة تحليل عامة عند بلوغ المهلة. لا يتغير عدد عمال Chromium؛ يبقى عامل واحد لحماية VPS.
- عند أي استثناء، يسجل `ProcessingQueue` المهمة بحالة `failed` ثم يطالب بالمهمة التالية فورًا، فتتحرر الفواتير اللاحقة دون انتظار حلقة n8n.
- أضيفت اختبارات لمسار `appjump`، ونجحت الاختبارات المستهدفة بالأمر `.\\.venv\\Scripts\\python.exe -m pytest tests\\test_gateway_lifecycle.py tests\\test_server_processing.py tests\\test_api.py -q` بعدد `22 passed` مع تحذير Starlette خارجي واحد. نجح كذلك `ruff check` للملفات المعدلة.
- رُفع رقم الإصدار المحلي وإعداد Docker إلى `0.2.3`. لا يزال هذا التحسين محليًا ويحتاج حزمة ونشرًا مستقلين على VPS قبل أن يؤثر في الطابور الحي.
- أنشئت حزمة النشر `shein-extractor-hostinger-20260724-expired-link-0.2.3.tar.gz` داخل جذر المشروع، بحجم `174690` بايت وبصمة SHA-256 `D917FB3AC5EB79269C354E82B655A7607D54FA667519EDC49FFE8A1FF00822D8`، وتحقق `tar -tzf` من سلامتها.
- رفع المستخدم حزمة `0.2.3` إلى `/root/shein-extractor-hostinger-20260724-expired-link-0.2.3.tar.gz` على VPS عبر `scp` بنجاح وبنسبة نقل `100%`. يلزم التحقق من البصمة على الخادم قبل استخراجها.
- تحقق المستخدم على VPS من بصمة SHA-256 للحزمة، وكانت مطابقة للبصمة المتوقعة: `D917FB3AC5EB79269C354E82B655A7607D54FA667519EDC49FFE8A1FF00822D8`. الحزمة سليمة وجاهزة للاستخراج في مجلد مرحلي، بينما تظل خدمة `0.2.2` العاملة دون أي تغيير حتى الآن.
- استخرج المستخدم حزمة `0.2.3` بنجاح إلى المجلد المرحلي `/root/shein-update-20260724-expired-link-0.2.3`. لم يظهر أي خطأ من `tar`، ولم يتم لمس `/opt/shein-extractor` أو الحاوية العاملة في هذه الخطوة.
- تحقق المستخدم من محتوى المجلد المرحلي: ظهر الملف `src/shein_extractor/infrastructure/shein/playwright_gateway.py` وظهر في `pyproject.toml` السطر `version = "0.2.3"`. تأكد بذلك أن الحزمة الصحيحة ستُنشر لاحقًا.
- أنشأ المستخدم نسخة احتياطية قبل النشر بالأمر `tar -czf /root/backups/shein-extractor-pre-0.2.3-20260724.tar.gz -C /opt/shein-extractor --exclude=./runtime --exclude=./.env .` دون ظهور خطأ. تحتوي النسخة على الشيفرة وإعدادات Compose، مع استثناء ملفات التشغيل والأسرار. سيُتحقق من وجودها وحجمها ضمن خطوة النقل التالية.
- تحقق المستخدم من النسخة الاحتياطية؛ الملف `/root/backups/shein-extractor-pre-0.2.3-20260724.tar.gz` موجود بحجم `237K`.
- نُسخت ملفات `0.2.3` من المجلد المرحلي إلى `/opt/shein-extractor/` بالأمر `cp -a .../. /opt/shein-extractor/`. لم يُعَد تشغيل الحاوية بعد، لذلك بقيت الخدمة العاملة على صورتها السابقة خلال النقل.
- اجتاز الأمر `docker compose --env-file .env -f compose.hostinger.yml config -q` دون مخرجات أو أخطاء، ما يثبت صحة إعداد Compose بعد النقل.
- اكتمل بناء صورة Docker الجديدة بنجاح بالأمر `docker compose --env-file .env -f compose.hostinger.yml build --pull` خلال نحو `142` ثانية، وحملت الصورة الاسم `shein-extractor-api:0.2.3`.
- نفذ المستخدم `up -d` وبدأت الحاوية `shein-api` بالإصدار `0.2.3`. عند أول فحص مباشر كانت حالتها `Up less than a second (health: starting)`، وهي حالة تهيئة متوقعة بعد البدء مباشرة. يلزم فحص health بعد انتظار قصير قبل اعتماد النشر.
- فحوص النشر النهائية للإصدار `0.2.3` نجحت: أظهر `docker compose ... ps` الحاوية `shein-api` بالصورة `shein-extractor-api:0.2.3` والحالة `Up ... (healthy)`.
- أعاد `/health/live` من داخل الحاوية `{"status":"ok"}`، وأعاد `/health/ready` `{"status":"ready"}`.
- تحقق الاتصال من حاوية `root-n8n-1` إلى `http://shein-api:8000/health/ready` وأعاد `200 {"status":"ready"}`. أصبح إصدار الكشف المبكر للرابط المنتهي `0.2.3` فعّالًا ومتاحًا لـ n8n.
- اختبار قبول أولي من Telegram بعد نشر `0.2.3`: أرسل المستخدم رابطًا منتهيًا ثم رابطًا صالحًا مباشرة بعده. تجاوزت المهمة الصالحة المهمة المنتهية وأنتجت PDF بنجاح، مما يؤكد أن فشل الرابط الأول لم يعد يحجز عامل FIFO حتى المهلة الكاملة.
- لم تصل رسالة فشل للرابط المنتهي. التحليل الحالي في n8n: عقدة `التحقق من انتهاء مهلة التحليل` تختبر فقط حالتي `queued` و`running`؛ عند وصول الحالة `failed` تكون النتيجة `false` طبيعيًا، ثم يجب أن تتولى عقدة `توجيه حالة التحليل` فرع `failed`. عدم وجود عقدة Telegram فعالة على هذا الفرع (أو عدم إعدادها) هو سبب عدم إرسال رسالة الفشل، وليس شرط المهلة الظاهر في لقطة الشاشة.
- مراجعة إعدادات n8n أكدت أن عقدة `إرسال فشل انتهاء المهلة` متصلة بفرع `true` لعقدة المهلة فقط؛ لذلك لا يمكنها استقبال حالة API العادية `failed`. كما تستخدم `Chat ID` بصيغة `.item` بينما يستخدم `Reply To Message ID` صيغة `.first()`؛ هذا لا يفسر غياب رسالة الفشل الحالية، لكنه سيُوحَّد لاحقًا ضمن عقدة حفظ السياق لتحسين الثبات.
- راجع المستخدم قواعد عقدة `توجيه حالة التحليل`: توجد قواعد `completed` و`queued` و`running` فقط، ولا توجد قاعدة `failed` حاليًا. اقترح المستخدم ربط خرج `failed` بعقدة `إرسال فشل انتهاء المهلة` القائمة. التوصية المعتمدة: إعادة استخدام العقدة لتقليل التكرار، مع تحويل نصها إلى Expression ديناميكي يفرّق بين مهلة n8n وفشل API ويعرض `error_message` عند توفره؛ لا ينبغي ربطها بالنص الثابت الحالي لأنه سيصف كل الأخطاء خطأً بأنها مهلة.
- أضاف المستخدم في عقدة `توجيه حالة التحليل` قاعدة رابعة: `{{ $json.status }}` يساوي `failed`، وسمى المخرج `فشل`. كما ربط هذا المخرج بعقدة `إرسال فشل انتهاء المهلة` القائمة. بقي تغيير نص العقدة إلى تعبير ديناميكي وتوحيد `Chat ID` قبل نشر الـWorkflow واختباره.
- اعتمد نص رسالة المهلة الحالي للمستخدم كما هو من حيث المحتوى: يذكر المهلة `3 دقائق و30 ثانية` والأسباب المحتملة وإرسال رابط جديد. ستصبح عقدة Telegram نفسها ذات نص ديناميكي؛ في فرع `failed` ستعرض سبب الخدمة `error_message`، بينما يبقى النص المعتمد للمهلة في الفرع الآخر.
- عدّل المستخدم عقدة الرسالة إلى `إرسال فشل التحليل`، ووحّد `Chat ID` إلى `$('Telegram Trigger').first().json.message.chat.id`، وأدخل النص الديناميكي لفرعي `failed` والمهلة. عند محاولة `Execute workflow` ظهر تنبيه n8n: لا يمكن لـ Telegram Trigger الاستماع لاختبارات يدوية وإنتاجية في الوقت نفسه لأن الـWorkflow منشور/نشط. هذا قيد اختبار طبيعي وليس خطأ في Expression أو الربط.
- اختبار يدوي بعد إلغاء نشر الـWorkflow نجح: أرسل المستخدم رابطًا منتهيًا، ووصلت عقدة `إرسال فشل التحليل` إلى التنفيذ وأرسلت رسالة فشل كرد على الرسالة الأصلية. احتوت الرسالة على `error_message` الفعلي: أن رابط مشاركة SHEIN منتهي الصلاحية أو لم يعد يحتوي على سلة متاحة. تحقق بذلك مسار `failed` والنص الديناميكي والرد المرجعي بنجاح. يلزم إعادة نشر الـWorkflow قبل العودة إلى التشغيل الإنتاجي.
- أعاد المستخدم نشر الـWorkflow، ثم أكد اختبار إنتاجي أن مسار `failed` يعمل: المهمة المنتهية بدأت عند `05:07:01.529653Z` وانتهت بفشل `ExpiredShareLinkError` عند `05:07:14.808893Z` (نحو 13.3 ثانية)، وأرسلت عقدة `إرسال فشل التحليل` الرسالة بنجاح. هذا يثبت كفاءة الكشف المبكر للطابور.
- مراجعة مهلة n8n: الشرط الحالي يستخدم `created_at` ومدة `210000` مللي ثانية (3 دقائق و30 ثانية). مهلة المعالجة داخل الخدمة هي 60 ثانية؛ لذلك لا يوصى بجعل حارس n8n 60 ثانية مطابقة لها، إذ قد ينهي n8n التنفيذ قبل وصول فشل الخدمة الطبيعي عند حد 60. التوصية: حارس n8n لمدة 90 ثانية، محسوبًا من `started_at` فقط عند الحالة `running`، لتجنب احتساب وقت انتظار FIFO كفشل ولإتاحة هامش آمن فوق مهلة الخدمة.
- ملاحظة المستخدم قبل تعديل الحارس: قد يرسل عدة روابط متتابعة، منها صالح ومنها منتهٍ. التوضيح: كل رسالة Telegram تنشئ `job_id` وتنفيذ n8n مستقلين، وFIFO يحافظ على ترتيب المعالجة داخل الخدمة. الخطر الفعلي هو شرط n8n الحالي لأنه يضم `queued` ويحسب من `created_at`؛ قد يعلن فشل رابط صالح وهو ينتظر دوره. الشرط المقترح يقتصر على `running` ويحسب من `started_at`، فلا يلغي أو يوقف مهام الروابط المنتظرة والصحيحة.
- أدخل المستخدم مؤقتًا قيمة `150000` مللي ثانية (150 ثانية) في شرط المهلة مع إبقاء `['queued', 'running']` و`created_at`. لا يُعتمد هذا التعديل بعد، لأنه ما زال يعرّض الروابط الصالحة المنتظرة في FIFO لرسالة فشل خاطئة. يلزم استبدال Expression كاملًا بالصيغة التي تعتمد `running` و`started_at` فقط قبل النشر.
- صحح المستخدم شرط المهلة إلى: الحالة `running` فقط، ووجود `started_at`، وفرق زمني قدره `150000` مللي ثانية (150 ثانية). أصبح الحارس لا يؤثر في المهام `queued` المنتظرة ضمن FIFO. بقي نشر هذا التعديل في n8n ليصبح فعالًا في الإنتاج.
- عدّل المستخدم الحارس من 150 إلى `90000` مللي ثانية (90 ثانية) بعد التأكد من أن العد يبدأ من `started_at` في حالة `running` فقط. هذا هو الإعداد المعتمد: مهلة الخدمة الداخلية 60 ثانية، وحارس n8n 90 ثانية كهامش حماية، من دون تأثر المهام المنتظرة في FIFO. بقي نشر التعديل إن لم يكن قد نُشر بعد.
- نشر المستخدم تعديل حارس المهلة في n8n. أصبح الشرط الفعّال في الإنتاج يعتمد `running + started_at + 90000ms`.
- بدأ المستخدم مسار النسخة الاحتياطية JSON في n8n: أضاف عقدة HTTP باسم `تنزيل ملف JSON` بعد `إرسال النص إلى مجموعة النسخ`. إعدادها `GET` إلى `/v1/jobs/{job_id}/json` باستخدام `job_id` من عقدة `فحص حالة التحليل`، وبمصادقة Header Auth نفسها، واستجابة `File` في الحقل الثنائي `data`. الإعداد والرابط صحيحان؛ بقيت عقدة إرسال المستند إلى المجموعة الثانية ثم الاختبار والنشر.
- أكمل المستخدم عقدة `إرسال JSON إلى مجموعة النسخ` وربطها بعد `تنزيل ملف JSON`. تستخدم `DmG73Bot` و`Send Document` وChat ID `-5188698928` والملف الثنائي `data`، مع `Reply To Message ID` يشير إلى `$('إرسال النص إلى مجموعة النسخ').first().json.result.message_id`. الإعداد والربط صحيحان؛ بقي نشر الـWorkflow واختبار فاتورة صالحة واحدة للتأكد من وصول النص ثم JSON كرد عليه في المجموعة الثانية.
- اختبار إنتاجي لمسار النسخ الاحتياطي JSON نجح: عولج رابط صالح للطلب `T-490`، ووصل ملف PDF إلى المجموعة الأولى، ووصلت رسالة الفاتورة النصية إلى مجموعة JSON ثم وصل ملف JSON (`54.4 KB`) كردّ مباشر على تلك الرسالة. هذا يثبت عمل endpoint `/v1/jobs/{job_id}/json`، والتنزيل الثنائي في الحقل `data`، وربط الرد في Telegram للمجموعة الثانية.
- أضاف المستخدم عقدة `حفظ سياق الطلب` بعد `فلترة رسائل المجموعة` وقبل `إرسال طلب التحليل`. تحفظ العقدة `origin_chat_id` و`origin_message_id` و`original_text` من رسالة Telegram، مع تفعيل `Include Other Input Fields`. موضعها وإعدادها صحيحان؛ لم يُنشر التعديل بعد، والخطوة التالية هي تحويل العقود اللاحقة تدريجيًا لاستخدام هذه الحقول المحفوظة بدل الرجوع إلى `Telegram Trigger`.
- مراجعة صور إعدادات العقد أكدت أن عقدتي `فحص حالة التحليل` و`تنزيل ملف PDF` و`تنزيل ملف JSON` تستخدمان `job_id` بالطريقة الصحيحة ولا تحتاجان تعديلًا. التعديلات المطلوبة لحفظ السياق تقتصر على: `raw_input` في `إرسال طلب التحليل`، وحقلي Chat/Reply في `إرسال فشل التحليل` و`كرر إرسال ملف PDF`، وحقل النص في `إرسال النص إلى مجموعة النسخ`. يجب ترك `إرسال JSON إلى مجموعة النسخ` كما هو، لأنه يرد على رسالة المجموعة الثانية لا على الرسالة الأصلية.
- تحقق من الكود المحلي أظهر أن الخدمة تسمح للطلب القادم من n8n بتحديد `timeout_seconds` حتى 300 ثانية، رغم أن القيمة الافتراضية داخل الخدمة هي 60 ثانية. وبما أن عقدة `إرسال طلب التحليل` ترسل حاليًا القيمة الثابتة `180`، فهي تتجاوز الافتراضي؛ يجب تغييرها إلى `60` لتحقيق مهلة المعالجة المتفق عليها.

### تحسينات مقترحة قبل نشر 0.2.3

- الأولوية الأولى: إكمال مسار `failed` في `n8n` ليجيب على رسالة الطلب الأصلية بسبب الفشل القادم من `error_message`، إضافة إلى مسار المهلة الموجود حاليًا.
- الأولوية الأولى: حفظ سياق Telegram فور الاستقبال في عقدة `Edit Fields` واحدة (`origin_chat_id` و`origin_message_id` و`original_text`) واستخدامه في الفروع اللاحقة. يقلل ذلك خطر فقدان ربط العناصر بعد `Wait` أو إرسال رد إلى معرّف قديم.
- الأولوية الأولى: إضافة حل بديل لإرسال PDF أو JSON دون `Reply To Message ID` إذا رفض Telegram رسالة الرد المرجعية؛ لا ينبغي أن تضيع نتيجة تحليل ناجحة بسبب فشل ربط الرد فقط.
- الأولوية الثانية: إعداد مسار أخطاء عام لـWorkflow في `n8n` يرسل تنبيهًا تقنيًا منظمًا إلى مجموعة النسخ الاحتياطي عند فشل عقدة HTTP أو Telegram، مع `job_id` وسبب الخطأ، دون إعادة تشغيل المهمة تلقائيًا.
- الأولوية الثانية: اختبار قبول قصير بعد كل نشر: رابط صالح واحد ورابط منتهي واحد، مع التحقق من اسم الملف بتوقيت GMT+3 وحالة المهمة ورسالة الفشل والطابور اللاحق.
- الأولوية الثالثة: مراقبة مساحة `runtime` والتنبيه قبل الامتلاء، لأن الاحتفاظ الدائم بملفات JSON وPDF قرار معتمد وقد يؤدي إلى نمو التخزين مع الوقت.
- لا يوصى بمنع الرسائل المكررة أو بإضافة عمال Chromium متوازية في المرحلة الحالية؛ يتعارض الأول مع متطلب معالجة كل رسالة، ويرفع الثاني الضغط على VPS.

### اعتمادات المستخدم

- اعتمد المستخدم تنفيذ مسار `failed` لإرسال سبب الفشل كرد على رسالة الطلب الأصلية.
- اعتمد حلًا بديلًا لإرسال الملفات دون رد عند رفض Telegram للرسالة المرجعية، كي لا تضيع نتيجة ناجحة.
- اعتمد إرسال تنبيه تقني منظم إلى مجموعة النسخ الاحتياطي عند فشل عقدة تقنية في Workflow.
- اعتمد اختبار قبول بعد كل نشر، يشمل رابطًا صالحًا ورابطًا منتهيًا وفاتورة صحيحة بعده.
- اعتمد إضافة مراقبة لمساحة التخزين لاحقًا، مع تنبيه فقط ودون حذف تلقائي للملفات.
- يحتاج قرار حفظ سياق Telegram داخل `Edit Fields` إلى شرح قبل البدء به؛ لا يغير هذا المقترح محتوى الرسائل أو يضيف قاعدة بيانات، بل يحفظ بيانات الرسالة الأصلية داخل تنفيذ n8n الواحد لتستخدمها الفروع اللاحقة بثبات.
- اعتمد المستخدم أيضًا عقدة `حفظ سياق الطلب` داخل `n8n`. ستوضع بعد الفلتر وقبل طلب التحليل، وتحفظ `origin_chat_id` و`origin_message_id` و`original_text` لاستخدامها في إرسال PDF وJSON ورسائل النجاح والفشل والبديل دون الاعتماد على ربط العناصر بعد `Wait`.

## تحديث 2026-07-24 — مراجعة ربط سياق الطلب

- راجعنا إعدادات عقدة `حفظ سياق الطلب` في `n8n`: تحفظ `origin_chat_id` و`origin_message_id` و`original_text` مع إبقاء الحقول الواردة؛ وهذا هو الأساس لمنع اختلاط الردود عند وصول روابط متتابعة.
- عُدّل حقل `raw_input` في عقدة `إرسال طلب التحليل` ليستخدم `{{ $json.original_text }}`، وضُبط `timeout_seconds` على `60` ثانية. هذه مهلة خدمة الاستخراج نفسها.
- عُدّلت عقدتا `إرسال فشل التحليل` و`كرر إرسال ملف PDF` لاستخدام `origin_chat_id` و`origin_message_id` من عقدة `حفظ سياق الطلب`، ليكون الرد أو الملف متعلقًا بالرسالة الأصلية لنفس التنفيذ.
- عُدّلت عقدة `إرسال النص إلى مجموعة النسخ` لاستخدام `original_text` المحفوظ. تبقى عقدة إرسال ملف JSON مرتبطة برسالة النسخ التي ترسلها هذه العقدة داخل المجموعة الثانية، وهو الربط الصحيح للمجموعة الثانية.
- لم يُسجَّل بعد اختبار إنتاج نهائي لهذه التعديلات مجتمعة. الخطوة التالية هي النشر ثم اختبار رابط منتهٍ يتبعه رابط صالح للتأكد أن كل رد وملف يعود إلى رسالته الأصلية.

## تحديث 2026-07-24 — اختبار قبول حفظ سياق الطلب

- بعد نشر تعديل `حفظ سياق الطلب`، أرسل المستخدم رسالتين متتاليتين إلى المجموعة الأولى عند `08:53`: الأولى للفاتورة `H-2195` برابط SHEIN منتهٍ، والثانية للفاتورة `T-490` برابط صالح.
- وصلت رسالة الفشل الخاصة بالفاتورة `H-2195` في السياق الصحيح، واشتملت على سبب الفشل القادم من الخدمة: رابط مشاركة SHEIN منتهي الصلاحية أو لم يعد يحتوي سلة متاحة.
- استمرت معالجة الفاتورة `T-490` بصورة مستقلة، ووصل ملف PDF الناتج عند `08:54`.
- النتيجة المتحققة: فشل رابط لا يلغي ولا يغير معالجة الرابط الذي يليه، وحفظ `origin_chat_id` و`origin_message_id` و`original_text` منع اختلاط الردود والملفات بين الطلبات المتتابعة.

### الخطوة التالية

- اختبار المسار نفسه في مجموعة JSON: إرسال رابط منتهٍ ثم رابط صالح، والتحقق من أن نص الفاتورة وملف JSON للطلب الصالح فقط يصلان إلى المجموعة الثانية، وأن JSON يرد على نص الفاتورة المطابق.
